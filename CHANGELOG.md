# WORLD FILM — บันทึกการแก้ไข (Change Log)

บันทึกทุกคำสั่ง (prompt) ของผู้ใช้ และสรุปสิ่งที่แก้ไขในแต่ละครั้ง

---

## ครั้งที่ 1 — 2026-06-29

**Prompt:**
> ตัดตรงใบเสนอราคาออก และเปลี่ยนเบอร์โทรและไลน์ซึ่งข้อมูลมีอยู่ในไฟล์เป็นรูปเบอร์โทรและไลน์ และตรงฟิล์มแทบเมนูเพิ่มตัวเลือกให้กดเข้าไปซึ่งจะมีอยู่ 4 ประเภทใหญ่ ๆ ของฟิล์ม เช่น 1.finnix 2.3M 3.regionfilm 4.Ultra Guard แล้วก็ตรง logo worldFilm เปลี่ยน logo มีภาพไฟล์ logo อยู่ในเครื่อง

**สิ่งที่แก้ไข:**
- ลบปุ่ม/ลิงก์ "ขอใบเสนอราคา" ออกทั้งหมด (หน้าแรก + 3 หน้าสินค้า)
- เปลี่ยนเบอร์โทร/ไลน์ในส่วน "ติดต่อเรา" เป็นรูปปุ่ม `line.png` / `call.png`
- ทำเมนู "ฟิล์มของเรา" เป็น dropdown 4 แบรนด์ + เพิ่มเซกชันแบรนด์ในหน้าแรก
- เปลี่ยนโลโก้ WORLD FILM เป็นรูปจริง (สร้าง `logo-dark.png` พื้นอ่อน / `logo-white.png` พื้นเข้ม) ทุกหน้า

---

## ครั้งที่ 2 — 2026-06-29

**Prompt:**
> Product-Cm15 / Product-cm35 / Product-xt เอาออกไปเลย และตรงลูกค้าแนะนำต่อ 90% รวมถึงช่อง comment ของลูกค้าเอาออกเลย และตรงหน้า Product พอแยกเป็นแต่ละแบรนด์เสร็จแล้ว สร้างหน้าแต่ละหน้าของแบรนด์นั้น ๆ ว่ามีรุ่นอะไรบ้าง เพื่ออธิบายว่าแต่ละรุ่นแตกต่างกันอย่างไร และรูป LOGO finnix เอาไฟล์ `LOGO FINNIX-01.png` ไปใส่แทน และจัดเว็บไซต์ทุกหน้าให้รองรับมือถือทุกรุ่นให้สมดุลเห็นครบทุกส่วน ส่วนหน้าอธิบายแต่ละรุ่นนำดีไซน์ product-15/35/xt มาใช้ และทุกครั้งที่พิมพ์ต้อง save prompt ทุกครั้ง

**สิ่งที่แก้ไข:**
- ลบเซกชัน "ฟิล์มเซรามิก Finnix Ceramic" (การ์ด CM15/XT/CM35) ออกจากหน้าแรก
- ลบเซกชันรีวิว "ลูกค้าแนะนำต่อ 90%" + ช่อง comment ลูกค้า ออกจากหน้าแรก
- เปลี่ยนโลโก้ Finnix ในการ์ดแบรนด์เป็น `LOGO FINNIX-01.png`
- เมนู dropdown + การ์ดแบรนด์ในหน้าแรก ลิงก์ไปหน้าแบรนด์ใหม่ (brand-*.html)
- สร้างหน้าแบรนด์ 4 หน้า: `brand-finnix.html`, `brand-3m.html`, `brand-regionfilm.html`, `brand-ultraguard.html` (รวมรายชื่อรุ่น + อธิบายความต่าง)
- สร้างหน้ารุ่นใหม่ 10 หน้า (ดีไซน์เดียวกับ product-cm/xt) ใส่สเปกตัวอย่าง:
  - Finnix: Titanium, UV Guard, Extra Clear, Phoenix
  - 3M: Ceramic Ultra Clear, Ceramate
  - Regionfilm: Ceramic, Smart
  - Ultra Guard: Ceramic, Nano
  - ทุกหน้ารุ่นที่ใส่สเปกตัวอย่างมีแถบ "หมายเหตุ: ตัวเลขสเปก…เป็นข้อมูลตัวอย่าง" — แก้ตัวเลขจริงได้ภายหลัง แล้วลบ block `.sample-note` ใน product.css
- ปรับหน้า product-cm15/cm35/xt เดิม: dropdown + breadcrumb + footer ลิงก์ไปหน้าแบรนด์
- เพิ่ม CSS รองรับมือถือ (เบรกพอยต์ 480/600/760/980) ทั้ง index และ product.css ให้สมดุลทุกขนาดจอ

**หมายเหตุ (รุ่นที่ใช้สเปกตัวอย่าง — รออัปเดตข้อมูลจริง):**
model-finnix-titanium, model-finnix-uvguard, model-finnix-extra-clear, model-finnix-phoenix,
model-3m-ultra-clear, model-3m-ceramate, model-regionfilm-ceramic, model-regionfilm-smart,
model-ultraguard-ceramic, model-ultraguard-nano

---

## ครั้งที่ 3 — 2026-06-29

**Prompt:**
> ฟิล์ม Finnix หน้า Product (มีปุ่มทักไลน์ปรึกษาฟรี และ 095-229-2086) — ย้าย Finnix Ceramic CM 15 ถึง 35 เข้าไปเป็นรุ่น "Finnix Ceramic" รุ่นเดียว ซึ่ง 1 รุ่นจะมีสเปกฟิล์ม 3 ตัวด้านใน คือ 35, 15, 05 แต่ละสเปกบอกคุณสมบัติของฟิล์มสเปกนั้น และลบ Finnix XT ออกไปเลย ลบ Finnix Phoenix ออกไปด้วย

**สิ่งที่แก้ไข:**
- รวม CM 15 + CM 35 เข้าเป็นรุ่นเดียว "Finnix Ceramic" → หน้า `model-finnix-ceramic.html` แสดง 3 เฉดสเปก: CM 05 / CM 15 / CM 35 พร้อมคุณสมบัติแต่ละเฉด (CM 05 เป็นสเปกตัวอย่าง รออัปเดต)
- ลบ Finnix XT (`product-xt.html`) และ Finnix Phoenix (`model-finnix-phoenix.html`) ออก
- ลบหน้าแยกเดิม `product-cm15.html`, `product-cm35.html` (เนื้อหาย้ายเข้าหน้า Finnix Ceramic แล้ว)
- หน้าแบรนด์ Finnix เหลือ 4 รุ่น: Ceramic, Titanium, UV Guard, Extra Clear
- เพิ่ม CSS การ์ดเฉดฟิล์ม (.shadegrid/.shadecard) ใน product.css รองรับมือถือ

---

## ครั้งที่ 4 — 2026-06-29

**Prompt:**
> แก้ส่วน "ติดต่อเรา" (พร้อมให้คำปรึกษาทุกพื้นที่): เปลี่ยนรูป LINE/เบอร์โทรให้เข้ากับธีม, เปลี่ยน Location เป็นเพจ https://www.facebook.com/Worldsfilm1 — โลโก้ 3M ใช้ไฟล์ 3M_7.svg แทน — logo WorldFilm กับเมนู "หน้าแรก" คลิกแล้วไปหน้าแรกของเว็บเลย — แถว "ทำไมต้องติดฟิล์ม" (4 ช่อง) เอาเอฟเฟกต์แสง/เงาตอน hover ออก — แถว "แบรนด์ฟิล์มของเรา" (4 ช่อง) ทำให้คลิกตรงไหนก็ได้ในช่องแล้วไปหน้าแบรนด์นั้น

**สิ่งที่แก้ไข:**
- ติดต่อเรา: เปลี่ยนปุ่มรูป LINE/โทร เป็นการ์ดไอคอนสไตล์ธีม (วงกลมกรมท่า + ไอคอนทอง) และเปลี่ยนการ์ด Location → Facebook (เพจ Worldsfilm1)
- อัปเดตลิงก์ Facebook ทั้งใน topbar และ footer เป็น https://www.facebook.com/Worldsfilm1
- โลโก้ 3M (การ์ดหน้าแรก + หน้า brand-3m) เปลี่ยนเป็น `3M/3M_7.svg.png`
- โลโก้ WorldFilm และเมนู "หน้าแรก" ในหน้าแรก ลิงก์ไป `index.html` (ไปหน้าแรกของเว็บโดยตรง)
- เอา hover effect (ยก + เงา) ของการ์ด "ทำไมต้องติดฟิล์ม" (.bcard) ออก
- การ์ดแบรนด์ทั้ง 4 (.brandcard) เปลี่ยนเป็น `<a>` ทั้งช่อง คลิกตรงไหนก็เด้งไปหน้าแบรนด์นั้น

---

## ครั้งที่ 5 — 2026-06-29

**Prompt:**
> ลบไฟล์ HTML ที่ไม่ได้ใช้งาน/ไม่จำเป็นออก และจัดระเบียบไฟล์ทั้งโปรเจกต์ให้เรียงสวยงาม คนอื่นเข้ามาเข้าใจง่าย

**สิ่งที่แก้ไข (จัดโครงสร้างโปรเจกต์ใหม่):**
- ลบไฟล์ `index.html` เปล่า (0 ไบต์) ที่ root ทิ้ง แล้วย้ายหน้าแรกจริงมาไว้ที่ root `index.html` (มาตรฐานเว็บ เปิดไฟล์เดียวเจอหน้าแรกเลย)
- ย้ายหน้าแบรนด์/หน้ารุ่นทั้งหมด + `product.css` ไปไว้ในโฟลเดอร์ `products/`
- รวมรูปที่ใช้งานไว้ใน `assets/` (โลโก้) และ `assets/brands/` (โลโก้แบรนด์ ตั้งชื่อสั้นไม่มีเว้นวรรค: finnix/3m/regionfilm/ultraguard.png)
- ย้ายไฟล์ภาพต้นฉบับ/ที่ไม่ได้ใช้บนเว็บไปเก็บใน `assets/source/` (ไม่ลบทิ้ง เผื่อใช้อนาคต)
- ลบโฟลเดอร์เก่า `index/` และ `Product/` ทิ้ง
- อัปเดต path ภายในทุกไฟล์ HTML ให้ตรงโครงสร้างใหม่ (ตรวจลิงก์/รูปครบ ไม่มีเสีย)
- เพิ่ม `README.md` อธิบายโครงสร้างไฟล์ ผังการเชื่อมหน้า และวิธีแก้ไขสิ่งที่ใช้บ่อย

---

## ครั้งที่ 6 — 2026-06-29

**Prompt:**
> สลับตำแหน่ง: ย้ายแถว "ทำไมต้องติดฟิล์ม" (4 ช่องคุณสมบัติ) ลงล่าง และย้าย "แบรนด์ฟิล์มของเรา" (4 ช่อง) ขึ้นบนแทน — และเสนอว่าหน้า product/ฟิล์มของเราควรดีไซน์ยังไงให้เข้าใจง่าย ไม่ซับซ้อน กดเข้าไปแล้วรู้เลยว่าแต่ละแบรนด์มีกี่รุ่น และแต่ละรุ่นมีกี่สเปก

**สิ่งที่แก้ไข:**
- สลับลำดับเซกชันในหน้าแรก: "แบรนด์ฟิล์มของเรา" ขึ้นไปอยู่เหนือ "ทำไมต้องติดฟิล์ม" (ปรับสีพื้นสลับโทนให้ลงตัว)
- ออกแบบลำดับชั้นให้เห็นจำนวนชัดเจน (3 ระดับ ไม่ซับซ้อน):
  - หน้าแรก — การ์ดแบรนด์แต่ละช่องมีป้าย "N รุ่น" (Finnix 4, ที่เหลือ 2)
  - หน้าแบรนด์ — หัวข้อบอกจำนวน "· N รุ่น" + การ์ดแต่ละรุ่นมีแถบสเปกย่อ (VLT · ลดความร้อน · UV) ใต้ชื่อรุ่น เห็นสเปกทันทีโดยไม่ต้องกดเข้า; รุ่น Finnix Ceramic ระบุ "3 เฉด (CM 05/15/35)" ให้เห็นว่ามี 3 สเปก
  - หน้ารุ่น — แสดงสเปกเต็ม
- อัปเดตตัวสร้างหน้า (generator) ให้ตรงโครงสร้างใหม่ และเพิ่มจำนวนรุ่น + แถบสเปกย่อ แล้วสร้างหน้าแบรนด์/รุ่นใหม่ทั้งหมด (ตรวจลิงก์/รูปครบ)

---

## ครั้งที่ 7 — 2026-06-29

**Prompt:**
> ในหน้าแบรนด์ (ฟิล์มของเรา) เอาโลโก้ FINNIX FILM + ประโยคอธิบาย "ฟิล์ม Finnix" ออก, เอาปุ่มสีเหลือง (ทักไลน์) + เบอร์โทรออกทั้งแถว แล้วเอาแถว "รุ่นฟิล์ม Finnix 4 รุ่น" ขึ้นมาแทน จัดองค์ประกอบใหม่ให้ดูเรียบ ๆ แต่ดูแพง

**สิ่งที่แก้ไข (หน้าแบรนด์ทั้ง 4 — ผ่าน template):**
- เอาส่วนหัวแบรนด์ (โลโก้แบรนด์ + หัวข้อ "ฟิล์ม X" + ย่อหน้าอธิบาย + ปุ่มทักไลน์/โทร) ออกทั้งหมด
- ย้ายเซกชันรุ่นฟิล์มขึ้นเป็นเซกชันแรก (ถัดจาก breadcrumb) — หัวข้อ "ฟิล์ม X · N รุ่น" + "เลือกรุ่นที่ใช่สำหรับคุณ"
- ปรับดีไซน์ให้ดูพรีเมียมเรียบหรู: พื้นขาวสะอาด เว้นช่องไฟเยอะขึ้น, การ์ดรุ่นมีเส้นทองไล่เฉดโผล่ตอน hover + ยกการ์ดเงานุ่ม, แถบสเปกย่อปรับเป็นโทนครีมนวลไม่มีกรอบ
- เก็บแถบ CTA ล่างสุด (ทักไลน์/โทร) + ปุ่มลอยไว้ตามเดิม เพื่อให้ติดต่อได้

---

## ครั้งที่ 8 — 2026-07-01

**Prompt:**
> เช็กทุกไฟล์ให้ละเอียดแก้ไขไฟล์ทุกไฟล์

**สิ่งที่แก้ไข (ตรวจสอบทั้งโปรเจกต์ + จัดการไฟล์ค้าง):**
- ตรวจไฟล์ที่ใช้งานจริง (index.html + products/ 14 หน้า + product.css) แบบละเอียด — ผลคือ **สมบูรณ์ทั้งหมด ไม่มีจุดต้องแก้**:
  - encoding ภาษาไทยปกติ ไม่มี mojibake, ทุกไฟล์ `charset=UTF-8` + `lang="th"` + มี `<title>` ครบ
  - ลิงก์/รูปทุกอัน (src/href) resolve ได้จริงบนดิสก์ ไม่มี path เสีย
  - anchor หน้าแรกครบทั้ง 5 จุด (#why #work #process #contact #brands)
  - แท็กโครงสร้างสมดุลทุกไฟล์ (html/head/body/header/footer/div), CSS วงเล็บปีกกาสมดุล 197/197
- **พบ + จัดการไฟล์ค้าง:** โฟลเดอร์ `Product/` (P ใหญ่) มีไฟล์เก่าก่อนจัดโครงสร้าง 7 ไฟล์ (5 model + product-cm15/cm35) — ลิงก์ข้างในยังชี้ path เก่าที่ถูกลบแล้ว (`../index/index.html`, `../index/PIC/…`) และไม่มีหน้าไหนลิงก์หา = ไฟล์ตายซ้ำซ้อน
  - ย้ายทั้งหมดไปเก็บที่ `assets/source/_old-pre-reorg/` (ไม่ลบถาวร เผื่อใช้อนาคต ตามแนวทางครั้งที่ 5) แล้วลบโฟลเดอร์ `Product/` ทิ้ง
  - root tree สะอาดเหลือเฉพาะที่ใช้งาน: `index.html`, `products/`, `assets/`, `README.md`, `CHANGELOG.md`
- ยืนยันหลังย้าย: ไม่มีลิงก์ไซต์ไหนพัง, README ไม่ได้อ้างอิง path เก่า

---

## ครั้งที่ 9 — 2026-07-01

**Prompt:**
> งั้นจัดการไฟล์ Product และเรียงไฟล์ที่เหลือให้เป็นระเบียบ

**สิ่งที่แก้ไข (ปิดจ๊อบไฟล์ Product + จัดระเบียบ):**
- **ไฟล์ 0 ไบต์โผล่กลับ:** โฟลเดอร์ `Product/` ถูก editor สร้างกลับมาพร้อมไฟล์เปล่า `model-ultraguard-ceramic.html` (0 ไบต์, สร้าง 12:30 หลังการย้าย) — ตัวจริง 15KB ปลอดภัยในคลังแล้ว, ลบไฟล์เปล่า + โฟลเดอร์ `Product/` ทิ้ง (ไม่มีหน้าไหนลิงก์หา)
  - ⚠️ เตือนผู้ใช้ให้ปิดแท็บ `Product/model-ultraguard-ceramic.html` ใน editor กันโฟลเดอร์ถูกสร้างกลับมาซ้ำ
- **ไฟล์เก่า 7 ไฟล์ในคลัง:** ผู้ใช้เลือก "เก็บคลังไว้" (โปรเจกต์ไม่มี git จึงไม่ลบถาวร) — คงไว้ที่ `assets/source/_old-pre-reorg/`
- เพิ่ม `assets/source/_old-pre-reorg/README.md` อธิบายว่าไฟล์พวกนี้คือเวอร์ชันเก่า path เสีย ห้ามนำไปใช้ + ตารางชี้ตัวจริงใน `products/`
- อัปเดต `README.md` หลัก: เพิ่ม `_old-pre-reorg/` ในผังโครงสร้าง + เปลี่ยนวันที่อัปเดตล่าสุดเป็น 2026-07-01
- ยืนยันผังสุดท้าย: root เหลือ index.html / products/ (14 html + 1 css) / assets/ (2 โลโก้ + brands 4 + source 10 + คลัง 7) — ลิงก์/รูปไซต์ครบ ไม่มีเสีย

---

## ครั้งที่ 10 — 2026-07-01

**Prompt:**
> Failed to save 'brand-finnix.html': The content of the file is newer. Please compare your version with the file contents or overwrite the content of the file with your changes.

**สิ่งที่แก้ไข (กู้หน้าที่ถูก Overwrite ทับ):**
- วิเคราะห์ต้นเหตุ save conflict: editor ของผู้ใช้ถือ buffer **เวอร์ชันเก่าก่อนครั้งที่ 7** ค้างอยู่ พอกด **Overwrite** จะเขียนทับตัวปัจจุบันที่ถูกต้องบนดิสก์ → งานรีดีไซน์หาย
- **พบความเสียหายจริง:** `brand-regionfilm.html` ถูก Overwrite ไปแล้ว (เซฟ 12:41, โต 9024→10530b) — ดีไซน์เก่าย้อนกลับมา (BRAND HERO: โลโก้แบรนด์ + `<h1>` + ย่อหน้า lead + ปุ่มทักไลน์/btn-line) และ **แถบ qspec หายไป**
- **กู้คืน:** เขียน `brand-regionfilm.html` ใหม่ให้ตรงแม่แบบครั้งที่ 7 (อ้างอิงจาก brand-ultraguard ที่ยังถูกต้อง) — เอา BRAND HERO ออก, ใส่ qspec กลับ (Ceramic: VLT 35% · ลดความร้อน 70% · UV 99% / Smart: VLT 20% · ลดความร้อน 75% · UV 99%), ขนาดกลับมา 8931b เท่าพี่น้อง, ไม่มี mojibake, ลิงก์ครบ
- สแกนยืนยัน: หน้าอื่นทั้งหมด (finnix/3m/ultraguard + model 10 หน้า) mtime ยังเป็น 2026-06-29 = **ไม่โดนแตะ** เสียแค่ regionfilm หน้าเดียว
- ⚠️ เตือนผู้ใช้: dialog `brand-finnix.html` ที่เด้งอยู่ **อย่ากด Overwrite** ให้กด Compare/Revert แทน (ตัวดิสก์ถูกต้องแล้ว) และแนะนำ reload หน้าต่าง VS Code เพื่อล้าง buffer เก่าค้างทั้งหมด

---

## ครั้งที่ 11 — 2026-07-01

**Prompt:**
> (หลายข้อความ save conflict: brand-finnix.html / brand-3m.html "The content of the file is newer…") + "มัน save ไม่ได้เกิดจากอะไร" + "แก้ไขดีไซน์เว็บใหม่ให้หน่อยให้ดูเป็นเว็บไซต์ราคาหลัก 100,000 เว็บไซต์ต้องดู Clean สบายตาแต่ยังคงธีมดีไซน์ที่ออกแบบมาอยู่แล้ว https://seaclearfilm.com/ เว็บนี้เป็นตัวอย่างแต่ไม่ต้องทำเหมือนเขา"

**ตอบเรื่อง save conflict:**
- ตรวจ brand-finnix / brand-3m บนดิสก์ = ถูกต้องดี (markers brandhero=0 btn-line=0 qspec=4/2, mtime ตรง batch) → สแกนทั้งโฟลเดอร์ยืนยันไม่มีไฟล์เสียหาย
- อธิบายต้นเหตุ: VS Code buffer เก่ากว่าไฟล์บนดิสก์ (generator เขียนทับทีหลัง) → editor เตือนกันทับของใหม่ ไม่ใช่ error; วิธีแก้ = Reload Window / Revert (ไม่ใช่ Overwrite)

**รีดีไซน์ทั้งเว็บ — ทิศทาง "โปร่ง สว่าง" (premium/clean คงธีมกรมท่า+ทอง):**
- design tokens (index.html + product.css): พื้น `--paper` ขาวอุ่นสะอาดขึ้น (#fcfbf7), `--light` นวลตา (#f5f3ec), `--line` บางลง, เพิ่ม `--gold-soft`, `--line-soft`, และชุดเงา `--shadow-sm/md/lg`
- เพิ่มที่ว่าง: section padding หน้าแรก 96→118px, หน้า product 88→108px; sec-head หายใจมากขึ้น
- **ยกแถบสถิติ (stats) จากพื้นกรมท่าเข้ม → พื้นขาว** ตัวเลขทองบนขาว เส้นคั่นบาง = จุดที่ทำให้ทั้งหน้าโปร่งขึ้นชัดที่สุด (กรมท่าเหลือใช้เฉพาะ hero + footer)
- การ์ดทั้งหมดขอบมนขึ้น (10–14px) + เงานุ่มตอนพัก (shadow-sm) + ยกลอย+เงาเข้มตอน hover (shadow-md): benefit card, brand card, contact card, closing CTA, gallery tile, intro figure, และในหน้า product: feature card (.fc), compare card (.cmc), shade card, usage, spec table, highlight (.phl .hi), swatch
- ปุ่มทอง (.btn-gold) เพิ่มเงาทองนุ่ม + ยกตัวตอน hover; header เพิ่ม blur 8→10px, สีให้เข้ากับ paper ใหม่
- โครง HTML/เนื้อหา/ลิงก์/JS เดิมไม่แตะ — แก้เฉพาะชั้นดีไซน์ (CSS)
- พรีวิวจริงใน Chrome ผ่าน HttpListener: ตรวจ index + brand-finnix + model-finnix-ceramic ครบ ทุกส่วนแสดงผลถูกต้อง โปร่ง พรีเมียม
- ⚠️ ไฟล์นี้ไม่มี git — เตือนผู้ใช้ก่อนแก้ให้ Reload Window ก่อน อย่า Overwrite buffer เก่าทับ

---

## ครั้งที่ 12 — 2026-07-01

**Prompt:**
> "คิดว่าดีไซน์เว็บไซต์หรือองค์ประกอบการจัดวางนั้นดีหรือยัง" → (วิจารณ์ตรงๆ) → "งั้นเริ่มได้เลยเอาทั้ง 3 อันเลย"

**บริบท:** ประเมินตรงๆ ว่าระบบดีไซน์แข็งแรงแล้ว (~7.5/10) แต่ที่ยังไม่ถึงระดับเว็บหลักแสนคือ 3 เรื่อง แล้วผู้ใช้สั่งทำทั้งหมด

**#2 ฟอนต์หัวข้อไทยพรีเมียม (Trirong — Thai serif เข้าคู่ Cormorant):**
- index.html: เพิ่ม Trirong ใน Google Fonts link; product.css: เพิ่ม `@import` Trirong (ครอบทุกหน้า product ในไฟล์เดียว)
- ใช้ Trirong กับ `.h-thai` (หัวข้อ section ทุกหน้า), `.intro-copy .statement`, `.closing .panel h3` (index), `.ctaband h2` (product) — จุดที่เป็นภาษาไทยและเดิม fallback เป็น serif เริ่มต้นแบบไม่ตั้งใจ; ชื่อรุ่น/ตัวเลขภาษาอังกฤษยังคง Cormorant

**#3 Trust signals (ไม่เอารีวิวกลับมาตามที่เคยสั่งลบครั้งที่ 2):**
- เพิ่ม **แถบแบรนด์พาร์ตเนอร์** ใต้แถบสถิติ: โลโก้ 4 แบรนด์ (grayscale → สีเมื่อ hover) + ป้าย "แบรนด์ฟิล์มชั้นนำที่เราเป็นผู้ติดตั้ง"
- เพิ่ม **ตรารับประกัน** (seal ทองกลม "8 ปี รับประกัน" ขอบขาว เงานุ่ม) วางมุมล่างซ้ายของรูป intro
- เพิ่ม responsive ของทั้งสอง (logo/seal ย่อบนมือถือ)

**#1 รูปจริง:**
- อธิบายว่าสร้างรูปจริงเองไม่ได้ — คงภาพให้สะอาด (ไม่ใส่ tint ทับ เพราะจะสู้กับธีมโปร่ง) และเตรียมโครงให้ดรอปรูปจริงลงได้ทันที + ให้เช็กลิสต์ไฟล์/ตำแหน่ง (hero, intro, gallery 5 รูป, closing, swatch หน้ารุ่น)

**พรีวิว:** ตรวจ index (partner strip + seal + หัวไทย Trirong) และ brand-finnix (หัว "เลือกรุ่นที่ใช่สำหรับคุณ" = Trirong) ผ่านครบ
- ⚠️ ไฟล์ที่แก้: index.html + products/product.css — reminder เดิมเรื่อง Reload Window / ห้าม Overwrite (ไม่มี git)

---

## ครั้งที่ 13 — 2026-07-01

**Prompt:**
> "แถวแบรนด์ฟิล์มชั้นนำที่เราเป็นผู้ติดตั้ง เอาไปแทนแบรนด์ฟิล์มของเราเผื่อให้มันเป็นตัวแทนแต่ล่ะแบรนด์เชื่อมไปยังหน้าอื่นด้วย logo และย้ายแถวที่มี ปรึกษาฟรี ผ่อน 0% รับประกัน 8 ปี ปลอดสารพิษ แถวนี้ย้ายไปอยู่ใต้พร้อมให้คำปรึกษาทุกพื้นที่"

**สิ่งที่แก้ไข (index.html):**
- ลบแถบโลโก้พาร์ตเนอร์ที่เพิ่งเพิ่ม (ครั้งที่ 12) ออกจากใต้แถบสถิติ
- เซกชัน "แบรนด์ฟิล์มของเรา" (#brands): เปลี่ยนจากการ์ด 4 ใบ (brandcard: โลโก้+ชื่อ+จำนวนรุ่น+คำอธิบาย+ปุ่ม) → เป็น **โลโก้ไทล์ 4 อัน** (`.brandlogo`) แต่ละอันคลิกลิงก์ไปหน้าแบรนด์ (คงหัวข้อ + id=brands + id แบรนด์เดิมไว้)
  - สไตล์: การ์ดขาวขอบมน 12px เงานุ่ม hover ยกลอย+ขอบทอง; responsive 4→2 คอลัมน์
- ย้าย featurebar (ปรึกษาฟรี / ผ่อน 0% / รับประกัน 8 ปี / ปลอดสารพิษ): จาก section เต็มความกว้างระหว่าง process↔contact → เข้าไป **ใต้ sec-head ของ #contact** ("พร้อมให้คำปรึกษาทุกพื้นที่") เหนือการ์ดติดต่อ
  - เปลี่ยน `.featurebar` จากแถบเต็มจอ (border บน-ล่าง) → พาเนลกล่องมน 14px (border รอบ + margin-top)
- ลบ CSS `.partners`/`.plogos` (เลิกใช้) แทนด้วย `.brandlogos`/`.brandlogo`
- พรีวิวผ่าน: โลโก้ไทล์ลิงก์ครบ, featurebar อยู่ใต้หัวข้อติดต่อถูกตำแหน่ง
- ⚠️ แก้เฉพาะ index.html (ไม่มี git) — Reload Window ก่อน / ห้าม Overwrite

---

## ครั้งที่ 14 — 2026-07-01

**Prompt:**
> ช่วยสร้างหน้า contact + (แนบ Midjourney-style prompt: หน้า Contact Us พรีเมียม มินิมอล ขาว/กรมท่า/ทอง — nav, หัวข้อ CONTACT US + ที่อยู่, แผนที่ + pin, Get In Touch แบบ split (ไอคอนกระจก 4 อัน + ฟอร์มเส้นบางปุ่มทอง), แถว value prop ไอคอนเมทัล 4 อัน, footer กรมท่า)

**สิ่งที่แก้ไข:**
- สร้าง **`/contact.html`** (root) ใช้ธีม/คอมโพเนนต์ร่วมกับเว็บ: link `products/product.css` + `<style>` เฉพาะหน้า, ฟอนต์ Cormorant/Trirong/IBM Plex Sans Thai, reveal + burger + header-scroll script
- โครง (บนลงล่าง): topbar → header (เมนู "ติดต่อ" = active) → breadcrumb → **CONTACT US** + ที่อยู่ → **แผนที่ Google ฝังจริง** (กรอบมนเงานุ่ม) → **Get In Touch** split: ซ้าย = ไอคอนกระจก 4 ช่องทาง (LINE/Facebook/Email/โทร, glassmorphism), ขวา = ฟอร์ม (ชื่อ/เบอร์/อีเมล/ข้อความ) เส้น hairline + ปุ่มทอง **SUBMIT** → **value props** 4 การ์ดไอคอนผิวเมทัล (ปรึกษาฟรี/ผ่อน 0%/รับประกัน 8 ปี/ปลอดสารพิษ) → footer กรมท่า + fab
- ต่างจาก prompt: ไอคอน 3D glass/steel ทำด้วย CSS (เรนเดอร์ 3D จริงไม่ได้); แผนที่ใช้ Google ฝังจริงแทน placeholder; ฟอร์ม submit เปิด mailto (native validation, ไม่ใช้ alert)
- **ต้องให้ผู้ใช้ยืนยัน 2 จุด (ตอนนี้เป็น placeholder):** อีเมลปลายทางฟอร์ม + อีเมลช่องทางติดต่อ = `worldfilmcenter@email.com` (แก้ตัวแปร `RECIPIENT` ใน script + ลิงก์ช่อง Email) — หรือจะต่อ Formspree/หลังบ้านก็ได้
- นำทาง: เมนู/footer ของ contact.html ชี้ไป index.html#... และหน้าแบรนด์ครบ; แต่เมนู "ติดต่อ" ในหน้า **อื่น ๆ ยังชี้ #contact เดิม** (ยังไม่ได้ wire ทั้งเว็บ — รอยืนยัน)
- พรีวิวผ่านครบทุกส่วน (แผนที่โชว์พิกัดจริงบางแค, ไอคอนกระจก/เมทัล, ปุ่มทอง, footer)
- contact.html เป็นไฟล์ใหม่ → ไม่มีปัญหา save-conflict

---

## ครั้งที่ 15 — 2026-07-01

**Prompt:**
> contact.html เชื่อม กับ แทบเมนูในหมวด ติดต่อ

**สิ่งที่แก้ไข (เชื่อมเมนู "ติดต่อ" ทั้งเว็บ → contact.html):**
- index.html: เมนู `<a href="#contact">ติดต่อ</a>` → `contact.html`
- หน้า product 14 ไฟล์ (brand 4 + model 10): `../index.html#contact` → `../contact.html` (ใช้ sed แทนเฉพาะ ASCII, ไม่แตะไบต์ไทย, ตรวจแล้วไม่มี mojibake)
- ข้าม `assets/source/_old-pre-reorg/` (ไฟล์เก่าไม่ใช้งาน)
- ทดสอบคลิกจริงจากหน้า product → เด้งไป contact.html สำเร็จ, เมนูขึ้น active
- ⚠️ แก้บนดิสก์ 15 ไฟล์ (index + 14 product) — ถ้าเปิดค้างใน VS Code ให้ Reload Window ก่อน / ห้าม Overwrite (ไม่มี git)

---

## ครั้งที่ 16 — 2026-07-02

**Prompt:**
> อัพไฟล์นี้ขึ้น github ได้ไหม → (เลือก) ไม่อัปไฟล์ต้นฉบับ ใส่ .gitignore, ทางเลือก A (สร้าง repo เอง), ชื่อ repo ProjectWorldFilm, ตั้งเป็น Private

**สิ่งที่แก้ไข (เตรียมโปรเจกต์ขึ้น GitHub):**
- สร้าง `.gitignore` — ยกเว้น `assets/source/` (~9MB, รวม `_old-pre-reorg/`) และไฟล์ระบบ/โปรแกรมแก้ไข
- `git init` + ตั้งค่า user แบบเฉพาะ repo (ไม่แตะ global): email `mr.pream.2555@gmail.com`
- commit แรก `e3b26e3`: เฉพาะเว็บที่ใช้งานจริง (index/contact/products/assets/brands + logo, README, CHANGELOG) 26 ไฟล์ ~0.5MB
- ตั้ง remote `origin` = https://github.com/KUYLA2555/ProjectWorldFilm.git (repo Private) แล้ว push branch `main`
- push ครั้งแรกโดน reject เพราะ repo ถูกสร้างพร้อม README auto (`# ProjectWorldFilm`) → เขียนทับด้วย `git push --force-with-lease` (README ตัวเต็มของเราแทน placeholder)
- ✅ ขึ้น GitHub สำเร็จ, local กับ origin/main ตรงกัน

---

## ครั้งที่ 17 — 2026-07-08

**Prompt:**
> (ถามความเห็น) คิดว่าการดีไซน์ในหน้าแรกการเรียงลำดับนั้นดูดีไหม → (สั่งต่อ) ปรับแค่ข้อ 1 กับ 3 พอ จัดการเลย

**สิ่งที่แก้ไข (จัดลำดับ section หน้าแรก):**
- **ข้อ 1** — สลับลำดับ: ย้าย section "ทำไมต้องติดฟิล์ม / ประโยชน์" (`#why`) ขึ้นมาก่อน section "แบรนด์" (`#brands`) เพื่อให้ flow บอก *ทำไม* ก่อน *มีอะไรให้เลือก* → ลำดับใหม่: เกี่ยวกับเรา → ประโยชน์ → แบรนด์ → ผลงาน
- สลับคลาสพื้นหลังให้ยังไล่ paper/tint สลับกันถูกต้อง: `#why` เป็น `section--tint`, `#brands` เป็น `section` ธรรมดา
- **ข้อ 3** — แก้ชื่อเมนูบนสุด `บริการ` → `ประโยชน์` ให้ตรงกับหัวข้อจริงของ section `#why` (เดิมชื่อเมนูไม่ตรงกับเนื้อหา)
- หมายเหตุ: ลำดับเมนูยังเป็น ฟิล์มของเรา → ประโยชน์ (ต่างจากลำดับ scroll ที่ประโยชน์มาก่อนแบรนด์) — ยังไม่ปรับเพราะอยู่นอกขอบเขตที่สั่ง

---

## ครั้งที่ 18 — 2026-07-08

**Prompt:**
> แถวคอลัมน์ ง่ายครบ จบในที่เดียว ตัดออก

**สิ่งที่แก้ไข:**
- ลบหัวข้อ h2 "ง่าย ครบ จบในที่เดียว" ออกจากส่วน "ขั้นตอนบริการ" (`#process`) — คงคำว่า "ขั้นตอนบริการ" (eyebrow) ไว้ แล้วตามด้วย 4 สเต็ปเลย

---

## ครั้งที่ 19 — 2026-07-08

**Prompt:**
> แถวคอลัมน์ขั้นตอนบริการ นั้นตัดออก

**สิ่งที่แก้ไข:**
- ลบทั้ง section "ขั้นตอนบริการ" (`#process`) ออกจากหน้าแรก (eyebrow + 4 สเต็ป: ปรึกษาฟรี / สำรวจ+เสนอราคา / ติดตั้ง / ตรวจงาน+รับประกัน)
- ลบลิงก์เมนูบน "ขั้นตอน" (ชี้ `#process`) เพื่อไม่ให้เป็นลิงก์ตาย
- ลบลิงก์ฟุตเตอร์ "ขั้นตอนบริการ" (ชี้ `#process`) ด้วยเหตุผลเดียวกัน
- CSS `.process` / `.steps` ยังคงไว้ในไฟล์ (unused ไม่กระทบการแสดงผล)
- ลำดับหน้าแรกตอนนี้: เกี่ยวกับเรา → ประโยชน์ → แบรนด์ → ผลงาน → ติดต่อ

---

## ครั้งที่ 20 — 2026-07-08

**Prompt:**
> แก้ไข code หน้าแรกให้สวยและสะอาด และแถวคอลัมน์ tailored solutions ทั้งแถวเอาออก

**สิ่งที่แก้ไข:**
- ลบ block "Tailored Solutions" (`.closing`) ทั้งแถวออกจากส่วนติดต่อ (รูป + หัวข้อ "ฟิล์มที่ใช่ สำหรับทุกพื้นที่ของคุณ" + 2 ปุ่ม CTA) → ส่วนติดต่อจบที่การ์ด Line/โทร/Facebook
- **ล้าง CSS ที่ตายแล้ว (dead code)** ที่ไม่มี HTML ใช้งานในหน้าแรกแล้ว เพื่อให้โค้ดสะอาด:
  - `.products / .pcard / .spec-row` (การ์ดสินค้า — ไม่เคยมีในหน้าแรก)
  - `.reviews / .rcard / .rev-score / .rev-top` (รีวิว — ไม่มีในหน้า)
  - `.steps / .step` (ขั้นตอนบริการ — ลบ section ไปแล้วครั้งที่ 19)
  - `.closing / .closing .pic / .closing .panel` (Tailored Solutions ที่เพิ่งลบ)
  - รวมถึง responsive rules ของคลาสเหล่านี้ใน `@media 980` และ `@media 480`
- ผลรวม index.html ในเซสชันนี้: -326 บรรทัด (จาก ~2117 → 1791) โครงสร้าง section เปิด/ปิดครบ 7 คู่ ไม่มีคลาส/ลิงก์ตายหลงเหลือ

---

## ครั้งที่ 21 — 2026-07-08

**Prompt:**
> ตรงหน้าติดต่อ คอลัมน์กรอกแบบฟอร์มนั้นเอาออกไปให้หมด และจัดหน้าดีไซน์ในหน้าติดต่อให้ดี

**สิ่งที่แก้ไข (contact.html):**
- ลบคอลัมน์ฟอร์มกรอกทั้งหมด: `<form class="cform">` (ชื่อ/เบอร์/อีเมล/รายละเอียด + ปุ่ม SUBMIT + hint) พร้อม CSS `.cform*` ทั้งบล็อก และ JS handler (`#cform` submit + `RECIPIENT` mailto)
- จัดดีไซน์ section "Get In Touch" ใหม่: จากเลย์เอาต์ 2 คอลัมน์ (ช่องทาง | ฟอร์ม) → เป็นหัวข้อจัดกึ่งกลาง (`.sec-head`) + การ์ดช่องทางติดต่อ 4 อัน (Line / Facebook / Email / โทร) เรียงกริด 2×2 จัดกึ่งกลาง (`max-width:880px`)
- ปรับการ์ดช่องทาง: ป้ายกำกับ (`.lab`) แสดงบน / ค่า (`.val`) แสดงล่าง (เดิม span เป็น inline ทำให้ติดกันบรรทัดเดียว)
- ลบ CSS `.git` (เลย์เอาต์ split เดิม) + responsive ที่เกี่ยวข้อง, แก้ meta description ตัดคำว่า "กรอกแบบฟอร์ม"
- ตรวจด้วย Chrome preview (localhost) แล้ว: ลำดับหน้า hero+แผนที่ → Get In Touch (4 ช่องทาง) → value props → footer ดูคลีนสมดุล
- **หมายเหตุ (ค้างไว้ให้ตัดสินใจ):** เมนูบน/ฟุตเตอร์ของ contact.html และหน้าอื่น ๆ ยังมี "บริการ" (→`index.html#why`) และ "ขั้นตอน" (→`index.html#process`) — หลังครั้งที่ 17–19 ชื่อบนหน้าแรกเปลี่ยนเป็น "ประโยชน์" และ section `#process` ถูกลบแล้ว ทำให้ลิงก์ "ขั้นตอน" กลายเป็นลิงก์ตายทุกหน้า → ควรแก้เมนูให้ตรงกันทั้งไซต์ (ยังไม่ทำ รอยืนยัน)

---

## ครั้งที่ 22 — 2026-07-08

**Prompt:**
> จัดการเคลียร์โค้ดที่ไม่จำเป็นออกและเคลียร์โค้ดให้สะอาด และถ้าเจอปัญหาที่ควรแก้ไขทั้งไซต์จัดการเลย

**สิ่งที่แก้ไข:**
- **ซ่อม contact.html ที่โดน editor revert บางส่วน** (อาการตรงกับ editor save conflict): กู้ให้กลับเป็นเวอร์ชันสะอาด — meta description (ตัด "กรอกแบบฟอร์ม"), ลบ CSS `.git` ที่ตายค้าง, คืน `.chan-list` เป็นกริด 2×2 (โดนย้อนเป็น flex คอลัมน์เดียว), แก้ `@media` (`.git`/`.chan-list gap:0`), และลบ JS handler ฟอร์มที่ค้าง (เรียก `getElementById("cform")` = null → โยน TypeError จริง)
- **แก้เมนูให้ตรงกันทั้งไซต์ (15 ไฟล์: contact + 14 หน้า products)** ด้วย sed:
  - `บริการ` (→`index.html#why`) → **`ประโยชน์`** ให้ตรงกับหน้าแรก
  - ลบลิงก์ **`ขั้นตอน`** (→`index.html#process`) ออกทุกหน้า เพราะ section `#process` ถูกลบไปแล้ว = ลิงก์ตาย
  - ตอนนี้ 16 หน้าใช้เมนู "ประโยชน์" ตรงกันหมด (ไฟล์สำรองใน `assets/source/_old-pre-reorg/` ไม่แตะ — gitignore ไม่ deploy)
- **ลบ dead CSS ใน products/product.css** (ยืนยันแล้วไม่มี HTML ไหนใช้): `.serif`, `.section--ink`, `.badge`, และทั้งชุด `.brandhero*` (หน้า brand ใช้ `.brand-top` แทน) รวม responsive overrides — brace ยังสมดุล 188/188
- **ตรวจ dead reference ทั้งไซต์:** ไม่มีลิงก์ไปหน้า/ไฟล์ที่ถูกลบ (cm15/cm35/xt/phoenix) และไม่มี asset (html/css/img) ที่ลิงก์ไปแล้วหาย — สะอาดทั้งหมด
- ยืนยันด้วย static analysis + preview (browser hang ที่ iframe แผนที่ตอนท้าย ไม่เกี่ยวกับโค้ด)

---

## ครั้งที่ 23 — 2026-07-08

**Prompt:**
> ช่วยเคลียร์โค้ด html ที่ไม่จำเป็นออกและจัดเรียงไฟล์ให้สวยและอัปขึ้น github KUYLA2555/ProjectWorldFilm เลย

**สิ่งที่แก้ไข / ดำเนินการ:**
- ตรวจ HTML รอบสุดท้าย: สคริปต์ index.html สะอาด (`data-img` ยังใช้จริง — JS โหลดรูป gallery แบบ lazy, เมนู/scroll/reveal ครบ ไม่มี JS ตาย), หน้าแรก+หน้าติดต่อไม่มี HTML ที่ไม่จำเป็นค้าง, ไฟล์ฟอร์แมต 2-space สม่ำเสมออยู่แล้ว (Node ไม่ได้ติดตั้ง จึงจัดฟอร์แมตด้วยมือ/คงสไตล์เดิม)
- รวมงานทั้งเซสชัน (ครั้งที่ 17–22) commit เดียว `de97397`: 18 ไฟล์, +237/−678 บรรทัด (assets/source ถูก .gitignore ไม่ติดมา)
- `git add -A` เพื่อรวมส่วนที่ค้าง staged แบบไม่สมบูรณ์ให้ตรง working tree ก่อน commit
- ✅ push ขึ้น GitHub สำเร็จ `35fc498..de97397 main -> main`, local = origin/main = `de97397`, working tree สะอาด

---

## ครั้งที่ 24 — 2026-07-11

**Prompt:**
> ออกแบบหน้าผลงานในเว็บหน่อยซึ่ง ตัวอย่างเว็บตาม https://seaclearfilm.com/articles/ และ https://seaclearfilm.com/corporate-sector/ ออกแบบมาเป็นโครงก่อนคราวๆ

**สิ่งที่แก้ไข / ดำเนินการ:**
- **สร้างไฟล์ใหม่ `works.html`** (หน้า top-level เหมือน contact.html) เป็น "โครงร่าง" หน้าผลงาน — ใช้ `products/product.css` ชุดเดิม + `<style>` เฉพาะหน้า, โทนกรมท่า+ทอง เข้าชุดทั้งไซต์
- อ้างอิงโครงจากเว็บตัวอย่าง 2 หน้า: `corporate-sector` (กริดผลงาน+โลโก้ลูกค้าองค์กร) และ `articles` (การ์ดกริด+ตัวกรอง/หน้าเพจ)
- โครงหน้าเรียงจากบนลงล่าง: topbar → header (เมนู "ผลงาน" = active ชี้ `works.html`) → breadcrumb → **PAGE HERO** กรมท่า (OUR WORK + สถิติ 200+/40+/8) → แถบแจ้งว่าเป็นภาพตัวอย่าง (`.sample-note`) → **ตัวกรองหมวด** (ชิป: ทั้งหมด/บ้าน/คอนโด/อาคาร-สำนักงาน/โรงแรม-รีสอร์ท/ร้านค้า-คาเฟ่) → **กริดการ์ดผลงาน 6 ใบ** (รูป degrade เป็นกรมท่าถ้าโหลดไม่ขึ้น + ป้ายหมวด + ชื่อโครงการ + ทำเล + ฟิล์มที่ใช้ + ลิงก์ "ดูผลงาน") → ปุ่ม "ดูผลงานเพิ่มเติม" → **แถบโลโก้ลูกค้าองค์กร** (กล่อง placeholder 12 ช่อง) → **CTA band** (โทร/LINE) → footer + FAB
- JS: reuse header-shadow/mobile-menu/reveal + lazy `.thumb[data-img]` (เหมือน tile หน้าแรก) + **ตัวกรองหมวดหมู่** (คลิกชิป → กรองการ์ดตาม `data-cat`)
- ตรวจด้วย Chrome preview (localhost:8753): hero/การ์ด/รูป Unsplash โหลดครบ, ตัวกรอง "บ้าน" เหลือ 2 การ์ด home ถูกต้อง, แถบโลโก้+CTA เข้าชุดกัน
- ยังไม่ทำ (รอยืนยัน): เนื้อหา/รูปยังเป็นตัวอย่าง รอแทนที่ด้วยรูปหน้างานจริง; และเมนู "ผลงาน" ในอีก 16 หน้ายังชี้ `index.html#work` — ถ้าจะใช้หน้านี้จริงต้องแก้ให้ชี้ `works.html` ทั้งไซต์

---

## ครั้งที่ 25 — 2026-07-11

**Prompt:**
> เอาหน้า works ที่สร้างเมื่อกี่ไปลิงก์กับหน้าผลงาน และในหน้า works ตรงลูกค้าองค์กรที่ไว้วางใจนั้นทั้งแถวนั้นตัดออกเลย

**สิ่งที่แก้ไข / ดำเนินการ:**
- **เดินสายเมนู "ผลงาน" ให้ชี้ `works.html` ทั้งไซต์ (17 ลิงก์ใน 16 ไฟล์):**
  - `index.html` — เมนู `#work` → `works.html` และฟุตเตอร์ "ผลงานติดตั้ง" `#work` → `works.html`
  - `contact.html` — เมนู `index.html#work` → `works.html`
  - 14 หน้า products (brand ×4 + model ×10) — เมนู `../index.html#work` → `../works.html` (ใช้ sed แทนที่เฉพาะส่วน ASCII ไม่แตะตัวอักษรไทย)
  - ตรวจแล้วไม่มี `#work` ค้างในลิงก์ใด ๆ (section `id="work"` บนหน้าแรกยังอยู่เป็นพรีวิว ไม่ใช่ลิงก์ตาย)
- **ตัด section "ลูกค้าองค์กรที่ไว้วางใจ" ออกจาก `works.html` ทั้งแถว:** ลบทั้ง `<section class="section section--tint">` (หัวข้อ + แถบโลโก้ 12 กล่อง) + CSS `.logoband`/`.logobox` + กฎ `.logoband` ใน media query 980/620px → เหลือ 3 section: hero → กริดผลงาน → CTA band
- ยืนยันด้วย Chrome preview: ลำดับหน้าใหม่ กริดผลงาน → ปุ่ม "ดูผลงานเพิ่มเติม" → CTA (กรมท่า) รอยต่อเนียน, เมนู "ผลงาน" ทุกหน้าเปิด works.html ได้

---

## ครั้งที่ 26 — 2026-07-11

**Prompt:**
> ตรงแถวคอลัมน์หน้าแรก ติดต่อเราพร้อมให้คำปรึกษาทุกพื้นที่ เอาแถวนี้ไปใส่ด้านล่างในหน้าผลงานและฟิล์มของเรา แทนแถวคอลัมน์ที่มีประโยคไม่แน่ใจว่ารุ่นไหนเหมาะกับคุณ

**สิ่งที่แก้ไข / ดำเนินการ:**
- **ยก section "ติดต่อเรา / พร้อมให้คำปรึกษาทุกพื้นที่" จากหน้าแรก** (หัวข้อ + `featurebar` 4 ช่อง: ปรึกษาฟรี/ผ่อน 0%/รับประกัน 8 ปี/ปลอดสารพิษ + การ์ดช่องทาง 3 ใบ: LINE/CALL/FACEBOOK) ไปวางท้ายหน้า **แทน CTA เดิม** ใน 5 ไฟล์
- **`works.html`** — แทน CTA "อยากให้บ้านหรืออาคาร...เย็นสบายแบบนี้บ้าง?" (กรมท่า) ด้วย section ติดต่อ (พื้น tint) → ลำดับใหม่: hero → กริดผลงาน → **ติดต่อเรา** → footer
- **4 หน้าแบรนด์** (brand-finnix / brand-3m / brand-regionfilm / brand-ultraguard) — แทน CTA "ไม่แน่ใจว่ารุ่นไหนเหมาะกับคุณ?" ด้วย section ติดต่อเดียวกัน
- **เพิ่ม CSS ที่จำเป็นใน `products/product.css`** (ไฟล์ร่วมของ works + brand): `.featurebar/.fitem/.contact-cards/.ccard` + responsive 760/480px — เดิม class ชุดนี้อยู่แค่ใน `<style>` ของ index.html ทำให้หน้าอื่นเรียกใช้ไม่ได้; ยืนยันก่อนเพิ่มว่าไม่ซ้ำของเดิมใน product.css
- **ไม่แตะ:** หน้า model 10 หน้า (ยังใช้ CTA เดิม "ทีมงานพร้อมประเมินหน้างาน..." ซึ่งไม่ใช่ประโยคที่ระบุ), section ติดต่อบนหน้าแรกคงเดิม
- ยืนยันด้วย Chrome preview ทั้ง `works.html` และ `brand-finnix.html`: section ติดต่อแสดงผลถูก (featurebar + การ์ด 3 ใบ) โทน/สไตล์ตรงหน้าแรก, ต่อด้วย footer เนียน; ตรวจแล้วไม่มี "ไม่แน่ใจว่ารุ่นไหน" เหลือทั้งไซต์

---

## ครั้งที่ 27 — 2026-07-11

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและสร้างไฟล์ CLAUDE.md สำหรับ Claude Code รุ่นถัดไป

**สิ่งที่แก้ไข / ดำเนินการ:**
- **สร้างไฟล์ใหม่ `CLAUDE.md`** ที่ราก สรุปสถาปัตยกรรมภาพรวมให้ instance ถัดไปทำงานต่อได้เร็ว
- เนื้อหาเน้นจุดที่ต้องอ่านหลายไฟล์ถึงจะเข้าใจ: เว็บสแตติกล้วน (ไม่มี build/test/lint, ไม่มี Node/Python), วิธีพรีวิวผ่าน PowerShell HttpListener (Chrome ext บล็อก `file://`), **CSS ซ้ำ 2 ที่** (inline ใน index.html ↔ `products/product.css`), **header/footer/nav ก๊อปทุกไฟล์** (แก้ต้องแก้ทุกหน้า, ใช้ sed เฉพาะส่วน ASCII), design tokens (กรมท่า+ทอง), กฎ path ราก↔`products/`, ผังแบรนด์→รุ่น, `.reveal`/`[data-img]`, แถบ `.sample-note`, จุดแก้ข้อมูลติดต่อ, และธรรมเนียมบันทึก CHANGELOG ทุกเทิร์น
- อ้างอิงจาก README.md + .gitignore + โครงไฟล์จริง ไม่มี CLAUDE.md เดิมอยู่ก่อน

---

## ครั้งที่ 28 — 2026-07-11

**Prompt:**
> ตรงแทบเมนู ประโยชน์เปลี่ยนเป็น about us และเอาหน้านี้เป็นโครง https://seaclearfilm.com/about-us/ และเอาหน้าที่กำลังจะสร้างนี้ไปลิงก์กับหน้าเมนู about us

**สิ่งที่แก้ไข / ดำเนินการ:**
- **สร้างไฟล์ใหม่ `about.html`** (หน้า top-level) เป็นโครงหน้า "About Us" อิงโครงจาก seaclearfilm.com/about-us/ แต่ปรับเป็นเนื้อหา WORLD FILM — ใช้ `products/product.css` + `<style>` เฉพาะหน้า
- ลำดับ section: topbar → header (เมนู About Us = active) → breadcrumb → **PAGE HERO** กรมท่า (ABOUT US) → แถบแจ้งเป็นตัวอย่าง (`.sample-note`) → **เรื่องราวของเรา** (2 คอลัมน์: รูป+กรอบทอง / ข้อความ + ลายเซ็น) → **แถบสถิติ** (40+/99%/85%/8) → **ความเชื่อของเรา** (คำคมกลางจอ พื้น tint) → **ทำไมต้องเลือกเรา** (`.fcards` 3 การ์ด: ทีมช่างมืออาชีพ / ฟิล์มแบรนด์ชั้นนำ / รับประกันยาวนาน) → **ติดต่อเรา** (featurebar + การ์ด 3 ช่องทาง เหมือนหน้าอื่น) → footer + FAB
- **เปลี่ยนเมนู "ประโยชน์" → "About Us" ทั้งไซต์ (17 ลิงก์ใน 17 ไฟล์)** และเปลี่ยนปลายทางจาก `#why` (section บนหน้าแรก) → `about.html`:
  - `index.html` `#why` → `about.html`; `works.html` / `contact.html` `index.html#why` → `about.html`
  - 14 หน้า products `../index.html#why` → `../about.html` (ใช้ sed จับ label ด้วย `[^<]*` เลี่ยงพิมพ์ไทยในสคริปต์)
  - `about.html` เมนูตัวเองตั้ง About Us = active
- **ไม่แตะ:** section `#why` (ประโยชน์) บนหน้าแรกยังอยู่ + ฟุตเตอร์ "บริการ" หน้าแรกยังลิงก์ `#why` ได้ปกติ (ไม่ใช่ลิงก์ตาย)
- ยืนยันด้วย Chrome preview: hero/เรื่องราว/สถิติ/ความเชื่อ/การ์ด 3 ใบ/ติดต่อ แสดงครบ, เมนู active = About Us; ตรวจแล้วไม่มี "ประโยชน์" เหลือทั้งไซต์
- หมายเหตุ: เมนู label เป็นภาษาอังกฤษ "About Us" ตามที่ขอ (แทรกอยู่ในเมนูไทย) — ถ้าต้องการ "เกี่ยวกับเรา" แทน แจ้งได้

---

## ครั้งที่ 29 — 2026-07-11

**Prompt:**
> ย้ายแทบเมนู about us ย้ายไปแทนผลงานแล้วเอาผลงานย้ายมาแทน about us และจัดการไฟล์ด้วยการเคลียร์ไฟล์ที่ไม่จำเป็นออกและเรียงไฟล์ให้เป็นระเบียบ

**สิ่งที่แก้ไข / ดำเนินการ:**
- **สลับตำแหน่งเมนู "ผลงาน" ↔ "About Us" ทั้งไซต์ (18 หน้า)** → ลำดับใหม่: หน้าแรก / ฟิล์มของเรา / **ผลงาน / About Us** / ติดต่อ
  - 4 หน้าราก (index/about/works/contact) ใช้ Edit (คง `class="active"` ให้ตรงหน้า)
  - 14 หน้า products ใช้ sed slurp + capture group (ASCII ล้วน สลับสองบรรทัด ไม่พิมพ์ไทยในสคริปต์); ตรวจครบทุกไฟล์ ผลงานมาก่อน About Us + DOM ยืนยันลำดับถูก
- **เคลียร์ไฟล์ + จัดระเบียบ:**
  - ตรวจไซต์จริง (ที่ track/deploy) — สะอาด ไม่มีไฟล์ตาย/asset กำพร้า/ลิงก์ตาย; ลิงก์ตายเก่า (`../index/`, `product-cm15/cm35/xt`, `index/PIC`) อยู่เฉพาะในโฟลเดอร์สำรองเท่านั้น
  - แก้ meta description `brand-finnix.html` ที่ยังพูดถึงรุ่น "Phoenix" (ลบไปแล้ว) → ตัดออกให้เหลือ Ceramic, Titanium, UV Guard, Extra Clear
  - **ลบโฟลเดอร์ `assets/source/_old-pre-reorg/`** (HTML เก่าก่อนจัดโครงสร้าง 7 ไฟล์ + README, path พังใช้ไม่ได้) — ตามที่ผู้ใช้ยืนยัน (เลือก "ลบเฉพาะ _old-pre-reorg/"); คงรูปต้นฉบับ .png ทั้ง 10 ไฟล์ไว้ [หมายเหตุ: โฟลเดอร์นี้ถูก gitignore ลบแล้วกู้ไม่ได้ผ่าน git]
  - อัปเดต `README.md` (เพิ่ม about/works/contact/CLAUDE.md ในผังไฟล์, ลบบรรทัด _old-pre-reorg, แก้ desc source, อัปวันที่) + `.gitignore` (ตัดคอมเมนต์อ้างอิง _old-pre-reorg)

---

## ครั้งที่ 30 — 2026-07-11

**Prompt:**
> ตรงแทบเมนูปรับเป็นภาษาอังกฤษและมีภาษาไทยเป็นตัวเล็กอะิบาย

**สิ่งที่แก้ไข / ดำเนินการ:**
- **ทำเมนูเป็นสองภาษาทั้งไซต์ (18 หน้า):** อังกฤษเป็นตัวหลัก + ไทยตัวเล็กบรรยายใต้ — Home/หน้าแรก · Our Films/ฟิล์มของเรา · Works/ผลงาน · About Us/เกี่ยวกับเรา · Contact/ติดต่อ
- **กลไก:** เปลี่ยน label เป็นอังกฤษ + ใส่ `data-th="<ไทย>"` บนแต่ละ `<a>`; CSS ใช้ `.menu a::after { content: attr(data-th) }` แสดงไทยตัวเล็ก และย้ายเส้นใต้ hover/active จาก `::after` → `::before` (เพราะ `::after` ถูกใช้แสดงไทยแล้ว); `.menu a` เป็น flex column, ส่วน `.menu .has-sub > a` เป็น flex row-wrap ให้ "Our Films + caret" อยู่บรรทัดบน ไทยอยู่ล่าง
- แก้ CSS 2 ที่ (index.html inline + `products/product.css`) ให้ตรงกัน; submenu (Finnix/3M/…) และ breadcrumb/footer ยังเป็นไทยตามเดิม (มี `.submenu a::after{display:none}` กันไทยเปล่าโผล่)
- **วิธีทำ markup:** ใช้ sed script ไฟล์ UTF-8 (`root.sed`/`prod.sed`) จับ label ไทยแบบ href-scoped (`<li>`) กัน breadcrumb/footer โดน + `>ฟิล์มของเรา$` ($ ท้ายบรรทัด) แยก has-sub ออกจาก breadcrumb/h4 — ทดสอบบนสำเนา brand-3m ก่อนว่าไทยไม่เพี้ยนแล้วค่อยรันจริง
- ยืนยัน: computed `::after` = ไทยจาก data-th ครบ 5 เมนู, พรีวิวมือถือ (EN บน/ไทยเล็กล่าง + submenu ถูก) และจำลองเดสก์ท็อป (แนวนอน + caret ข้าง Our Films) ถูกต้อง

---

## ครั้งที่ 31 — 2026-07-11

**Prompt:**
> จัดเลยขั้นเลย (= ยืนยันให้ commit + push ขึ้น GitHub)

**สิ่งที่แก้ไข / ดำเนินการ:**
- รวมงานทั้งเซสชัน (ครั้งที่ 24-30) เป็น commit เดียว `6c31968` — 23 ไฟล์, +2258/−162 (มีไฟล์ใหม่ about.html, works.html, CLAUDE.md)
- `git add -A` รวมไฟล์ใหม่ + การลบ backup ที่ค้าง; `assets/source/` ยังถูก gitignore ไม่ติดมา (ตรงตาม workflow เดิม)
- ✅ push สำเร็จ `5c32cfe..6c31968 main -> main`, local = origin/main, working tree สะอาด (เหลือแค่บรรทัดนี้ที่จะเข้าคอมมิตรอบถัดไป)

---

## ครั้งที่ 32 — 2026-07-11

**Prompt:**
> ปรับเว็บให้เป็น Responsive Design (โฟกัส ≤768px): คงดีไซน์เดสก์ท็อปเดิม, เพิ่ม viewport ถ้ายังไม่มี, ใช้ media queries ปรับ layout/เมนู hamburger/ฟอนต์/ปุ่ม ≥44px, รูปไม่ล้นจอ, ตารางเลื่อนแนวนอน + อธิบายว่าแก้ตรงไหน — วิเคราะห์ + เสนอแผนก่อนลงมือ

**สิ่งที่แก้ไข / ดำเนินการ:** (แผนได้รับอนุมัติแล้วจึงลงมือ — งานเป็น CSS ล้วน ไม่แตะ markup/โครงสร้าง)
- **ผลวิเคราะห์:** เว็บมี responsive foundation อยู่แล้ว (viewport ครบ 18 หน้า, hamburger, `img max-width:100%`, grid ยุบได้) → งานคือ "อุดช่องโหว่ให้ครบ" ไม่ใช่ทำใหม่
- **หลักคุมข้อ 1 (จอใหญ่ต้องไม่เปลี่ยน):** ทุกการแก้อยู่ใน `@media (max-width:768px)` หรือ `480px` เท่านั้น + ไม่แตะ tier 980px → จอ >768px หน้าตาเดิมเป๊ะ (ยืนยัน brace สมดุลทุกไฟล์)
- **Breakpoint สม่ำเสมอ:** เปลี่ยนจุดยุบหลักให้เป็น **768px** ทุกไฟล์ — product.css 760→768 (3 บล็อก), index.html 760→768 (2), works.html 620→768, about.html 760→768, contact.html 560→768 (คง 560 ไว้เฉพาะปรับความสูงแผนที่, คง 900 ไว้ tier แท็บเล็ต vprops)
- **3.4 touch target ≥44px** (เพิ่มใน ≤768): topbar links (+ `.topbar .wrap` height auto/min 44), submenu links (flex center 44), footer links (inline-flex 44), `.btn`, `.chip` (works), `.cmc .go`/`.brand-go` — วัดจริงบนมือถือ: topbar 44 / nav 56 / submenu 44 / footer 44 / chip 46 / hero btn 60 ✓
- **4. รูป/สื่อไม่ล้น:** เสริม `img,svg,iframe,video { max-width:100% }` ใน ≤768 (index + product.css)
- **5. ตารางเลื่อนแนวนอน:** `.spectable { overflow-x:auto; -webkit-overflow-scrolling:touch }` ใน ≤768 (ไม่มี `<table>`/`<video>` จริงในเว็บ — สเปกทำด้วย flex; เสริมกันเหนียว) — วัดจริง spectable overflow-x=auto กว้าง 390 พอดีจอ 430 ✓
- **3.3 ฟอนต์/ระยะห่าง ≤480px:** ลดขนาดหัวเรื่อง/ตัวเลขสถิติ/statement/quote + ลด padding (index/product.css/works/about/contact)
- **ยืนยันด้วย Chrome preview @430px:** ทุกหน้า (index, model+spectable, works, contact) ไม่มี horizontal overflow, ไม่มี element กว้างเกินจอ, hamburger ทำงาน, grid เป็นคอลัมน์เดียว — ผ่านครบ; จอ >768px พิสูจน์ว่าไม่เปลี่ยน (การแก้ทั้งหมดอยู่ใน media query ≤768)

---

## ครั้งที่ 33 — 2026-07-11

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและอัปเดต CLAUDE.md ให้ทันสถานะปัจจุบัน

**สิ่งที่แก้ไข / ดำเนินการ:**
- **อัปเดต `CLAUDE.md` เดิม** (สร้างครั้งที่ 27) ให้ตรงงานที่ทำหลังจากนั้น — ไม่เขียนใหม่ทั้งไฟล์ แก้เฉพาะส่วนที่ล้าสมัย:
  - เพิ่ม `about.html` ในรายการหน้า + ผังหน้า + path rules (เดิมมีแค่ works/contact)
  - เพิ่มหัวข้อ **Bilingual nav**: label อังกฤษ + ไทยจาก `data-th` ผ่าน `::after{content:attr(data-th)}`, เส้นใต้ย้ายไป `::before`, submenu/breadcrumb/footer ยังไทย — แก้ชื่อเมนูต้องแก้ทั้ง EN + data-th
  - เพิ่มหัวข้อ **Responsive**: breakpoint มาตรฐาน 768 (hamburger/stack/touch 44px) + 480 (ฟอนต์) + 980 (tablet tier), แก้มือถือต้องอยู่ใน media query ≤768 และ mirror ทั้ง index inline + product.css
  - แก้ nav order เป็น Home · Our Films ▾ · Works · About Us · Contact
  - ลบการอ้างถึง `_old-pre-reorg/` (ลบไปแล้วครั้งที่ 29) — เหลือแค่ design originals ใน assets/source/
- ตรวจสถานะจริงก่อนแก้ (หน้า html, markup เมนู, ไฟล์ที่ลบ, การลิงก์ css) ให้ตรงก่อนเขียน

---

## ครั้งที่ 34 — 2026-07-11

**Prompt:**
> font ฟิล์มของเราไม่ตรงแก้ไขให้มันตรง และเปลี่ยนชื่อ Our Films เป็น Products และย่อขนาดเว็บให้เหมาะกับ 430×932 (มือถือยังมีปัญหาเรื่องขนาด ย่อลงอีก) และตรวจเช็คแก้ไขดีไซน์ที่เพี้ยนเมื่อเปิดในโทรศัพท์

**สิ่งที่แก้ไข / ดำเนินการ:**
- **แก้ไทย "ฟิล์มของเรา" ใต้เมนูไม่กึ่งกลาง:** ต้นเหตุคือ `::after` ของ dropdown ถูกตั้ง `flex-basis:100%` แต่ข้อความชิดซ้าย → เพิ่ม `text-align:center` ที่ `.menu a::after` + `.menu .has-sub > a::after` (ทั้ง index inline + product.css) — ซูมยืนยันบนเดสก์ท็อป ไทยกึ่งกลางใต้ EN ทุกเมนู
- **เปลี่ยนเมนู "Our Films" → "Products" (18 ไฟล์)** ด้วย sed `>Our Films$` → `>Products` (data-th ยังเป็น "ฟิล์มของเรา" ตามเดิม) + อัปเดต CLAUDE.md 2 จุด
- **ย่อขนาดชั้นมือถือ (≤480px ครอบ 430×932):**
  - คีย์หลัก `html{font-size:15px}` (ทั้ง 2 stylesheet) — ย่อ typography ที่เป็น rem ทั้งเว็บ ~6% โดย touch target 44px (px) ไม่ลด
  - index: hero 78→70vh, h1/สถิติ/หัวข้อ/statement เล็กลง, `.section` 44px, `.wrap` 18px, ซ่อน scrollcue, การ์ด/brandlogo/fitem/ccard padding ลด, gallery row 200px, **footer เหลือ 1 คอลัมน์**
  - product.css: phead h1/phl.hi b/swatch 260px/fc/cmc/ctaband h2 ลด, breadcrumb ตัดบรรทัดได้ (height:auto + flex-wrap กันบีบ/ล้น), footer 1 คอลัมน์
  - works: hero 48px + mini b 1.9rem, filterbar/works gap ลด, workcard body 18px; about: astat/quote/statement ลด; contact: map margin/vprop/chan ลด
- **ไล่เช็กดีไซน์เพี้ยนบนมือถือ (iframe 430×932 จริง เพราะ OS ไม่ให้ย่อหน้าต่าง):**
  - 🐛 **เจอ+แก้:** submenu (Finnix/3M/…) ในเมนู burger **ชิดซ้ายติดขอบจอ** — กฎ touch `display:flex;justify-content:center` แพ้กฎ base `.submenu a{display:block}` ที่นิยามท้ายไฟล์กว่า (specificity เท่ากัน) → เปลี่ยน selector เป็น `.menu .submenu a` ทั้ง 2 stylesheet — ยืนยันแล้ว display:flex, กึ่งกลาง, สูง 44px
  - 🐛 **กันไว้:** กรอบทองรอบรูป (`.figure::after` translate 18px) เกือบชนขอบจอ → ลดเป็น 12px ที่ ≤480 (index intro + about) — วัดจริง 397+12=409 ≤ 415 พอดี
- **ยืนยันทุกหน้า @430 (iframe):** index/works/about/brand/model/contact — ไม่มี horizontal overflow, ไม่มี element กว้างเกินจอ, root font 15px ทุกหน้า, เมนู+submenu กึ่งกลาง, footer 1 คอลัมน์, hero กระชับ; จอเดสก์ท็อป 1920 ตรวจแล้วหน้าตาเดิม (แก้เฉพาะใน ≤480 + กฎ ::after ที่ผลลัพธ์เท่าเดิมบนจอใหญ่ ยกเว้นไทยที่กึ่งกลางขึ้นตามที่ขอ)

---

## ครั้งที่ 35 — 2026-07-11

**Prompt:**
> (ส่งพรอมป์ต์เจนภาพ Floating Capsule Navbar 2 แบบ — Option 1 มู้ดแอนด์โทน / Option 2 ปุ่ม 3 สเตป default-hover-copied) ปรับมาเป็นในแบบของเราธีมเราและจัดเรียงให้สวยงาม

**สิ่งที่แก้ไข / ดำเนินการ:**
- **สร้างไฟล์ใหม่ `navbar-demo.html`** (หน้าเดโมคอนเซปต์ standalone — ยังไม่ผูกกับเว็บจริง มีแถบเหลืองแจ้งบนหน้า) แปลงพรอมป์ต์เจนภาพเป็น **HTML mockup จริงในธีม WORLD FILM**: แคปซูลกรมท่า `--ink` ลอยด้วยเงานุ่ม บนพื้น `--light` ลายจุด, โลโก้ `logo-white.png` ซ้าย, เมนูสองภาษากลาง (Home/Products/Works/About Us/Contact + ไทยตัวเล็ก), ปุ่ม pill ทอง `--gold-bright` ขวาเป็นเบอร์โทร (แทนอีเมลในต้นแบบ)
- จัดเรียง 3 บอร์ด: **01 Mood & Tone** (ตาม Option 1 + ghost hero ให้เห็นบริบทลอย + ปุ่มกดได้จริง: กดแล้วคัดลอกเบอร์ → ✓ "คัดลอกเบอร์แล้ว" เขียว แล้วคืนสภาพใน 2 วิ) · **02 Button States** (ตาม Option 2: ปกติ/ชี้เมาส์ "แตะเพื่อคัดลอกเบอร์"/กดแล้ว pill ขาว+✓เขียว) · **03 Mobile** (แคปซูลย่อ: โลโก้ + burger กลม + ปุ่มโทรวงกลมทอง ≥44px)
- ยืนยันด้วย Chrome preview: ทั้ง 3 บอร์ดแสดงถูกต้อง + ทดสอบคลิกปุ่มจริง (state เปลี่ยน → คืน) ผ่าน
- **ไม่แตะเว็บจริง** — เป็นบอร์ดคอนเซปต์ให้ดูมู้ดก่อน ถ้าชอบค่อยสั่งติดตั้ง navbar แบบนี้ทั้งไซต์ (แทน header เดิม 18 หน้า)

---

## ครั้งที่ 36 — 2026-07-13

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและสร้าง/ปรับปรุง CLAUDE.md สำหรับ Claude Code

**สิ่งที่แก้ไข / ดำเนินการ:**
- ตรวจสอบ CLAUDE.md เดิมเทียบกับโค้ดจริง: โครงสร้างหน้า, เมนูสองภาษา `data-th` (ครบ 15 หน้า), `.sample-note` (ยังอยู่ครบ 10 หน้ารุ่น), breakpoints, path rules — **ถูกต้องทั้งหมด ไม่ต้องแก้**
- **เพิ่ม 2 จุดที่ขาด:**
  - หมายเหตุ `navbar-demo.html` เป็นหน้าเดโมคอนเซปต์ standalone (ไม่ลิงก์กับเว็บจริง ไม่ใช้ shared chrome/product.css) — **ต้องข้ามไฟล์นี้เวลาแก้ sitewide** (เมนู/เบอร์โทร/footer)
  - หมายเหตุ `README.md` เป็นคู่มือภาษาไทยของเจ้าของเว็บ (มี file tree + ข้อมูลติดต่อซ้ำ) — เวลาเพิ่ม/ลบหน้า หรือแก้ข้อมูลติดต่อ ต้องอัปเดต README.md และบรรทัด "อัปเดตล่าสุด" ด้วย

---

## ครั้งที่ 37 — 2026-07-13

**Prompt:**
> แถบเมนูแก้ไขปรับเรียงให้มันสวยงามเพราะแถบเมนูยังมีช่องว่างอยู่แก้ไข → (สเปก Floating Navigation Bar: แคปซูลลอย เงานุ่ม dark glass, โลโก้ = ปุ่ม Home, hover เปลี่ยนสี 0.2–0.3s, ปุ่ม CTA ขยายความกว้างตอน hover, responsive, minimal) เอาแถบเมนูนี้ไปแทนที่ของเราในเว็บหลักเลยและส่วนธีมปรับให้อยู่ในรูปแบบเรา

**สิ่งที่แก้ไข / ดำเนินการ:**
- **จัดช่องว่างแคปซูลใน `navbar-demo.html`:** `.cap-menu` เพิ่ม `flex:1` + `justify-content:space-evenly` — เมนูกระจายเต็มกลางแถบ ไม่มีช่องโหว่สองข้างแล้ว (ยืนยันทั้งบอร์ด 01/02)
- **ติดตั้ง Floating Capsule Navbar แทน header เดิมทั้งเว็บ 18 หน้า** (root 4 + products 14, ผ่านสคริปต์ PowerShell + template UTF-8):
  - **ถอด `.topbar` ออกทุกหน้า** (LINE/Facebook ยังอยู่ครบใน ส่วนติดต่อ + footer + ปุ่มลอย FAB)
  - `.header` เป็น sticky โปร่งใส, `.nav` เป็นแคปซูลกรมท่า dark glass (`rgba(20,34,61,.92)` + blur 12px + ขอบ hairline + เงานุ่ม 2 ชั้น, เงาเข้มขึ้นเมื่อ scroll), มุมโค้ง 999px, กว้างสุด 980px
  - **โลโก้ขาว = ปุ่มกลับหน้าแรก** — ตัดเมนู "Home" ออกตามสเปก เหลือ Products ▾ / Works / About Us / Contact (สองภาษา data-th เดิม) กระจาย `space-evenly`; hover/active = เปลี่ยนสีทอง transition 0.25s (เลิกใช้เส้นใต้ ::before), ไทยตัวเล็กซ่อนที่ ≤980px
  - **ปุ่ม CTA ขยายได้:** ปกติ "โทร 095-229-2086" → hover พื้นทองเข้ม + สลับข้อความเป็น "แตะเพื่อคัดลอกเบอร์" ด้วยแอนิเมชัน max-width ease-in-out 0.28s → กดคัดลอกเบอร์ ขึ้น ✓ "คัดลอกเบอร์แล้ว" (พื้นขาว) 2 วิ — สคริปต์ `#copyPill` เพิ่มในทุกหน้า
  - **Dropdown Products** เปลี่ยนเป็นการ์ดกรมท่าโค้ง 18px ตามธีมแคปซูล (ลิงก์ขาว hover ทอง)
  - **มือถือ ≤768:** แคปซูลย่อ 58px, pill ซ่อน → ปุ่มโทรวงกลมทอง 44px + burger วงกลมขอบขาว 44px, เมนูเป็นแผงการ์ดกรมท่าโค้ง 24px ลอยใต้แคปซูล (ลิงก์ ≥44px, submenu แบรนด์สีเทาฟ้า)
  - อัปเดต CSS ทั้ง 2 ชุดให้ตรงกัน (index inline + product.css), แก้ specificity `.brand .brand-logo` ให้กฎมือถือชนะกฎ base ที่นิยามทีหลัง
- 🐛 **เจอ+แก้ระหว่างทดสอบ:** `await navigator.clipboard.writeText()` ค้างเมื่อเอกสารไม่โฟกัส ทำให้ปุ่มไม่ตอบสนอง → เปลี่ยนเป็น fire-and-forget `.catch(()=>{})` ทั้ง 18 หน้า — feedback ขึ้นทันทีเสมอ
- **ยืนยันด้วย Chrome (localhost:8753):** เดสก์ท็อป index/works/brand-finnix/model-finnix-ceramic/contact — แคปซูลลอย+active ทองถูกหน้า, dropdown, hover/กดปุ่ม CTA ครบวงจร, เงาเข้มขึ้นตอน scroll; มือถือ 430×932 (iframe) — ไม่มี horizontal overflow (413=413), burger เปิดแผงเมนูครบทุกลิงก์สูง ≥44px
- อัปเดต CLAUDE.md (สถาปัตยกรรม header ใหม่ + กฎ clipboard) และ README.md ให้ตรงของจริง

---

## ครั้งที่ 38 — 2026-07-13

**Prompt:**
> Ultra clean, luxury, modern, minimal, premium web navigation with polished micro-interactions and buttery smooth animations. ใส่ลงไปในแถบเมนูด้วย

**สิ่งที่แก้ไข / ดำเนินการ:**
- **เพิ่มบล็อก `nav micro-interactions` ท้าย stylesheet ทั้ง 2 ชุด** (index inline + product.css — ต้องอยู่ท้ายไฟล์เพื่อ override กฎเดิม) ใช้ easing เดียวกันทั้งระบบ `cubic-bezier(0.22, 1, 0.36, 1)`:
  - **เปิดตัวตอนโหลด:** แคปซูลเลื่อนลง+จางเข้า (`navIn` 0.7s)
  - **Hover เมนู:** ยกตัว -1px นุ่มๆ ควบคู่การเปลี่ยนสีทองเดิม · caret หมุนช้าลงเป็น 0.35s
  - **Dropdown Products:** เปิดแบบ scale 0.97→1 + รายการแบรนด์ 4 ตัว**ไล่จังหวะทีละตัว** (transition-delay 0.05–0.2s) จางกลับทันทีตอนปิด
  - **ปุ่ม CTA ทอง:** hover ยกตัว -1px · **กดแล้วยุบ scale 0.96** (press feedback) · ปุ่ม burger/โทรมือถือกดยุบ scale 0.9, ขอบ burger สว่างขึ้นตอน hover
  - **แผงเมนูมือถือ:** เปิดแบบเลื่อนลง+จาง พร้อมลิงก์ 4 รายการ**ไล่จังหวะ** (animation-delay 0.05–0.2s) — มีบล็อก ≤768 ล้าง transform ของ dropdown เดสก์ท็อปกันชนกัน
- **Smart hide-on-scroll (ทั้ง 18 หน้า, สคริปต์ PS 2 รอบ — root มีคอมเมนต์เดิม / products ไม่มี):** เลื่อนลงเกิน 260px แถบเมนูเลื่อนหลบขึ้นนุ่มๆ (translateY -130%, 0.45s) · เลื่อนขึ้นเมื่อไหร่โผล่กลับทันที · ใกล้หัวหน้า/แผงเมนูมือถือเปิดอยู่ = ไม่ซ่อน
- **ยืนยันด้วย Chrome:** wheel ลง → แคปซูลหลบ, wheel ขึ้น → โผล่กลับ ✓ · dropdown ทุกรายการถึง opacity 1 พร้อม delay ไล่ลำดับ ✓ · แผงมือถือ (iframe 430) ลิงก์+submenu ครบ ✓ — หมายเหตุ: ใน iframe/แท็บ background Chrome หยุดนาฬิกาแอนิเมชัน (currentTime=0) เป็นพฤติกรรมปกติ ตรวจ end state ด้วย WAAPI `finish()` แล้วถูกต้อง
- อัปเดต CLAUDE.md: บล็อก micro-interactions ต้องคงอยู่ท้าย stylesheet + พฤติกรรม `.nav-hidden`

---

## ครั้งที่ 39 — 2026-07-13

**Prompt:**
> แถบเมนูตรง product มันห่างทำให้มันชิดๆ กัน หรือจัดสัดส่วนให้มันสวยงามและใส่ Animation: Framer Motion style / Smooth width interpolation / Fade text transition / Hover scale 1.02 / Active state with subtle glow ไปแถบเมนูนั้นด้วย

**สิ่งที่แก้ไข / ดำเนินการ (ทั้ง index inline CSS + product.css):**
- **จัดสัดส่วนเมนูใหม่:** เลิกใช้ `space-evenly` (ที่ดันเมนูห่างกันมาก) → `justify-content: center` + gap 4px — เมนู 4 รายการเกาะกลุ่มกลางแถบแบบกระชับ สมมาตรระหว่างโลโก้กับปุ่มทอง; ลิงก์แต่ละตัวเป็นทรง pill (padding 7×15px + radius 999px) ให้มีจังหวะหายใจ · tier ≤980 ลด padding เหลือ 12px
- **Animation สไตล์ Framer Motion:**
  - **Hover scale 1.02** + ยกตัว -1px + พื้นหลัง pill ขาวโปร่ง `rgba(255,255,255,.07)` จางเข้า 0.3s (ปุ่มทอง CTA ก็ scale 1.02 ตอน hover ด้วย)
  - **Active state = subtle glow ทอง:** พื้น `rgba(217,165,58,.13)` + เงาเรือง 18px + วงแหวน inset บางๆ — หน้าไหนเปิดอยู่ pill นั้นเรืองทอง
  - **Smooth width interpolation + fade text:** ปุ่ม CTA ใช้ max-width interpolate + crossfade ข้อความ (มีอยู่แล้วจากครั้งที่ 37 — คงเดิม), พื้น pill hover ยืดตามความกว้างรายการอัตโนมัติ
  - มือถือ ≤768: pill radius ปรับเป็น 14px (แถวเต็มกว้าง), ตัด scale ตอน hover, active glow ยังอยู่
- **ยืนยันด้วย Chrome:** works.html เดสก์ท็อป — เมนูเกาะกลุ่มกลาง, "Works" เรืองทอง, hover "Products" ขึ้น pill ขาว+scale พร้อม dropdown ปกติ; มือถือ 430 (iframe) — ไม่มี overflow, แถว active radius 14 + glow ครบ, แผงเมนูเปิดปกติ

---

## ครั้งที่ 40 — 2026-07-13

**Prompt:**
> แถบเมนูเพิ่มหน้า home เข้าไปด้วยแต่ลิงก์กับหน้าหลัก และแถบเมนูยังมีช่องว่างอยู่เลยตรง product แก้ไขเดี๋ยวนี้

**สิ่งที่แก้ไข / ดำเนินการ:**
- **เพิ่มเมนู "Home" (หน้าแรก) กลับเข้าแถบเมนูทั้ง 18 หน้า** (สคริปต์ PS + snippet UTF-8): ลิงก์ไป `index.html` (products ใช้ `../index.html`), หน้าแรกได้ `class="active"` → pill เรืองทองบนหน้า Home; ลำดับเมนูเป็น **Home · Products ▾ · Works · About Us · Contact**
- **แก้ช่องว่างตรง Products:** ต้นเหตุคือ caret (ลูกศร dropdown) เพิ่มความกว้างท้ายคำ — ลด gap ระหว่างคำกับ caret เหลือ 5px + ตัด padding ขวาของลิงก์ Products เหลือ 11px (ทั้ง 2 stylesheet) → วัดจริงช่องว่างระหว่างเมนูทุกคู่ **เท่ากันหมด 4px** และระยะข้างซ้าย/ขวาของกลุ่มเมนูสมมาตร 100.6px พอดี
- เพิ่ม stagger delay รายการที่ 5 ในแผงเมนูมือถือ (0.25s) รองรับ 5 เมนู
- เก็บกวาด: ลบคอมเมนต์ `<!-- TOP BAR -->` ตกค้างจากยุค topbar ออกทั้ง 18 หน้า
- **ยืนยันด้วย Chrome:** works.html — Home เข้าแถบ ช่องว่างเท่ากันทุกคู่ (วัดด้วย JS), Works เรืองทอง; index.html — Home active เรืองทอง; มือถือ 430 (iframe) — เมนู 5 รายการ delay ไล่ 0.05–0.25s ครบ ไม่มี overflow
- อัปเดต CLAUDE.md (ลำดับเมนูใหม่ + index มาร์ก Home active)

---

## ครั้งที่ 41 — 2026-07-13

**Prompt:**
> แก้ไขหัวข้อ product มันยังใหญ่กว่าหัวข้ออื่นๆ อยู่เลยปรับขนาดให้เท่ากับหัวข้ออื่นเดี๋ยวนี้

**สิ่งที่แก้ไข / ดำเนินการ:**
- ต้นเหตุ: ลิงก์ Products มีโครงพิเศษต่างจากเมนูอื่น — ลูกศร caret + เลย์เอาต์ row + padding เฉพาะตัว ทำให้กล่อง/pill ใหญ่กว่าหัวข้ออื่น
- **ถอด caret (ลูกศร dropdown) ออกจาก markup ทั้ง 18 หน้า** (สคริปต์ PS regex) — สอดคล้องสเปกเดิม "minimal ไม่มีไอคอนเกินจำเป็น"; dropdown ยังเปิดตอน hover/focus เหมือนเดิม
- **ลบ CSS พิเศษของ Products ทั้งหมด** (ทั้ง index inline + product.css): กฎ row/flex-wrap/padding-right ของ `.has-sub > a`, กฎ `::after flex-basis`, กฎ caret ทุกจุด (base + hover หมุน + micro-transition + ซ่อนที่มือถือ) → Products ใช้เลย์เอาต์ column เดียวกับเมนูอื่นทุกประการ
- **วัดจริงหลังแก้:** ทั้ง 5 หัวข้อ สูง 42.6px / ฟอนต์ 14.08px / padding 7×15px **เท่ากันหมด** — ความกว้าง Products 87.7px แคบกว่า About Us 88.8px แล้ว (เหลือต่างตามความยาวข้อความเท่านั้น)
- ยืนยันด้วย Chrome: brand-finnix — pill ทอง active ของ Products ขนาดเดียวกับหัวข้ออื่น, dropdown เปิดปกติ (ทดสอบผ่าน focus-within = เส้นทาง CSS เดียวกับ hover)

---

## ครั้งที่ 42 — 2026-07-13

**Prompt:**
> แถบเมนูตรงหัวข้อจัดหัวข้อให้มัน spread ให้มันเท่าๆ และตรวจเช็คข้อผิดผลาดในเว็บไซต์

**สิ่งที่แก้ไข / ดำเนินการ:**
- **จัดหัวข้อเมนูแบบ spread เท่ากัน:** `.menu` เปลี่ยน `justify-content: center` → `space-evenly` (ทั้ง index inline + product.css) — วัดจริง: ช่องว่างระหว่างหัวข้อทุกคู่ **44px เท่ากันหมด** ขอบซ้าย/ขวาของกลุ่มเมนูสมมาตร 54px, เมนู 5 หัวข้อกระจายเต็มพื้นที่กลางแคปซูลอย่างสมดุล
- **ตรวจสุขภาพทั้งเว็บ (สคริปต์ site-check.ps1 + Chrome):**
  - ✅ ลิงก์/รูป/`data-img` ภายในทั้ง 18 หน้า — ไฟล์ปลายทางมีจริงครบ ไม่มีลิงก์เสีย
  - ✅ ไม่มี id ซ้ำในหน้าเดียวกัน · แท็กหลัก (header/footer/nav/section/style/script) เปิด-ปิดครบทุกหน้า
  - ✅ Console ไม่มี error เลยทั้ง 6 หน้าหลักที่ไล่โหลด (index/works/about/contact/brand-finnix/model-finnix-ceramic)
  - ✅ มือถือ 430: ไม่มี horizontal overflow, แผงเมนู 5 รายการเปิดปกติ
  - 📌 พบว่า `navbar-demo.html` ถูกลบออกจากโปรเจกต์แล้ว (ไฟล์เดโมคอนเซปต์ — ดีไซน์ถูกติดตั้งจริงไปแล้วตั้งแต่ครั้งที่ 37) → เอาการอ้างถึงออกจาก CLAUDE.md ให้ตรงสภาพจริง

---

## ครั้งที่ 43 — 2026-07-13

**Prompt:**
> เอาแสงสีเหลืองเวลาเราอยู่หน้านั้นแล้วมีแสงค้างอยู่ตรงหัวข้อหน้าเมนูนั้นเอาออก แล้วตรงหัวข้อ product เวลาเอาเคอร์เซอร์ไปชี้ให้มันโชว์แบรนด์ฟิล์มค้างหน่อยเพราะแบบเดิมพอจะมาเลือกแบรนด์แล้วมันหาย และขอ logo แบรนด์ใหญ่ขึ้นให้เห็นชัดๆ ในขนาดเหมาะสม และหน้าไหนที่มีแถวเขียนว่า โครงร่างหน้า "…" เอาแถวนั้นออก (ยืนยันผ่าน AskUserQuestion: ใส่โลโก้ในดรอปดาวน์)

**สิ่งที่แก้ไข / ดำเนินการ (แผนอนุมัติใน plan mode):**
- **เอา glow ทองของเมนู active ออก** (ทั้ง index inline + product.css): ลบกฎ `.menu a.active { background + box-shadow }` — เหลือตัวหนังสือสีทองบอกหน้าปัจจุบันอย่างเดียว (วัดจริง: bg โปร่งใส, shadow none)
- **Dropdown ค้างให้เลือกแบรนด์ได้:**
  - ปิดช่องตาย: สะพาน hover `.submenu::before` 14→18px (ช่องว่างแคปซูล→ดรอปดาวน์ = 16px, วัดจริงสะพานคลุมเต็ม)
  - เพิ่ม grace delay ตอนปิด: `opacity/transform` หน่วง 0.35s + `visibility` หน่วง 0.6s, สถานะเปิด `transition-delay: 0s` (เปิดทันที ปิดค่อยหาย) + `.submenu li` หน่วง 0.35s ให้รายการไม่จางก่อนแผง
- **ใส่โลโก้แบรนด์ในดรอปดาวน์ทั้ง 18 หน้า** (สคริปต์ PS จำกัดเฉพาะบล็อก `<ul class="submenu">` — ไม่โดน footer/breadcrumb ที่มีลิงก์หน้าตาเดียวกัน): แต่ละรายการมีชิปพื้นขาวโค้ง 8px ขนาด 64×36px ใส่โลโก้จาก `assets/brands/*.png` แบบ object-fit contain + ชื่อแบรนด์ข้างๆ; ขยาย `.submenu` เป็น min-width 230px, แถวสูง 54px
- 🐛 เจอ+แก้ระหว่างทำ: ลิงก์ดรอปดาวน์สืบทอด `flex-direction: column` จากกฎ `.menu a` ทำให้ชิปซ้อนบนชื่อ → เพิ่ม `flex-direction: row` ที่ `.submenu a`
- **ลบแถว "โครงร่างหน้า …" (skeleton note):** works.html ("ผลงาน") + about.html ("About Us") — สองหน้าที่มีทั้งหมด; แบนเนอร์สเปกตัวอย่างบนหน้ารุ่นคงไว้ตามเดิม
- **ยืนยัน:** เดสก์ท็อป works — active ทองล้วนไม่มีเรือง, ดรอปดาวน์โชว์ชิปโลโก้ 4 แบรนด์ชัดเจน (Finnix/3M/Regionfilm/Ultra Guard); มือถือ 430 (iframe) — แผงเมนูมีชิป 64×36 แถวสูง 54px ไม่ล้นจอ; ไม่มี console error; "โครงร่างหน้า" หายจากทั้ง 2 หน้า

---

## ครั้งที่ 44 — 2026-07-13

**Prompt:**
> เก่งมากเคลียร์ไฟล์ที่ไม่จำเป็นออกและอัพขึ้น git เราได้เลย

**สิ่งที่แก้ไข / ดำเนินการ:**
- **ตรวจไฟล์ไม่จำเป็น:** ใน repo ไม่มีไฟล์ขยะ/untracked เหลืออยู่ — `navbar-demo.html` (ไฟล์เดโม) ถูกลบไปแล้วก่อนหน้านี้, ไฟล์ชั่วคราวทั้งหมด (template/สคริปต์ PS) อยู่นอก repo ใน scratchpad ของเซสชัน, `assets/source/` ถูก .gitignore ตามเดิม
- **Commit + push ขึ้น GitHub** (`KUYLA2555/ProjectWorldFilm`, branch main): งานครั้งที่ 36–44 ทั้งหมด — Floating Capsule Navbar ทั้งไซต์, micro-interactions, โลโก้ในดรอปดาวน์+grace delay, ลบแถวโครงร่าง, อัปเดตเอกสาร (22 ไฟล์)

## ครั้งที่ 45 — 2026-07-15

**Prompt:**
> /init (วิเคราะห์โค้ดเบสและปรับปรุง CLAUDE.md)

**สิ่งที่แก้ไข / ดำเนินการ:**
- ตรวจสอบ CLAUDE.md เดิมกับสภาพโค้ดจริง — เนื้อหาสถาปัตยกรรม/เส้นทางไฟล์/เบอร์ติดต่อยังถูกต้องทั้งหมด
- เพิ่มหมายเหตุใหม่ใน CLAUDE.md: editor ของผู้ใช้ auto-format HTML ตอนบันทึก (สไตล์ Prettier) ทำให้แท็กถูกตัดบรรทัดต่างกันในแต่ละไฟล์ — ให้ยึด fragment สั้น ๆ (href, เบอร์โทร) เวลาแก้ทั้งไซต์ และ diff แบบ reflow เป็นเรื่องปกติ
- เพิ่มหัวข้อ "Git & publishing" ใน CLAUDE.md: remote คือ repo ส่วนตัว KUYLA2555/ProjectWorldFilm, push เมื่อผู้ใช้สั่งเท่านั้น, รูปแบบ commit message (conventional prefix + สรุปภาษาไทย)

## ครั้งที่ 46 — 2026-07-15

**Prompt:**
> หน้า home แถวคอลัมน์ ที่บอก40+ สาขาทั่วประเทศทั้งแถวนั้นเอาออกแล้วย้าย คอลัมน์ ฟิล์มกรองแสงที่ให้มากกว่าการลดแสงย้ายไป แทนคอลัมน์ที่เอาออกและย้าย ช่องทางการติดต่อ ทั้ง line call facebook ย้ายไปรวมกับคอลัมน์ศูนย์ติดตั้งฟิล์มกรองแสง และตรงคอลัมน์ศูนย์ติดตั้งฟิล์มกรองแสงพื้นหลังทำเป็นภาพสไลด์ เอา 3 รูป

**สิ่งที่แก้ไข / ดำเนินการ:** (index.html หน้าเดียว)
- **ลบแถบสถิติ (STAT BAND)** "40+ สาขา / 99% UV / 85% ลดร้อน / 8 ปีรับประกัน" ออกทั้ง section พร้อม CSS `.stats`/`.stat` และ rules ใน media query ทุกจุด
- **ย้าย section "ฟิล์มกรองแสงที่ให้มากกว่าการลดแสง" (#why)** ขึ้นมาแทนที่แถบสถิติ (อยู่ใต้ hero) และสลับพื้นหลังกับ section แนะนำ (#about): #why เป็นพื้น paper, #about เป็น section--tint เพื่อคงจังหวะสลับสีพื้นหน้าเพจ
- **ย้ายการ์ดช่องทางติดต่อ LINE / Call / Facebook** จาก section ติดต่อ (#contact) ขึ้นไปอยู่ใน hero แทนปุ่มโทรสีทองเดิม (ปุ่ม .hero-cta ถูกถอดเพราะซ้ำกับการ์ด Call) พร้อมสไตล์ใหม่ `.hero .ccard` แบบกระจกโปร่ง (พื้นขาวโปร่ง + blur + ไอคอนวงกลมโทนทอง) — #contact เหลือหัวข้อ + featurebar
- **เปลี่ยนพื้นหลัง hero เป็นสไลด์โชว์ 3 รูป** (`.hero-slides` + keyframe `heroSlide` 18 วิ/รอบ เฟดสลับรูปละ 6 วิ พร้อมซูมช้าแบบ Ken Burns): รูปโหลดผ่าน `data-img` (ขยาย selector ใน JS เป็น `.tile[data-img], .hero-slides .slide[data-img]` — พังก็เห็นพื้นน้ำเงินเหมือนเดิม), ย้าย gradient ทึบไปไว้ที่ `.hero::before` ทับสไลด์, รองรับ `prefers-reduced-motion` (หยุดสไลด์ค้างรูปแรก)
- **ตรวจสอบผ่านเบราว์เซอร์** (HttpListener :8137): สไลด์หมุนครบ 3 รูป (เช็ค opacity สลับ 0/1 จริง), การ์ดติดต่อแสดงใน hero, ลำดับ section ใหม่ถูกต้อง, ไม่มี console error

## ครั้งที่ 47 — 2026-07-15

**Prompt:**
> หน้าhome ภาพที่เป็นพื้นหลัง Full-screen Immersive Imagery: ภาพสไลด์ใช้เป็นภาพพื้นหลังเต็มความกว้างของหน้าจอ … Overlaid and Integrated Typography: ข้อความหลัก "Welcome to SEACLEAR" ฟอนต์ Serif หรูหรา มีใบไม้จากต้นสนทางด้านซ้ายมาบังตัวอักษรบางส่วน … Active Pagination Navigation: จุด pagination กลางล่าง จุด active สีทอง จุดอื่นเทาอ่อน แล้วตรงหัวข้อ works เปลี่ยนชื่อเป็น Project reference

**สิ่งที่แก้ไข / ดำเนินการ:**
- **Hero เต็มจอจริง (Full-screen Immersive):** `min-height: 100svh` + ดึง hero ขึ้นใต้แคปซูล nav ด้วย `margin-top: -92px` ให้ภาพสไลด์ชนขอบบนของจอ (มือถือ ≤480 เปลี่ยนจาก 70vh เป็น 100svh ด้วย)
- **เปลี่ยนรูปสไลด์ 3 รูปเป็นธีมกระจกอาคาร/สภาพแวดล้อม** (ตึกกระจกสีน้ำเงิน, บ้านผนังกระจกพร้อมสวน, อาคารกระจกสมัยใหม่ — ตรวจ HTTP 200 ทุก URL แล้ว)
- **Typography ทับบนภาพ:** เปลี่ยน H1 เป็น "Welcome to / WORLD FILM" (serif Cormorant + em ทองตัวเอียง) — ปรับจาก "SEACLEAR" ในภาพอ้างอิงให้เป็นแบรนด์จริง; ข้อความรองภาษาไทยเปลี่ยนเป็นฟอนต์ Trirong (serif) สีขาว weight 500
- **ใบสนบังตัวอักษร:** เพิ่ม SVG `.hero-foliage` (กิ่งสน 8 ช่อจาก ellipse fan + มวลใบชิดขอบซ้าย) เบลอ 10px + แกว่งช้า ๆ (`foliageSway`) วาง z-index ทับข้อความ, ความกว้างคำนวณ `calc(53vw - 272px)` ให้ปลายกิ่งบังต้นตัวอักษรพอดีทุกขนาดจอ, ซ่อนที่ ≤768, ปิดแกว่งเมื่อ prefers-reduced-motion
- **จุด Pagination:** เพิ่ม `.hero-dots` 3 จุดกลางล่าง (ปุ่มจริง คลิกข้ามสไลด์ได้) จุด active สีทอง #e7c879 + วงแหวนโปร่ง จุดอื่นเทาโปร่ง; เปลี่ยนกลไกสไลด์จาก CSS keyframes เป็น JS interval 6 วิ (class `.on` + transition เฟด 1.4s / ซูม Ken Burns 7s) เพื่อ sync กับจุด
- **ย้าย scrollcue "เลื่อนลง"** จากกลางล่างไปขวาล่าง (right:120px ไม่ชน FAB) เปิดพื้นที่ให้จุด pagination
- **เปลี่ยนเมนู "Works" → "Project Reference"** ทั้งไซต์ 18 ไฟล์ (root 4 + products 14) — data-th="ผลงาน" คงเดิม; อัปเดต CLAUDE.md 3 จุดที่อ้างชื่อเมนู
- **ตรวจผ่านเบราว์เซอร์:** ภาพเต็มจอถึงขอบบน, ใบสนบังตัว "W" พอดี, จุดที่ active เป็นสีทองแสดงสไลด์ปัจจุบัน, ไม่มี console error

## ครั้งที่ 48 — 2026-07-15

**Prompt:**
> หน้า home คอลัมน์ยินดีต้อนรับ Layout แบบ 2 คอลัมน์ (Split Layout): ซ้ายเป็นกรอบรูป ขวาเป็นตัวหนังสือ white space เยอะ หัวข้อ sans-serif หนาใหญ่ (แบบ "SIX ELEMENTS") เนื้อหาฟอนต์บางอธิบายสรรพคุณฟิล์ม + ลูกศรซ้ายเส้นบาง/ลูกศรขวาในกรอบสี่เหลี่ยม + จุดไข่ปลาล่างสุด จุด active สีทอง จุดอื่นเทาเข้ม นำลูกเล่นมาปรับให้เข้ากับเว็บเรา

**สิ่งที่แก้ไข / ดำเนินการ:** (index.html — section ยินดีต้อนรับ #about)
- **แปลง section ยินดีต้อนรับเป็น carousel แบบ Split Layout 5 สไลด์:** ซ้าย = กรอบรูป (`.figure-slides` + `.fslide` เฟด/ซูมเบา ๆ, กรอบทอง + ตราประกัน 8 ปีคงเดิม), ขวา = แผงข้อความ (`.intro-panels` + `.ipanel` ซ้อนใน grid เดียว เฟดสลับ)
- **Typography ตามแบบ:** kicker ทองตัวพิมพ์เล็ก "01 / 05 · …", หัวข้อ IBM Plex Sans Thai **หนา 700 ขนาดใหญ่** (แทน .statement serif เดิม — ลบ CSS เก่า), เนื้อหา weight 300 บรรทัดโปร่ง line-height 1.9 จำกัด 46ch เว้น white space เยอะ
- **เนื้อหา 5 สไลด์:** 1) ยินดีต้อนรับ + ลายเซ็น WORLD FILM Center 2) ลดความร้อนสูงสุด 85% 3) กัน UV 99% 4) ใสคมชัดไม่บังวิว 5) เพิ่มความเป็นส่วนตัว — รูปประกอบใหม่ 5 รูป (ตรวจ HTTP 200 แล้ว) โหลดผ่าน data-img (ขยาย selector loader รวม `.fslide[data-img]`)
- **ลูกศรนำทาง:** ซ้าย = ลูกศรเส้นบางเรียบ (hover เป็นทอง+ขยับซ้าย), ขวา = ลูกศรในกรอบสี่เหลี่ยม 58px (hover พื้นกรมลูกศรทอง) ตามภาพอ้างอิง
- **จุดไข่ปลาล่างสุด:** 5 จุดกลาง section (ตามจำนวนสไลด์จริง — ภาพอ้างอิงมี 8) จุด active สีทอง+ขยาย จุดอื่นเทาเข้ม #97a1b3 ตัดกับพื้น section--tint; กดจุดกระโดดไปสไลด์นั้นได้
- **JS controller ใหม่** (`showIntro` + ปุ่ม iprev/inext + จุด) เลื่อนวนลูปได้สองทิศ ไม่มี autoplay (ให้ผู้ใช้กดอ่านเอง)
- **ตรวจผ่านเบราว์เซอร์:** กดลูกศรขวาแล้วรูป+ข้อความ+จุดสลับพร้อมกัน (จุดที่ 2 เป็นสีทองตรงตามภาพอ้างอิง), ไม่มี console error

## ครั้งที่ 49 — 2026-07-15

**Prompt:**
> อัพขึ้น github เลย หลังจากนี้อัพขึ้น github ทุกครั้ง

**สิ่งที่แก้ไข / ดำเนินการ:**
- Commit + push งานครั้งที่ 45–48 ทั้งหมดขึ้น GitHub (`KUYLA2555/ProjectWorldFilm`, branch main): hero เต็มจอ+สไลด์ 3 รูป+ใบสน+จุด pagination, section ยินดีต้อนรับแบบ split carousel, เมนู Works → Project Reference (18 ไฟล์), เอกสาร CLAUDE.md/CHANGELOG
- **กติกาใหม่:** หลังจากนี้ทุกครั้งที่แก้งานจะ commit + push ขึ้น GitHub ให้อัตโนมัติ (บันทึกใน CLAUDE.md หัวข้อ Git & publishing แล้ว)

## ครั้งที่ 50 — 2026-07-16

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและสร้าง/ปรับปรุงไฟล์ CLAUDE.md

**สิ่งที่แก้ไข / ดำเนินการ:**
- ตรวจสอบ CLAUDE.md เดิมเทียบกับโค้ดจริงทั้งหมด (โครงสร้างเมนู, CSS สองชุด, ตำแหน่งบล็อก micro-interactions, เบอร์ติดต่อ, README, .gitignore) — ถูกต้องครบถ้วน
- เพิ่มกติกาที่ยังไม่ได้บันทึกในหัวข้อ Page hierarchy: ลิงก์เมนู "Products" เป็น anchor ไปยัง `#brands` (หน้าแรก) / `index.html#brands` (หน้า root อื่น) / `../index.html#brands` (หน้าในโฟลเดอร์ products)

## ครั้งที่ 51 — 2026-07-16

**Prompt:**
> ในหมวด Products ที่มีให้เลือกแบรนด์ เอารูป logo ออกเหลือแค่ font

**สิ่งที่แก้ไข:**
- เอาชิปโลโก้แบรนด์ (`<span class="bl">` + รูป `assets/brands/*.png`) ออกจาก dropdown เมนู Products ทั้ง 15 หน้า — เหลือชื่อแบรนด์เป็นตัวอักษรอย่างเดียว (Finnix / 3M / Regionfilm / Ultra Guard)
- ลบ CSS `.submenu .bl` และ `.submenu .bl img` ออกจากทั้งสอง stylesheet (inline ใน `index.html` + `products/product.css`)
- ลด `min-width` ของ `.submenu` จาก 230px → 180px ให้สมดุลกับเมนูแบบข้อความล้วน (แก้ทั้งสองไฟล์)
- ตรวจสายตาผ่าน localhost แล้วทั้งหน้าแรกและหน้ารุ่นฟิล์ม — dropdown แสดงผลถูกต้อง
- อัปเดต CLAUDE.md (หมายเหตุเรื่องชิปโลโก้ใน dropdown)

หมายเหตุ: การ์ดโลโก้แบรนด์ 4 ช่องในหน้าแรก (section `#brands`) ยังคงเป็นรูปโลโก้เหมือนเดิม — แก้เฉพาะ dropdown ตามคำสั่ง

## ครั้งที่ 52 — 2026-07-16

**Prompt:**
> ในหน้า home แถวคอลัมน์ยินดีต้อนรับสู่ world film ปรับลูกศรไปไว้ด้านข้างและสร้างกรอบตรงนั้น

**สิ่งที่แก้ไข:**
- ย้ายลูกศรเลื่อนสรรพคุณ (iprev/inext) จากแถวล่างใต้ข้อความ ไปประกบซ้าย–ขวาของคอลัมน์ข้อความ กึ่งกลางแนวตั้ง (`.intro-copy` เป็น `position:relative` + padding ข้างละ 76px, ปุ่มเป็น `position:absolute`)
- ใส่กรอบวงกลมให้ลูกศรทั้งสองปุ่ม (52px, เส้นขอบสีกรมท่าโปร่ง 40%) — เดิมซ้ายเป็นเส้นลอยไม่มีกรอบ ขวาเป็นกรอบสี่เหลี่ยม
- Hover: พื้นเปลี่ยนเป็นสีกรมท่า ลูกศรสีทอง พร้อมขยับตามทิศทาง (ซ้าย -3px / ขวา +3px)
- จอมือถือ: ≤768px ปุ่ม 46px + padding 58px, ≤480px ปุ่ม 44px + padding 50px (แก้ใน media query เดิม)
- ตรวจผ่าน localhost แล้ว: ตำแหน่ง/กรอบ/hover ถูกต้อง คลิกแล้วสไลด์เปลี่ยนแผง+รูป+จุดตามปกติ

## ครั้งที่ 53 — 2026-07-18

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและสร้าง/ปรับปรุงไฟล์ CLAUDE.md

**สิ่งที่แก้ไข:**
- ตรวจสอบ CLAUDE.md เดิมเทียบกับโค้ดจริง — โครงสร้างไฟล์/เมนู/CSS สองชุด/เบอร์ติดต่อ/กติกา git ยังถูกต้องครบ (รวมงานครั้งที่ 51–52 ที่สะท้อนในเอกสารแล้ว)
- เพิ่มย่อหน้าใหม่ "Homepage-only sliders" ใน CLAUDE.md: hero slideshow (`.hero-slides` เฟดทุก 6 วิ + `.hero-dots`) และ intro split carousel ใน section ยินดีต้อนรับ (`.figure-slides`/`.intro-panels` sync ผ่าน `showIntro`, ลูกศรวงกลม `.iprev`/`.inext` ประกบข้างคอลัมน์ข้อความ + `.intro-dots`) — CSS+JS อยู่เฉพาะใน `index.html` inline
- แก้คำอธิบาย lazy background images ให้ตรงจริง: มีเฉพาะ `index.html` (`.tile`/`.slide`/`.fslide`) และ `works.html` (`.thumb`) ไม่ได้อยู่ทุกหน้า

## ครั้งที่ 54 — 2026-07-18

**Prompt:**
> แก้ไข font ทั้งหมดข้อเป็น font ในข้อความ ความเย็นและธรรมชาติ อยู่ร่วมกันได้อย่างลงตัว

**สิ่งที่แก้ไข:**
- ข้อความอ้างอิง "ความเย็นสบายและแสงธรรมชาติ อยู่ร่วมกันได้อย่างลงตัว" (หัวข้อ section ยินดีต้อนรับ หน้าแรก) ใช้ font **IBM Plex Sans Thai** → เปลี่ยน font ทั้งเว็บให้เป็น IBM Plex Sans Thai ตัวเดียว
- แทนที่ `font-family` ที่เป็น Trirong (หัวข้อไทย `.h-thai`) และ Cormorant Garamond (ตัว display อังกฤษ `.serif`) ทั้งหมดด้วย `"IBM Plex Sans Thai", sans-serif` — แก้ทั้ง inline CSS ใน `index.html`, `products/product.css`, และ style เฉพาะหน้าใน `works.html` / `about.html` / `contact.html` (รวม 19 ไฟล์, sed pass เดียว)
- ตัด Cormorant Garamond + Trirong ออกจากลิงก์ Google Fonts ทุกหน้า (เหลือโหลด IBM Plex Sans Thai family เดียว) และลบ `@import` Trirong ใน `product.css` — หน้าเว็บโหลดเบาลง
- คลาส `.h-thai` / `.serif` ยังอยู่ใน markup เหมือนเดิม (เปลี่ยนเฉพาะ font ที่ชี้ไป) ขนาด/น้ำหนักตัวอักษรคงเดิมทุกจุด
- **ตรวจผ่าน localhost + Chrome:** หน้าแรก (hero "Welcome to WORLD FILM" + section ยินดีต้อนรับ) และหน้า brand-finnix — computed font ของทุก element เป็น IBM Plex Sans Thai, `document.fonts` โหลดแค่ family เดียว, ไม่มี Trirong/Cormorant หลงเหลือ (grep ยืนยัน)
- อัปเดต CLAUDE.md (หมายเหตุ Design tokens: เหลือ font เดียว)

## ครั้งที่ 55 — 2026-07-18

**Prompt:**
> ลบลายน้ำในหน้า home ตรงแถวคอลัมน์ welcome to world film ที่มีลายน้ำฝั่งซ้ายเอาออก world film ให้ดูเป็นทางการมากกว่านี้หน่อย

**สิ่งที่แก้ไข:** (index.html หน้าเดียว — ลายน้ำเป็น component เฉพาะหน้าแรก)
- **ลบลายน้ำใบสนเบลอฝั่งซ้ายของ hero ออกทั้งหมด:** SVG `.hero-foliage` (กิ่งสน 8 ช่อ + เบลอ 10px ที่บังตัวอักษร) พร้อม CSS ทุกจุด — กฎหลัก + keyframe `foliageSway` + กฎใน `prefers-reduced-motion` + กฎซ่อนที่ ≤768px
- **ปรับ "WORLD FILM" ให้ดูเป็นทางการขึ้น:** เปลี่ยนจากตัวเอียง (italic) เป็นตัวตรง หนา 700 + เพิ่มระยะห่างตัวอักษร 0.04em — ยังคงสีทอง #e7c879 เดิม
- ตรวจผ่าน localhost + Chrome: ฝั่งซ้าย hero โล่งสะอาด ไม่มีเงาเบลอบังข้อความแล้ว หัวข้อ "Welcome to / WORLD FILM" อ่านชัดเต็มตา

## ครั้งที่ 56 — 2026-07-18

**Prompt:**
> ในหน้า Products นั้นในแต่ล่ะแบรนด์ที่มีรุ่นแต่ล่ะรุ่นเป็นช่องสี่เหลี่ยมแก้ไขสามารถให้มันกดตรงไหนก็ได้ในช่องสี่เหลี่ยมนั้นเเพื่อไปยังอีกหน้านึ่งแก้ไขทุกทุกรุ่น

**สิ่งที่แก้ไข:** (products/product.css ที่เดียว — มีผลการ์ดรุ่นทั้ง 10 ใบใน 4 หน้าแบรนด์)
- ทำให้**การ์ดรุ่นฟิล์ม (`.cmc`) กดได้ทั้งใบ** ด้วยเทคนิค stretched-link: เพิ่ม `position:relative` ที่ `.cmc` และ pseudo-element `.cmc .go::after` (`position:absolute; inset:0`) ยืดพื้นที่คลิกของลิงก์ "ดูรายละเอียด" ให้คลุมเต็มการ์ด — ไม่ต้องแก้ HTML แม้แต่บรรทัดเดียว
- โบนัสจาก markup เดิม: hover ตรงไหนของการ์ดก็ได้ลูกศร "ดูรายละเอียด" ขยับ + การ์ดยกตัว + cursor เป็นรูปมือทั้งใบ ทำงานทั้ง desktop และมือถือ
- **ตรวจผ่าน localhost + Chrome:** brand-finnix — `elementFromPoint` กลางการ์ดชี้ที่ลิงก์ `a.go` และคลิกจริงบนกล่องสเปค (ไม่ใช่ปุ่ม) นำทางไป `model-finnix-ceramic.html` สำเร็จ

## ครั้งที่ 57 — 2026-07-18

**Prompt:**
> ในหน้า Products จุดเด่นทำไมต้องเลือก แถวคอลัมน์นั้นเอาออก และแถวคอมลัมน์เหมาะกับพื้นที่ เอาออก แล้วนำแถวคอลัมน์ในติดต่อเราพร้อมให้คำปรึกษาทุกพื้นที่ มาเพิ่มในหน้า รุ่นแต่ล่ะและตัด หมายเหตุตัวเลขสเปกในหน้านี้เป็นข้อมูลตัวอย่าง เอาแถวคอลัมน์นั้นออก

**สิ่งที่แก้ไข:** (หน้ารุ่นฟิล์มทั้ง 10 หน้า — perl pass เดียวผ่าน marker comment ASCII)
- **ลบ section "จุดเด่นของรุ่น / ทำไมต้องเลือก …"** (FEATURES, การ์ด `.fcards`) ออกทุกหน้ารุ่น
- **ลบ section "เหมาะกับพื้นที่ / … เหมาะกับใคร"** (USAGE, รายการ `.usage`) ออกทุกหน้ารุ่น
- **ลบแถบเหลือง `.sample-note`** ("หมายเหตุ: ตัวเลขสเปก…เป็นข้อมูลตัวอย่าง") ออกทุกหน้ารุ่น — ⚠️ ตัวเลขสเปกยังเป็นข้อมูลตัวอย่างอยู่ แค่ไม่แสดงแถบเตือนแล้ว (บันทึกไว้ใน CLAUDE.md + README แล้ว)
- **เพิ่ม section "ติดต่อเรา / พร้อมให้คำปรึกษาทุกพื้นที่"** (featurebar 4 ช่อง: ปรึกษาฟรี · ผ่อน 0% · รับประกัน 8 ปี · ปลอดสารพิษ + การ์ด LINE/Call/Facebook — บล็อกเดียวกับหน้า works/about) แทรกก่อน CTA band ทุกหน้ารุ่น; ลำดับพื้นหลังสลับ tint/paper ยังลงตัว (รุ่นอื่นในแบรนด์ = paper → ติดต่อเรา = tint → CTA = navy)
- **ตรวจผ่าน localhost + Chrome:** model-finnix-ceramic — โครง section เหลือ phero → เฉดฟิล์ม → รุ่นอื่นในแบรนด์ → ติดต่อเรา → CTA, ไม่มี `.sample-note`/`.fcards`/`.usage` ตกค้าง (grep ยืนยันทั้ง 10 ไฟล์), featurebar + การ์ดติดต่อ 3 ใบแสดงผลถูกต้อง
- อัปเดต CLAUDE.md (กติกา sample-note เดิม → หมายเหตุสเปกยังเป็นตัวอย่าง + โครงหน้ารุ่นใหม่) และ README.md (หมายเหตุสเปกตัวอย่าง + วันที่อัปเดต)

## ครั้งที่ 58 — 2026-07-18

**Prompt:**
> ตัด textbox ทักไลน์สอบถามรุ่นนี้ โทร 095-229-2086 ในหน้า Products สนใจติดตั้ง และแถวคอลัมน์ สนใจติดตั้ง เอาออก และย้าย text box ดูรุ่นฟิล์ม ทั้งหมดไปไว้ใต้ คำอธิบาย แทนที่ ทักไลน์สอบถามรุ่นนี้

**สิ่งที่แก้ไข:** (หน้ารุ่นฟิล์มทั้ง 10 หน้า — perl pass เดียว จับลิงก์แบรนด์ของแต่ละไฟล์อัตโนมัติ)
- **ตัดปุ่มใน hero ออกทั้งคู่:** "ทักไลน์สอบถามรุ่นนี้ / ทักไลน์ปรึกษาฟรี" (LINE) และ "โทร 095-229-2086" (tel)
- **ย้ายปุ่ม "ดูรุ่นฟิล์ม <แบรนด์> ทั้งหมด"** จาก section รุ่นอื่นในแบรนด์ ขึ้นมาไว้ใต้คำอธิบาย (ตำแหน่งปุ่มไลน์เดิม) เป็นปุ่มทอง `btn-gold` ปุ่มเดียวใน hero — ลิงก์ไปหน้าแบรนด์ตามเดิม
- **ลบ section "รุ่นอื่นในแบรนด์"** (OTHER MODELS) — เหลือแต่หัวข้อเปล่าหลังย้ายปุ่มออก
- **ลบ section "สนใจติดตั้ง …?"** (CTA band สีกรมท่า พร้อมปุ่มไลน์/โทรซ้ำ) ออกทุกหน้ารุ่น
- เปลี่ยนพื้น section ติดต่อเราบนหน้ารุ่นจาก tint → paper ให้จังหวะสลับสียังลงตัว (phero → สเปก tint → ติดต่อเรา paper → footer กรมท่า)
- **ตรวจผ่าน localhost + Chrome:** model-finnix-titanium — hero มีปุ่มทองปุ่มเดียว คลิกแล้วไปหน้า brand-finnix จริง; model-3m-ceramate — โครง section = phero → สเปก → ติดต่อเรา, ไม่มี ctaband; grep ยืนยันทั้ง 10 ไฟล์ไม่มีปุ่ม/section เดิมตกค้าง
- อัปเดต CLAUDE.md (โครงหน้ารุ่นใหม่ + hero CTA ปุ่มเดียว)

## ครั้งที่ 59 — 2026-07-18

**Prompt:**
> ใน หน้า products จะบอกรุ่นแต่ล่ะรุ่นเใส่รูปหน่อยซึ่งอยู่ในไฟล์ที่ชื่อ source เอาไปใส่ตามรุ่นแต่ล่ะรุ่น

**สิ่งที่แก้ไข:**
- **จับคู่รูปโลโก้รุ่นจาก `assets/source/`** (ไฟล์ต้นฉบับ ไม่ขึ้น git): `CM.png` = Finnix Ceramic · `T.T.png` = Finnix Titanium · `EC.png` = Finnix Extra Clear · `C.png` (Finnix Crystalize) = Finnix UV Guard (จับคู่โดยการคัดออก — รุ่น UV เป็นรุ่นเดียวที่เหลือ) · `3M Ceramaate.png` = 3M Ceramate · `3M Ceramic Ultra Clear.png` = 3M Ultra Clear
- **ย่อรูปเป็นกว้าง 1200px + ตั้งชื่อ ASCII** ด้วย PowerShell System.Drawing แล้วเก็บที่โฟลเดอร์ใหม่ `assets/models/` (ไฟล์ละ ~84–229KB, ต้นฉบับใน source ไม่ถูกแตะ)
- **แสดงโลโก้รุ่นในกรอบรูปใหญ่ของ hero หน้ารุ่น:** เปลี่ยนกล่องไล่สี `.swatch tone-*` (พร้อมตัวเลข VLT ซึ่งซ้ำกับการ์ดสถิติด้านขวา) เป็นการ์ดขาว `.swatch--img` ใส่รูปโลโก้กลางกรอบ — CSS ใหม่ใน product.css, แก้ HTML 6 หน้ารุ่นที่มีรูป (perl pass เดียว)
- **รุ่นที่ยังไม่มีรูปในโฟลเดอร์ source:** Regionfilm Ceramic/Smart และ Ultra Guard Ceramic/Nano (4 รุ่น) — ยังใช้กล่องไล่สีเดิม รอไฟล์รูปจากเจ้าของ
- **ตรวจผ่าน localhost + Chrome:** model-finnix-ceramic และ model-3m-ultra-clear — โลโก้แสดงกลางการ์ดขาวพร้อมกรอบทองเยื้องเดิม โหลดรูปสำเร็จ (naturalWidth 1200)
- อัปเดต CLAUDE.md (โฟลเดอร์ assets/models + ข้อสังเกตชื่อรุ่น Crystalize) และ README.md (file tree)

## ครั้งที่ 60 — 2026-07-18

**Prompt:**
> หมายถึงใส่รูปในหน้า เลือกรุ่นที่ใช่สำหรับคุณ แล้วเอารูปในไฟล์ source ไปแทนข้อความแต่ล่ะรุ่นใน text box นั้นๆ

**สิ่งที่แก้ไข:** (หน้าแบรนด์ — section "เลือกรุ่นที่ใช่สำหรับคุณ")
- **แทนข้อความในการ์ดรุ่นด้วยโลโก้รุ่น:** ถอด series tag / ชื่อรุ่น / กล่องสเปก / คำอธิบาย ออกจากการ์ด `.cmc` แล้วใส่รูปโลโก้จาก `assets/models/` แทน (div ใหม่ `.cmc-img` + CSS ใน product.css) — คงลิงก์ "ดูรายละเอียด →" ไว้ท้ายการ์ด ทั้งเพื่อบอกว่ากดได้และเป็นตัว host ของ stretched-link (กดทั้งการ์ดยังใช้ได้)
- ทำได้ 6 การ์ดที่มีรูป: brand-finnix.html (Ceramic / Titanium / Crystalize=UV Guard / Extra Clear) + brand-3m.html (Ultra Clear / Ceramate) — การ์ดของ Regionfilm และ Ultra Guard ยังเป็นข้อความเดิม รอไฟล์รูป
- รูปโลโก้ในหน้ารุ่น (ครั้งที่ 59) คงไว้ตามเดิม
- **ตรวจผ่าน localhost + Chrome:** brand-finnix — การ์ด 4 ใบแสดงโลโก้ครบ; brand-3m — รูปโหลดครบ 2 ใบ และ `elementFromPoint` กลางรูปชี้ที่ลิงก์ `a.go` (คลิกทั้งการ์ดนำทางได้เหมือนเดิม)

## ครั้งที่ 61 — 2026-07-18

**Prompt:**
> พอใส่รูปลงไปแล้วจัดองค์ประกอบให้ดูสวยด้วยเพราะมันดูธรรมดามากๆตอนนี้

**สิ่งที่แก้ไข:** (products/product.css + เพิ่ม class `cmc--logo` ให้การ์ดโลโก้ 6 ใบ)
- **จัดองค์ประกอบการ์ดรุ่นแบบโลโก้ใหม่ทั้งชุด:**
  - พาแนลรูปเป็นพื้นไล่เฉดงาช้าง (radial ขาว → งาช้างอุ่น) เต็มขอบการ์ดด้านบน สูงขั้นต่ำ 210px
  - **กรอบเส้นทองบาง ๆ ด้านใน** พาแนล (inset 12px) ให้อารมณ์งานพรีเมียม + **เส้นทองเรืองคั่น** ระหว่างพาแนลรูปกับแถบลิงก์
  - แถบ "ดูรายละเอียด" เป็น footer ของการ์ด — ข้อความชิดซ้าย **ลูกศรในวงกลมทองอ่อนชิดขวา**
  - Hover: โลโก้ซูม 1.07 นุ่ม ๆ (easing เดียวกับ nav), วงกลมลูกศรกลายเป็นทองเข้ม+ลูกศรขาว+ขยับขวา, การ์ดยกตัว+แถบทองบนหัวการ์ด (ของเดิม)
  - มือถือ ≤768: พาแนลย่อเหลือ 170px / โลโก้ 110px
- **แต่งกรอบรูปหน้ารุ่น (.swatch--img) ให้เข้าชุด:** พื้นไล่เฉดงาช้างแบบเดียวกัน + กรอบเส้นทองด้านใน (inset 16px) — จากเดิมพื้นขาวเปล่า
- **ตรวจผ่าน localhost + Chrome:** brand-finnix — การ์ด 4 ใบองค์ประกอบใหม่ครบ hover ทำงานตามออกแบบ; model-finnix-ceramic — กรอบรูป hero เข้าชุดกับการ์ด

## ครั้งที่ 62 — 2026-07-18

**Prompt:**
> ขอรูปใหญ่ขึ้นให้เห็นชัดๆหน่อยในหน้าเลือกรุ่นฟิล์ม

**สิ่งที่แก้ไข:** (products/product.css — การ์ด `.cmc--logo` ใน section เลือกรุ่นที่ใช่สำหรับคุณ)
- ขยายพาแนลรูป: ความสูงขั้นต่ำ 210px → **300px**, ลด padding ข้างลงเล็กน้อย (36/30 → 34/26) ให้โลโก้กินพื้นที่กว้างขึ้น
- เพดานขนาดโลโก้: max-height 128px → **220px** — โลโก้ขยายเต็มความกว้างการ์ด อ่านชื่อรุ่นชัดขึ้นมาก
- มือถือ ≤768: พาแนล 240px / โลโก้สูงสุด 180px (เดิม 170/110)
- ตรวจผ่าน localhost + Chrome: brand-finnix — โลโก้ทั้ง 4 ใบใหญ่เต็มการ์ด กรอบทอง/เส้นคั่น/แถบลิงก์ยังจัดวางสวยตามเดิม

## ครั้งที่ 63 — 2026-07-20

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและสร้าง/ปรับปรุง CLAUDE.md สำหรับ Claude Code instance ถัดไป

**สิ่งที่แก้ไข:** (ตรวจสอบ CLAUDE.md เดิมกับสภาพ repo ปัจจุบัน — ข้อมูลหลักยังถูกต้องครบ จึงปรับปรุงเฉพาะจุดแทนการเขียนใหม่)
- section "Running / previewing": เพิ่มวิธีปิด server HttpListener ให้ถูกตัว (socket เป็นของ http.sys / PID 4 — ต้อง kill powershell.exe ที่ CommandLine มีชื่อสคริปต์ ไม่ใช่เจ้าของพอร์ต) + วิธีทดสอบจอมือถือใน Chrome automation (resize_window ใช้ไม่ได้บนเครื่องนี้ ให้ inject iframe กว้าง 430px แทน)
- section "Page hierarchy & path rules": ระบุชื่อ class การ์ดโลโก้รุ่นในหน้าแบรนด์ให้ชัด (`.cmc--logo` — CSS อยู่ใน product.css เท่านั้น) เพื่อให้ grep เจอทันที
- ยืนยันความถูกต้องของคำอธิบายเดิม: บล็อก nav micro-interactions ยังอยู่ท้ายสุดของทั้งสอง stylesheet (index.html:1572, product.css:1482), โครงหน้า/เส้นทางลิงก์/รายชื่อไฟล์ assets/models ตรงกับของจริงทั้งหมด

## ครั้งที่ 64 — 2026-07-20

**Prompt:**
> ในหน้า products แบรนด์ regionfilm ลบ 2 รุ่นนั้นออกแล้วเอาหน้าโชว์สเปคนั้นขึ้นมาเลยถ้าเรากดไปที่ regionfilm และในหน้าโชว์สเปครุ่นแต่ล่ะรุ่น ใส่ ผลงาน ในหน้า project reference เพิ่มตรงข้างบนคอมลัมน์แถวพร้อมให้คำทุกพื้นที่

**สิ่งที่แก้ไข:**
- **Regionfilm เหลือหน้าเดียว — กดแล้วเจอสเปคเลย:** ลบ `model-regionfilm-ceramic.html` + `model-regionfilm-smart.html` ออกจากเว็บ แล้วแปลง `brand-regionfilm.html` เป็นหน้าโชว์สเปคเต็มรูปแบบ (hero + swatch โทนใส 35% + ตัวเลข 35%/75%/99% + ตารางสเปครวมสองโทน ใส/เข้ม, ประกันสูงสุด 5 ปี) — ลิงก์เดิมทุกจุด (dropdown/การ์ดแบรนด์/footer) ชี้มาหน้านี้อยู่แล้ว ไม่ต้องแก้
- **เพิ่ม section "ผลงานการติดตั้ง" เหนือแถว "พร้อมให้คำปรึกษาทุกพื้นที่" ใน 9 หน้า:** หน้ารุ่นทั้ง 8 (Finnix ×4, 3M ×2, Ultra Guard ×2) + หน้า Regionfilm ใหม่ — การ์ดผลงาน 3 ใบยกมาจากหน้า Project Reference (บ้านเดี่ยว/คอนโด/สำนักงาน พร้อมรูป) + ปุ่ม "ดูผลงานทั้งหมด" ลิงก์ไป `works.html`
- ก๊อปสไตล์ `.works` / `.workcard` / `.more` จาก works.html ลง `product.css` (บล็อกใหม่ก่อน nav micro-interactions) + เพิ่ม JS โหลดรูปการ์ดแบบ lazy (`[data-img]`) ในหน้ารุ่นทุกหน้า
- เปลี่ยนพื้น section ติดต่อในหน้ารุ่นจาก paper เป็น `.section--tint` เพื่อให้สลับสีกับ section ผลงานที่แทรกใหม่ (เหมือนคู่ works→contact ในหน้า Project Reference)
- อัปเดต README.md (ตัด 2 ไฟล์ที่ลบ + ระบุ Regionfilm เป็นหน้าสเปคเดี่ยว) และ CLAUDE.md (ผังหน้า + โครงหน้ารุ่นใหม่ + ขอบเขต lazy images)
- ตรวจผ่าน localhost + Chrome: brand-regionfilm — hero/สเปค/ผลงาน/ติดต่อครบ; model-finnix-ceramic — การ์ดผลงาน 3 ใบพร้อมรูปอยู่เหนือ "พร้อมให้คำปรึกษาทุกพื้นที่" ถูกตำแหน่ง

## ครั้งที่ 65 — 2026-07-20

**Prompt:**
> ในหน้า home ตรงแถว welcome to world film ปรับตรง text box line ปรับให้เป็นพื้นหลังใสสีเขียวอ่อนเมื่อเอาเคอร์เซอร์ไปชี้ที่ line จะเข้มขึ้น ส่วน facebook เหมือนกันกับ line แต่ปรับให้เป็นตามโลโก้ ส่วน icon เอารูปในไฟล์ assets เลย ส่วนแถวคอลัมน์พร้อมให้คำปรึกษาทุกพื้นที่ ปรับให้ icon มันตรงกับคำพูด ตรงหน้า และคอลัมน์นี้มีคำว่าเลื่อนลงตรงมุมขวาเอาออกเลย ส่วนหน้า Project reference ในแต่ล่ะผลงานเพิ่มลูกเล่นสามารถสามารถดูรูปในผลงานนั้นได้ และเลื่อนดูรูปได้เอา 5 และมีจุดใต้รูป ทำทุกผลงาน

**สิ่งที่แก้ไข:**
- **การ์ด LINE/Facebook ในแถว "Welcome to WORLD FILM" (index.html hero):** เปลี่ยนไอคอนจาก SVG เป็นรูปโลโก้จริง (`assets/line.png`, ย้าย `assets/communication.png` → `assets/facebook.png` ให้ชื่อสื่อความหมาย) การ์ด LINE พื้นเขียวอ่อนโปร่งใส เข้มขึ้นเมื่อชี้; การ์ด Facebook พื้นฟ้าอ่อนโปร่งใสตามสีโลโก้ เข้มขึ้นเมื่อชี้เช่นกัน (การ์ด Call ไม่แตะต้อง)
- **แก้ icon section "พร้อมให้คำปรึกษาทุกพื้นที่" ให้ตรงกับคำพูด:** หน้าแรก — สลับไอคอนที่สลับกันอยู่ ("วัดหน้างานฟรี" เคยได้ไอคอน % ที่จริงเป็นของ "ผ่อน 0%" → เปลี่ยนเป็นไอคอนไม้บรรทัดวัด, "ผ่อน 0%" เคยได้ไอคอนโล่+อัศเจรีย์ → เปลี่ยนเป็นไอคอน %); อีก 15 หน้า (works/about/contact/brand-*/model-*) — ไอคอน "ปลอดสารพิษ" เดิมเป็นโล่+อัศเจรีย์ (สื่อถึงคำเตือน ขัดกับความหมาย "ปลอดภัย") เปลี่ยนเป็นไอคอนใบไม้
- **เอาคำว่า "เลื่อนลง" (scrollcue) มุมขวาล่างของ hero หน้าแรกออก** ทั้ง HTML และ CSS ที่เกี่ยวข้อง
- **หน้า Project Reference (works.html) — เพิ่ม gallery ดูรูปผลงานในการ์ด ทำครบทั้ง 6 ผลงาน:** แต่ละการ์ดมีรูป 5 รูป เลื่อนดูได้ด้วยปุ่มลูกศรซ้าย/ขวา (โผล่ตอนชี้เมาส์ที่รูป) พร้อมจุดไข่ปลา 5 จุดใต้รูป (กดจุดเพื่อข้ามไปรูปนั้นได้ตรง ๆ) แต่ละการ์ดทำงานอิสระจากกัน — CSS/JS อยู่ในไฟล์ works.html เท่านั้น (ไม่ใช่ product.css เพราะ scope เฉพาะหน้านี้)
- ตรวจผ่าน localhost + Chrome: hero LINE เขียว/Facebook ฟ้า เข้มขึ้นเมื่อชี้; featurebar หน้าแรกไอคอนตรงคำพูดครบ 4 อัน ไม่มี "เลื่อนลง" หลงเหลือ; works.html การ์ดทั้ง 6 มีจุดใต้รูปครบ 5 จุด ปุ่มลูกศร/จุด สลับรูปถูกต้อง (ทดสอบ wrap-around ทั้งสองทิศแล้ว)
- อัปเดต CLAUDE.md: บันทึกไอคอน assets ใหม่ (line.png/facebook.png), ข้อยกเว้นสไตล์การ์ด LINE/Facebook เฉพาะ hero, กติกาจับคู่ icon-คำพูดของ featurebar (หน้าแรกกับอีก 15 หน้าใช้ label ต่างชุดกัน), และ gallery ใหม่ของ works.html

## ครั้งที่ 66 — 2026-07-20

**Prompt:**
> ในหน้า project reference ในแต่ล่ะผลงานสามารถกดดูรูปแล้วรูปเด้งขยายภาพขึ้นมาให้ดูได้ และในหน้า Products ดูสเปก text box ที่สามารถใส่รูปได้นั้นเอา logo ใน text box นั้นออกแล้วเพิ่มระบบสามารถ zoom ดูรูปภาพได้และสามารถกดแล้วรูปเด้งขยาย ขึ้นมาเหมือนหน้าดูสเปก แล้ว หา icon line มาใส่แทนอันเก่าตรงแถวคอลัมน์ พร้อมให้คำปรึกษาทุกพื้นที่ ทุกหน้าความถึงหน้า contact คอลัมน์ get in touch และพอเปลี่ยนแล้วปรับ สี icon ให้เข้ากับธีมด้วย ยกเว้นหน้า home ตรงแถวคอลัมน์ welcome to world film ที่มีช่องทางการติดต่อตรงนั้นห้ามเปลี่ยน

**สิ่งที่แก้ไข:**
- **หน้า Project Reference (works.html) — เพิ่ม lightbox กดรูปแล้วขยายเต็มจอ:** กดที่รูปในการ์ดผลงาน (นอกปุ่มลูกศร) แล้วรูปเด้งขึ้นมาขยายเต็มจอแบบ overlay มืด มีปุ่มปิด/ลูกศรซ้าย-ขวาเลื่อนดูรูปต่อได้ (รองรับคีย์ Esc ปิด, ลูกศรซ้าย/ขวาคีย์บอร์ด, กดพื้นหลังเพื่อปิด) เมื่อเลื่อนดูใน lightbox แล้วปิด การ์ดจะจำตำแหน่งรูป/จุดล่าสุดที่ดูไว้ ทำครบทั้ง 6 ผลงาน
- **หน้าดูสเปกฟิล์ม (model-finnix-ceramic/titanium/uvguard/extra-clear, model-3m-ultra-clear/ceramate) — เอาโลโก้รุ่นออกจากกล่องรูป:** ตามที่ยืนยัน กลับไปใช้กล่องพื้นไล่เฉด + ตัวเลข VLT% แบบเดียวกับ Regionfilm/Ultra Guard (ลบ `.swatch--img` + `<img>` โลโก้ 6 หน้า, ลบ CSS `.swatch--img` ที่ไม่ได้ใช้แล้วออกจาก product.css) — โลโก้ในไฟล์ assets/models/ ยังใช้อยู่ในการ์ดเลือกรุ่นหน้าแบรนด์ Finnix/3M เหมือนเดิม ไม่ได้ลบไฟล์รูป
- **เปลี่ยนไอคอน LINE เป็นโลโก้จริงทุกหน้า (ยกเว้นแถว hero หน้าแรกตามที่สั่งห้าม):** แทนที่ไอคอน SVG เดิมด้วยรูป `assets/line.png` ใน (1) การ์ด `.ccard` คอลัมน์ "พร้อมให้คำปรึกษาทุกพื้นที่" ของ works/about/brand-*/model-* ทุกหน้า (14 หน้า) และ (2) คอลัมน์ "Get In Touch" ของหน้า contact.html — ปรับพื้นวงกลมไอคอนให้โปร่งใส (ตัดพื้นกรมท่า/กระจกเดิมออกเฉพาะไอคอน LINE) ให้สีเขียวแบรนด์ขึ้นเข้ากับธีม ไม่แตะไอคอน Facebook/โทร และไม่แตะการ์ด LINE/Facebook แถว "Welcome to WORLD FILM" หน้าแรกตามที่สั่ง
- อัปเดต CLAUDE.md (แก้คำอธิบาย swatch--img ที่ถูกยกเลิก, เพิ่มหัวข้อ lightbox, เพิ่มหัวข้อไอคอน LINE ใหม่) และ README.md (แก้บรรทัด assets/models/ + เพิ่ม line.png/facebook.png ในผังไฟล์)
- ตรวจผ่าน localhost + Chrome: works.html คลิกรูปเปิด lightbox ได้ ปุ่ม next/close ทำงานถูกต้อง การ์ดซิงก์ตำแหน่งหลังปิด; model-finnix-ceramic กล่องรูปกลับเป็นพื้นไล่เฉด 35%; ไอคอน LINE ในทุกจุดที่แก้ขึ้นเป็นโลโก้เขียวจริงถูกต้อง (ทดสอบ works.html, contact.html Get In Touch)

## ครั้งที่ 67 — 2026-07-20

**Prompt:**
> คิดว่าจะทำยังไงให้คำว่าแบรนด์ เช่น 3M finnix regionfilm Ultra Guard มันดูเด่นในหน้า products เพื่อเป็นการบอกว่าแบรนด์ นี้ที่บอกมีรุ่นอะไรบ้างส่วนรุ่นไม่ต้องทำอะไรเอาแบบเดิมปกติ ส่วนในหน้า contact แถวคอลัมน์ บริการครบ มั่นใจได้ทุกขั้นตอนที่มี 4 ขั้นตอนนั้น แก้ไขข้อความให้ตรงกับ 4 ขั้นตอนที่อยู่ในหน้า HOMR คอลัมน์พร้อมให้คำปรึกษาทุกพื้นที่ แล้วถึงปรับ icon ให้เหมาะกับพูดที่เขียน

**สิ่งที่แก้ไข:**
- **หน้า Contact — แก้ section "บริการครบ มั่นใจได้ทุกขั้นตอน" ให้ตรงกับหน้า Home:** เปลี่ยนข้อความ+ไอคอนให้ตรงกับ 4 ขั้นตอนในคอลัมน์ "พร้อมให้คำปรึกษาทุกพื้นที่" ของหน้าแรกทุกตัวอักษร — "ผ่อน 0%" (ไอคอน %) → **"วัดหน้างานฟรี"** (ไอคอนไม้บรรทัดวัด), "รับประกัน 8 ปี" → **"รับประกัน 10 ปี"** (ไอคอนโล่+ถูกเดิม), "ปลอดสารพิษ" (ไอคอนใบไม้) → **"ผ่อน 0%"** (ไอคอน %) ส่วน "ปรึกษาฟรี" ตรงกันอยู่แล้วไม่ต้องแก้
- **ข้อเสนอเรื่องทำให้ชื่อแบรนด์เด่นในหน้า Products** — ยังไม่ได้ลงมือทำ รอฟังทิศทางจากเจ้าของร้านก่อน (ดูข้อเสนอในแชท)
- อัปเดต CLAUDE.md: บันทึกว่า contact.html section นี้ (class `.vprop`/`.m` คนละชุดกับ `.fitem`) ต้องซิงก์กับ featurebar หน้าแรกแทนอีก 14 หน้าที่เหลือ
- ตรวจผ่าน localhost + Chrome: หน้า contact แถบ 4 ไอคอนตรงกับหน้าแรกทุกตัวอักษรและไอคอนแล้ว

## ครั้งที่ 68 — 2026-07-20

**Prompt:**
> เอาตัวเลือกแรกทำมาเลย
> (ต่อจากข้อเสนอ: เพิ่ม mini-hero ใต้ breadcrumb ให้ชื่อแบรนด์เป็น h1 ตัวใหญ่แบบเดียวกับหน้าดูสเปกรุ่น)

**สิ่งที่แก้ไข:**
- **เพิ่ม section "แบรนด์เด่น" (.bhero) ใต้ breadcrumb ใน 3 หน้าแบรนด์แบบเลือกรุ่น** — `brand-finnix.html`, `brand-3m.html`, `brand-ultraguard.html`: ชื่อแบรนด์ตัวใหญ่ (h1 ขนาดเดียวกับหน้าดูสเปกรุ่น) + eyebrow "ฟิล์มกรองแสงจาก" + คำโปรยสั้นบอกจุดเด่นแบรนด์ (ดึงจาก meta description เดิมของแต่ละหน้า) ก่อนเข้า section "เลือกรุ่นที่ใช่สำหรับคุณ" — ส่วนการ์ดเลือกรุ่นด้านล่างไม่แตะเลยตามที่สั่ง
- **ไม่แตะ brand-regionfilm.html** — หน้านี้มี `<h1>ฟิล์ม Regionfilm</h1>` ใหญ่อยู่แล้วใน `.phero` (จากตอนรวมหน้าสเปก 2 ครั้งก่อน) เพิ่ม `.bhero` ซ้ำจะกลายเป็นสอง hero ซ้อนกัน
- เพิ่ม CSS `.bhero` ใน product.css (ใช้ร่วมกับทั้ง 3 หน้า, มี responsive ≤768px)
- อัปเดต CLAUDE.md บันทึกส่วนนี้ + เหตุผลที่ Regionfilm ไม่ได้แตะ
- ตรวจผ่าน localhost + Chrome: ทั้ง 3 หน้า (Finnix/3M/Ultra Guard) ชื่อแบรนด์เด่นชัดเจน + ตรวจจอมือถือ 390px ผ่าน (ตัวอักษรย่อสัดส่วนดี ไม่ล้น)

## ครั้งที่ 69 — 2026-07-21

**Prompt:**
> งั้นแก้ไขตรงปุ่มมือถือไม่ให้ทับการ์ดก่อนและถ้าเปิดเว็บไซน์ในมือถือย่อขนาดเว็บไซต์ให้เห็นองค์ประกอบโดยรวมในเว็บ เช่น ตัวเลือกแบรนด์ที่ปกติเปิดในเว็บบน pc จะเรียง 4 แบรนด์ต่อแถว แล้วถ้าเปิดบนมือถือมันจะมี 2 แถวแถวล่ะ 2 แบรนด์งั้นจัดการให้มันอยู่ในแถวเดียว และดูองค์ประกอบอื่นๆ และหน้าอื่นๆ ด้วยว่ามันใช้งานง่ายไหมเพราะฉันยังมองว่ามันเต็มหน้าจอเกิ้นไป
> (ต่อจากการวิเคราะห์เว็บไซต์ในเทิร์นก่อนหน้า — พบว่าปุ่มลอย LINE/โทรทับการ์ดบนมือถือ)

**สิ่งที่แก้ไข:**
- **ย่อปุ่มลอย LINE/โทร (.fab) บนมือถือ** — เพิ่ม override ใน `@media (max-width: 768px)` ทั้ง `index.html` และ `product.css`: ปุ่มจาก 56px → 46px, ระยะขอบจอ 22px → 14px, ไอคอนย่อตาม (ยังเกิน 44px touch target ตามมาตรฐานเดิม)
- **แก้สาเหตุจริงที่การ์ดสเปก 3 ช่อง (.phl .hi) ในหน้ารุ่นสินค้าทับปุ่มลอย** — การ์ดที่ 3 (ตัวเดียวในแถวที่ 2) เดิมยืดเต็มความกว้างเพราะ `flex:1` ทำให้ขอบขวาไปชนปุ่มลอยพอดี แก้เป็น `flex-grow:0` + `max-width` เท่ากับการ์ดคู่ ทำให้การ์ดเดี่ยวอยู่ชิดซ้ายไม่ทับปุ่มอีกต่อไป
- **ตัวเลือกแบรนด์หน้าแรก (.brandlogos) อยู่แถวเดียว 4 แบรนด์บนมือถือ** — เพิ่ม override ใน `@media (max-width: 480px)` ของ `index.html`: กลับเป็น `repeat(4, 1fr)` (เดิม 2 คอลัมน์ 2 แถวจาก breakpoint 980px) พร้อมย่อ padding/ความสูงการ์ด และย่อโลโก้เหลือสูงสุด 22px ให้พอดีแถวเดียว
- **พบและแก้บั๊ก CSS เดิมที่ทำให้การ์ด "3 เฉดของ Finnix Ceramic" (.shadegrid) ไม่เคยยุบเป็นคอลัมน์เดียวบนจอเล็ก** — มี rule `.shadegrid { grid-template-columns: 1fr }` อยู่ใน `@media (max-width: 980px)` ตั้งแต่บรรทัดต้นไฟล์ แต่ถูก rule พื้นฐาน `repeat(3, 1fr)` ที่อยู่ *หลัง* ในไฟล์ทับอีกที (ลำดับ CSS ชนะ) ทำให้บนมือถือการ์ดทั้ง 3 ใบบีบเหลือคอลัมน์ละ ~90px อ่านยากและก็ไปทับปุ่มลอยด้วย — ลบ rule ที่ตายแล้วออกจากบล็อก 980px เดิม แล้วเพิ่ม rule เดียวกันใหม่ไว้หลังนิยามหลักของ `.shadegrid` เพื่อให้ลำดับ CSS ถูกต้อง ตอนนี้บนมือถือการ์ดยุบเป็นคอลัมน์เดียวเต็มความกว้าง อ่านง่ายขึ้นมาก และปุ่มลอยไม่ทับข้อความอีก
- ตรวจผ่าน localhost + Chrome จำลองจอ 390px: หน้าแรก (แบรนด์ 4 ช่องแถวเดียว + featurebar + ปุ่มลอย), หน้ารุ่น Finnix Ceramic (การ์ดสเปก 3 ช่อง + การ์ดเฉดสี 3 แบบ), works.html (แกลเลอรีการ์ด), about.html (สถิติ + จุดเด่น) — ไม่พบปุ่มลอยทับเนื้อหาอ่านไม่ออกอีก
- ยังคงพบตัวเลข "ปี รับประกัน" ไม่ตรงกัน (8 ปี ในหน้าแรก/works/about vs 10 ปี ใน featurebar) ตามที่รายงานไว้ในเทิร์นก่อน — ยังไม่ได้แก้ในเทิร์นนี้ (นอกขอบเขตคำสั่งครั้งนี้ซึ่งเน้นเรื่องปุ่มมือถือ/การจัดวาง)

## ครั้งที่ 70 — 2026-07-21

**Prompt:**
> ในรูปแบบมือถือตรงหมวดหมู่มือถือ products นั้นที่มีให้เลือก 4 แบรนด์ ใส่ลูกเล่นซ้อนไว้ใน products พอกดที่ products แล้วค่อยขึ้นแบรด์ 4 แบรนด์มาให้เลือก และในหน้า home ตรงแถวคอลัมน์ ฟิล์มกรองแสงที่ให้มากกว่าการลดแสงที่ มี 4 คุณ สมบัติย่อตัว text box ให้เป็น 1 แถวมี 2 อันเอาแค่ 2 แถว และใน แถวคอลัมน์ ยินดีต้อนรับสู่ world Film ทั้งภาพและคำอธิบายทำให้อยู่ในแถวเดี๋ยวกัน แถวคอลัมน์พร้อมให้คำปรึกษาเหมือนกันทำให้อยู่ในแถวเดี๋ยวกัน รวมถึงแถวคอลัมน์ world film ศุนย์บริการติดตั้งฟิล์มกรองแสง บ้าน อาคาร สำนักงานคอนโดมากว่า 40 สาขาทั่วประเทศ จัดให้มันอยู่ในแถวเดียวกันเหมือนกับเว็บไซต์ในหน้า PC แล้วในหมวดหมู่อื่น ทั้ง Pro droducts แบรนด์ 4 แบรนด์ รวมทุกรุ่น และหมวดหมู่ผลงาน หมวดหมู่เกี่ยวกับเรา และติดต่อ ถ้าอันไหนสามารถทำให้อยู่ในแถวเดียวกันคอลัมน์เดียวกัน แก้ไขเลย ที่เขียน prompt ไปทั้งหมดนี้คือการแก้ไขรูปแบบเว็บไซต์บนมือถือ

**สิ่งที่แก้ไข:**
- **เมนู "Products" บนมือถือ เป็น accordion** — เดิมกางรายชื่อ 4 แบรนด์ให้เห็นตลอดใต้ "Products" ในเมนู, ตอนนี้หุบไว้ก่อน (มีลูกศร chevron บอกสถานะ) แตะ "Products" หนึ่งครั้งเพื่อกาง/หุบ แทนที่จะลิงก์ออกทันที — ใช้งานได้ทุกหน้า (16 ไฟล์) แม้หน้าอื่นที่ "Products" ปกติจะลิงก์ไปหน้าแรกก็ตาม เพราะ intercept การคลิกด้วย JS เมื่ออยู่ในโหมดมือถือ (เช็คจาก burger icon มองเห็นอยู่) ส่วน desktop hover-dropdown เดิมไม่กระทบ — แก้ CSS ใน `index.html` และ `product.css` (breakpoint ≤768px) + เพิ่ม JS logic เดียวกันในทั้ง 16 หน้า HTML
- **การ์ด 4 คุณสมบัติหน้าแรก "ฟิล์มกรองแสงที่ให้มากกว่าการลดแสง" (.benefits) เป็น 2 ต่อแถว 2 แถวบนมือถือ** — ของเดิมมี override ที่ ≤768px บังคับเป็นคอลัมน์เดียวทับ rule ที่ถูกต้องจาก ≤980px อยู่ — ลบ override ที่ผิดออก
- **แถบ "พร้อมให้คำปรึกษาทุกพื้นที่" (.featurebar) คงเป็น 2 คอลัมน์ตลอดจนจอมือถือเล็กสุด** — ของเดิมมี override ที่ ≤480px บังคับกลับเป็นคอลัมน์เดียว ทั้งที่ ≤768px จัดเป็น 2 คอลัมน์ไว้ดีอยู่แล้ว — ลบออก (แก้ทั้ง index.html และ product.css จึงมีผลทุกหน้า)
- **Footer คงเป็น 2 คอลัมน์ตลอดจนจอมือถือเล็กสุด** — บั๊กแบบเดียวกับ featurebar (override ที่ ≤480px ทับ rule ≤980px ที่ถูกต้อง) — ลบออก (ทั้ง index.html และ product.css)
- **หมวด "ยินดีต้อนรับสู่ WORLD FILM" — รูปเล็ก+หัวข้ออยู่แถวเดียวกัน คำบรรยายเต็มความกว้างด้านล่าง** (ตามตัวเลือกที่ผู้ใช้เลือกจาก 3 แบบที่เสนอ) — ปรับ `.intro-grid` เป็น `display:block` แล้วให้รูป (`.figure`) ลอย (`float:left`) ขนาด 96px ชิดซ้าย, หัวข้อ (h3) ไหลเลียบข้างรูปตามธรรมชาติของ float, คำบรรยาย (p) ใช้ `clear:left` บังคับขึ้นบรรทัดใหม่เต็มความกว้างเสมอ, ย้ายปุ่มลูกศร prev/next จาก absolute ขนาบข้างข้อความ มาเป็นแถวกึ่งกลางด้านล่างแทน (ของเดิมจะไปชนรูปที่ลอยอยู่), ซ่อน badge "ปีรับประกัน" กับกรอบทองตกแต่งที่ขนาดนี้ (เล็กเกินจะแสดงสวยงาม) — ระหว่างทำเจอปัญหา: eyebrow "ยินดีต้อนรับสู่ WORLD FILM" เป็นกล่อง `inline-flex` กว้าง 231px ซึ่งกว้างเกินพื้นที่ว่างข้างรูป (~225px) ทำให้เนื้อหาทั้งหมดหล่นไปอยู่ใต้รูปแทนที่จะไหลเลียบข้างๆ — แก้โดยซ่อน eyebrow เฉพาะจุดนี้ (ไม่กระทบ eyebrow ที่ section อื่น)
- **ปุ่มลอย LINE/โทร และแถวแบรนด์ 4 ช่อง** — ทำไปแล้วในเทิร์นก่อนหน้า (69) ยังคงอยู่ ไม่กระทบจากการแก้ไขรอบนี้
- ระหว่างทำ ระบบเข้า Plan Mode กลางคัน (หลังโค้ดส่วนใหญ่แก้ไปแล้ว) จึงสรุปสถานะเป็นแผนให้ผู้ใช้อนุมัติก่อนตรวจสอบต่อ — ผู้ใช้อนุมัติแล้วดำเนินการต่อจนจบ
- ตรวจผ่าน localhost + Chrome จำลองจอ 390px ครบทุกจุด: accordion Products (ทั้งหน้าแรกและหน้ารุ่นสินค้า — ยืนยันไม่ลิงก์ออกตอนกด), .benefits 2×2, featurebar 2 คอลัมน์, footer 2 คอลัมน์, หมวดยินดีต้อนรับ (รูป+หัวข้อแถวเดียวกัน + สลับรูปการ์รูเซลยังทำงานปกติ + ปุ่มลูกศรกดเปลี่ยนสไลด์ได้)

## ครั้งที่ 71 — 2026-07-22

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและปรับปรุงไฟล์ CLAUDE.md สำหรับ Claude Code instance ถัดไป

**สิ่งที่แก้ไข:**
- อัปเดต `CLAUDE.md` ให้ครอบคลุมการเปลี่ยนแปลง 2 commit ล่าสุด (ครั้งที่ 69–70) ที่ยังไม่ได้บันทึก:
  - เพิ่มคำอธิบาย **accordion เมนู Products บนมือถือ** (JS intercept คลิก + `.has-sub.open` + `max-height: 0→260px` + chevron ลูกศรหมุน อยู่ใน ≤768 ของทั้ง 2 stylesheet ทั้ง 16 หน้า) ในหัวข้อ capsule header, Per-page JS และหมายเหตุว่า anchor `#brands` กดไม่ถึงบนมือถือ
  - เพิ่มแนวทาง **มือถือคงแถว/คอลัมน์ใกล้ PC** ในหัวข้อ Responsive: `.benefits` 2×2, featurebar + footer คง 2 คอลัมน์ (override 1 คอลัมน์เดิมถูกลบแล้ว — ห้ามใส่กลับ), `.brandlogos` แถวเดียว 4 ช่องที่ ≤480px, `.fab` ย่อเป็น 46px + บทเรียนบั๊ก `.shadegrid` (rule ที่มาทีหลังทับ media query เดิม)
  - เพิ่ม layout มือถือของ section ยินดีต้อนรับ (รูป float 96px + ซ่อน eyebrow/seal + ปุ่มลูกศรย้ายลงล่าง) ในหัวข้อ Homepage-only sliders

## ครั้งที่ 72 — 2026-07-22

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและปรับปรุงไฟล์ CLAUDE.md สำหรับ Claude Code instance ถัดไป

**สิ่งที่แก้ไข:**
- ตรวจทาน CLAUDE.md เทียบกับโค้ดจริงทั้งหมด (โค้ดไม่มีการเปลี่ยนแปลงตั้งแต่ /init ครั้งก่อน) — ยืนยันว่าข้อมูลเดิมถูกต้องครบ: accordion Products อยู่ครบ 16 หน้า, `.bhero` + responsive ใน product.css, smart-hide `.nav-hidden` ในทั้ง 2 stylesheet, โครงสร้าง README/assets ตรงตามที่บันทึก
- เพิ่ม 2 จุดที่ยังไม่ได้บันทึกใน CLAUDE.md:
  - **บันทึกปัญหาค้าง "รับประกัน 8 ปี vs 10 ปี"** — index.html/contact.html ใช้ 10 ปี ส่วนอีก 14 หน้า (รวมข้อความใน about/works) ใช้ 8 ปี; รายงานผู้ใช้ไปแล้วตั้งแต่ครั้งที่ 69 และตั้งใจคงไว้รอเลขจริงจากเจ้าของร้าน — กำชับ instance ถัดไปไม่ให้แก้เองหรือรายงานซ้ำ
  - **ระบุว่า `assets/brands/` (โลโก้ 4 แบรนด์) ใช้เฉพาะ index.html เท่านั้น** — หน้าแบรนด์แสดงชื่อเป็นตัวอักษร ไม่ใช้โลโก้

## ครั้งที่ 73 — 2026-07-22

**Prompt:**
> เว็บไซต์ในหน้า Products ในหน้าแบรนด์ต่างล่ะแบรนด์ที่โชว์ logo ใส่แถบให้ตัว font แบรนด์แต่ล่ะแบรนด์ มันเด่นชัด และเข้ากับธีม

**สิ่งที่แก้ไข:**
- **เพิ่มแถบชื่อแบรนด์ (`.bband`) ในหน้าแบรนด์ทั้ง 4 หน้า** — ครอบชื่อแบรนด์ตัวใหญ่ด้วยแถบพาแนลสีกรมท่าไล่เฉด + กรอบเส้นทองด้านใน (ภาษาดีไซน์เดียวกับการ์ดโลโก้รุ่น `.cmc--logo` แต่กลับสี) และตัวอักษรชื่อแบรนด์เป็นสีทองไล่เฉด (gradient text) ให้เด่นชัดและเข้าธีมกรมท่า-ทองของเว็บ
  - `brand-finnix.html` / `brand-3m.html` / `brand-ultraguard.html` — แถบอยู่กึ่งกลางใน section `.bhero` ใต้ eyebrow "ฟิล์มกรองแสงจาก"
  - `brand-regionfilm.html` — ใช้ variant `.bband--phead` (ชิดซ้าย ขนาดกะทัดรัดกว่า) ครอบ `<h1>ฟิล์ม Regionfilm</h1>` ใน hero เดิมของหน้า
  - หน้ารุ่น (`model-*.html`) ไม่ใส่แถบ — เน้นเฉพาะชื่อแบรนด์ตามคำสั่ง
- CSS `.bband` เพิ่มใน `product.css` ที่เดียว (หน้าแบรนด์ไม่ได้ใช้ stylesheet ของ index.html) พร้อม responsive ≤768px (ย่อ padding/มุมโค้ง)
- ระหว่างทำพบบั๊กแรก: `.bband` แบบ `inline-block` ไปต่อแถวเดียวกับ eyebrow (ซึ่งเป็น `inline-flex`) ทำให้ eyebrow ไปอยู่ข้างซ้ายแถบ — แก้เป็น `display:block; width:fit-content` แถบจึงขึ้นบรรทัดใหม่และกึ่งกลางถูกต้อง
- ระหว่างทำ ระบบเข้า Plan Mode กลางคัน (หลังแก้โค้ดเสร็จแล้ว) — สรุปสถานะและออกจาก Plan Mode แล้วตรวจสอบต่อจนจบ
- อัปเดต CLAUDE.md: บันทึก `.bband` / `.bband--phead` + เหตุผลที่ต้องเป็น `display:block` ไม่ใช่ `inline-block`
- ตรวจผ่าน localhost + Chrome ครบทั้ง 4 หน้าแบรนด์บนเดสก์ท็อป + จำลองจอมือถือ 430px (Finnix และ Regionfilm): แถบกึ่งกลาง/ชิดซ้ายถูกต้อง ชื่อทองบนพื้นกรมท่าอ่านชัด ไม่ล้นจอ

## ครั้งที่ 74 — 2026-07-22

**Prompt:**
> ขอดีไซน์ที่มันเรียบกว่านี้หรือมี idea ที่ดีกว่านี้เสนอมา

**สิ่งที่แก้ไข:**
- **ทำหน้าเดโมเปรียบเทียบ 4 ตัวเลือกดีไซน์ชื่อแบรนด์** (ไฟล์ชั่วคราว `_bband-options.html` เปิดผ่าน localhost ให้เลือก แล้วลบทิ้งหลังจบงาน): (1) ชื่อกรมท่า+เส้นทองสั้นใต้ชื่อ (2) ชื่อสีทองล้วนไม่มีแถบ (3) แถบงาช้างขอบทองบาง (4) ใช้โลโก้จริงของแบรนด์จาก assets/brands/ — ผู้ใช้เลือก **แบบที่ 1**
- **เปลี่ยนดีไซน์ `.bband` จากแถบพาแนลกรมท่า → แบบมินิมอล:** ชื่อแบรนด์สีกรมท่าปกติ + เส้นทองไล่เฉดสั้น 68×3px ใต้ชื่อ (`::after`) — ถอดกล่องพื้นกรมท่า กรอบทอง เงา และ gradient text ของเวอร์ชันแรก (ครั้งที่ 73) ออกทั้งหมด — แก้เฉพาะ CSS ใน `product.css` ที่เดียว มาร์กอัป `<div class="bband">` เดิมทั้ง 4 หน้าใช้ต่อได้ไม่ต้องแตะ
- variant `.bband--phead` (หน้า Regionfilm) เส้นทองชิดซ้ายตามแนว hero เดิม ส่วนอีก 3 หน้าแบรนด์เส้นอยู่กึ่งกลางใต้ชื่อ
- อัปเดต CLAUDE.md: บันทึกดีไซน์ใหม่ + ประวัติว่าเวอร์ชันแถบกรมท่าถูกแทนที่ในวันเดียวกันตามคำขอ "เรียบกว่า" — ห้ามนำกลับมาเองโดยไม่ถูกขอ
- ตรวจผ่าน localhost + Chrome: brand-finnix (กึ่งกลาง) + brand-regionfilm (ชิดซ้าย) บนเดสก์ท็อป และจำลองจอมือถือ 430px — เส้นทองย่อระยะพอดี ไม่ล้น

## ครั้งที่ 75 — 2026-07-22

**Prompt:**
> ในหน้า รุ่นแต่ล่ะรุ่น แถวคอลัมน์ผลงานติดตั้งจริงจาก world film ตรง text box ทำให้เหมือนกับ project reference เอาแค่ลูกเล่นตรงtext box

**สิ่งที่แก้ไข:**
- ตรวจเทียบก่อนทำ: กล่องข้อความของการ์ดผลงาน (ชื่องาน/หมุดที่ตั้ง/ฟิล์มที่ใช้/ดูผลงาน→) สองหน้า **เหมือนกันอยู่แล้ว** — สิ่งที่หน้า Project Reference มีเพิ่มคือแถวจุดสลับรูป 5 จุดใต้ภาพ (เหนือกล่องข้อความ) — ถามยืนยันแล้วผู้ใช้เลือก "เอาเฉพาะจุดสไลด์รูป 5 จุด" (ไม่เอาปุ่มลูกศรบนรูป และไม่เอา lightbox กดดูรูปเต็มจอ)
- **เพิ่มมินิแกลเลอรีจุดสลับรูปให้การ์ดผลงานทั้ง 3 ใบ ในหน้าสเปกทั้ง 9 หน้า** (8 หน้ารุ่น + brand-regionfilm): เปลี่ยน `.thumb` รูปเดียวเป็น 5 เลเยอร์ `.gslide[data-img]` cross-fade + แถว `.gdots` 5 จุดกดได้ใต้ภาพ — ชุดรูปแต่ละการ์ดยกมาจากการ์ดเดียวกันในหน้า works.html (บ้านเดี่ยว/คอนโด/สำนักงาน) ทำงานแยกอิสระต่อการ์ดเหมือนต้นฉบับ
- ใช้สคริปต์ perl แปลงมาร์กอัปทีเดียวทั้ง 9 ไฟล์ (จับ pattern เฉพาะส่วน ASCII ตามข้อควรระวังภาษาไทยใน CLAUDE.md พร้อม sanity check 3 การ์ด/ไฟล์ครบทุกไฟล์)
- เพิ่ม CSS `.workcard .gslide`/`.gdots` ใน works-preview block ของ `product.css` (คัดจาก works.html ตัดส่วน `.gnav` ทิ้ง) + `.thumb` เพิ่ม `overflow:hidden` ให้ตรงต้นฉบับ
- JS: แทนที่ loader `.thumb[data-img]` เดิมของ 9 หน้า ด้วยตัว init แกลเลอรีต่อการ์ด (โหลดรูป gslide + ผูกคลิกจุด — โค้ดเดียวกับ works.html ตัด gnav/lightbox ออก)
- อัปเดต CLAUDE.md 3 จุดให้ตรงสถานะใหม่ (works-preview มีจุดแล้ว / loader เดิมถูกแทน / ต้อง sync กับ works.html)
- ตรวจผ่าน localhost + Chrome: หน้า Finnix Ceramic เดสก์ท็อป (จุดขึ้นครบ 3 การ์ด กดจุดที่ 3 สลับรูปได้ การ์ดอื่นไม่กระทบ), หน้า Regionfilm (รูปโหลดครบ), จำลองจอมือถือ 430px (จุด+กล่องข้อความแสดงถูกต้อง)

## ครั้งที่ 76 — 2026-07-22

**Prompt:**
> ในหน้า products ตรงหน้าดูผลงานหรือผลงานติดตั้งแต่ล่ะบ้านที่ โชว์ 3 ผลงาน ช่องผลงานแต่ล่ะช่องสี่เหลื่ยมทำให้มันเหมือนกับในช่องที่อยู่ใน Project refetence ทั้งดูรูปผลงานได้เลื่อนดูรูปได้ เหมือนกับผลงานใน project refetence

**สิ่งที่แก้ไข:**
- **อัปเกรดการ์ดผลงานในหน้าสเปกทั้ง 9 หน้าให้เหมือนหน้า Project Reference ครบทุกลูกเล่น** (ต่อยอดจากครั้งที่ 75 ที่ใส่เฉพาะจุดสไลด์): เพิ่ม **ปุ่มลูกศรวงกลม ‹ › บนรูป** (โผล่ตอน hover, hover ปุ่มเป็นสีทอง) กดเลื่อนรูปไป-กลับได้ และ **กดที่รูปเปิด lightbox ดูรูปขยายเต็มจอ** พร้อมปุ่มปิด/ลูกศรซ้ายขวาในจอเต็ม, ปิดด้วย Escape หรือคลิกพื้นหลัง, เลื่อนรูปด้วยลูกศรคีย์บอร์ดได้ — สถานะรูป/จุดของการ์ดยังซิงก์กันหลังปิด lightbox เหมือนต้นฉบับ works.html ทุกอย่าง
- มาร์กอัป: แทรกปุ่ม `.gnav .gprev/.gnext` ในกล่องรูปทั้ง 3 การ์ด + แทรก `#lightbox` overlay ท้ายหน้า (ก่อน script หลัก) — ใช้สคริปต์ perl แปลงทีเดียว 9 ไฟล์พร้อม sanity check เหมือนครั้งก่อน
- CSS: เพิ่มชุด `.workcard .gnav` + `.lightbox` (พร้อม media ≤768px ของ lightbox) ใน `product.css` และเติม `z-index:1` ให้ `.thumb::after` ตรงตามต้นฉบับ works.html
- JS: แทนบล็อกแกลเลอรีแบบจุดอย่างเดียว (ครั้งที่ 75) ด้วยโค้ดเต็มจาก works.html — lightbox handlers (`closeLightbox`/`lbStep`/คีย์บอร์ด) + per-card init (โหลดรูป + ลูกศร + จุด + คลิกรูปเปิด lightbox ผ่าน `{ slides, showSlide }` ที่ stash ต่อการ์ด)
- อัปเดต CLAUDE.md: การ์ดผลงานหน้าสเปกเป็น full clone ของ component ใน works.html แล้ว (เล่าประวัติว่าเช้าวันเดียวกันเป็นแบบจุดอย่างเดียวก่อน แล้วผู้ใช้ขอแบบเต็ม) + lightbox ไม่ใช่ works.html-only อีกต่อไป
- ตรวจผ่าน localhost + Chrome หน้า Finnix Ceramic: hover เห็นลูกศร, กดลูกศรขวารูปเลื่อน+จุดตาม, กดรูปเปิด lightbox แสดงรูปเดียวกันขยายเต็มจอ, Escape ปิดแล้วการ์ดคงสถานะสไลด์เดิมถูกต้อง

## ครั้งที่ 77 — 2026-07-22

**Prompt:**
> งั้นขัดเลยจัดการเลยตามขั้นตอนที่ 1 ส่วนประกัน 10 ปีแก้เลยไม่ 8 ส่วนคำไหนที่มันไม่จำเป็นต้องมีเอาออก
> (ต่อจากการวิเคราะห์ข้อความทั้งเว็บในเทิร์นก่อน)

**สิ่งที่แก้ไข — หมวด 1 ตัดข้อความซ้ำ:**
- **หน้าแรก: ตัดสไลด์สรรพคุณ 02–05 ในหมวด "ยินดีต้อนรับสู่ WORLD FILM"** — เนื้อหาซ้ำกับการ์ด 4 ใบ "ฟิล์มกรองแสงที่ให้มากกว่าการลดแสง" ที่อยู่ติดกันด้านบน (ลดความร้อน 85% / UV 99% / ไม่บังวิว / ความเป็นส่วนตัว) เหลือสไลด์แนะนำร้าน (01) อย่างเดียว → ตัดปุ่มลูกศร `.intro-nav`, จุดไข่ปลา `.intro-dots`, รูปสไลด์ `.fslide` ที่เกิน (5→1), CSS ทั้ง 3 ชุด (desktop + ≤768 + ≤480) และ JS `showIntro` ทั้งบล็อกออกด้วย · ตัด kicker "01 / 05 · WORLD FILM" (ซ้ำกับ eyebrow ด้านบน)
- **หน้า About: ตัด section "ทำไมต้องเลือก WORLD FILM" (การ์ด 3 ใบ)** — เนื้อหาทับกับแถบ 4 ไอคอนที่อยู่ถัดลงไปในหน้าเดียวกัน
- **ตัด section "ติดต่อเรา / พร้อมให้คำปรึกษาทุกพื้นที่" ออกจากหน้าแบรนด์ 3 หน้า** (Finnix/3M/Ultra Guard) — หน้าเหล่านี้เป็นแค่หน้าเลือกรุ่น เนื้อหาน้อยกว่า section ติดต่อเสียอีก และยังมีช่องทางติดต่อครบทั้งปุ่มเมนู ปุ่มลอย และ footer อยู่แล้ว (หน้า Regionfilm เป็นหน้าสเปกจึงคงไว้)
- **ลดการพูดซ้ำ "มากกว่า 40 สาขาทั่วประเทศ"** — หน้าแรกจาก 4 ครั้งเหลือ 2 (hero + footer), หน้า About จาก 3 เหลือ 2
- **หน้ารุ่น Finnix Ceramic: ตัดแถว "เหมาะสำหรับ" ในการ์ดเฉดสีทั้ง 3 ใบ** — ซ้ำกับคำโปรยโทนที่อยู่บนหัวการ์ดใบเดียวกัน (เช่น "โทนเข้ม — กันร้อน เป็นส่วนตัว" แล้วตามด้วย เหมาะสำหรับ "เน้นกันร้อน · เป็นส่วนตัวสูง")

**สิ่งที่แก้ไข — รับประกันเป็น 10 ปีทุกจุด:**
- เปลี่ยน "รับประกัน 8 ปี" → **"รับประกัน 10 ปี"** ในแถบ 4 ไอคอนของ 14 หน้า (about, works, brand ×4, model ×8) ตอนนี้ตรงกับหน้าแรก/contact ที่เป็น 10 ปีอยู่แล้ว
- ป้ายกลมข้างรูปหน้าแรก 8 → **10 ปี**, สถิติหน้า About "8 ปี รับประกันสูงสุด" → **10**, สถิติหน้าผลงาน "8 ปี รับประกัน" → **10**
- ข้อความหน้า About "รับประกันสูงสุด 8 ปี" หายไปพร้อม section ที่ตัดออก
- **ไม่แตะตาราง spec รายรุ่น** (Regionfilm 5 ปี · Ultra Guard 6 ปี · Finnix Extra Clear/Titanium 7 ปี · Finnix UV Guard 8 ปี · 3M ตามมาตรฐาน) — เป็นสเปกรับประกันของแต่ละรุ่นซึ่งไล่ระดับกันอยู่ คนละเรื่องกับคำสัญญาบริการของร้าน (ยังเป็นตัวเลขตัวอย่าง รอยืนยันจากเจ้าของร้าน)

**สิ่งที่แก้ไข — ตัดคำ/ส่วนที่ไม่จำเป็น:**
- **ตัดคอลัมน์ "บริการ" ใน footer หน้าแรก** — ลิงก์ "ฟิล์มบ้าน" กับ "ฟิล์มอาคาร/สำนักงาน" ชี้ไป `#why` จุดเดียวกันทั้งคู่ ไม่ใช่หน้าบริการจริง (ปรับ grid footer 4 → 3 คอลัมน์)
- **ตัดบรรทัดท้าย footer "ฟิล์มเซรามิกพรีเมียม Finnix Ceramic"** ออกจากหน้า root ทั้ง 4 หน้า — โปรโมตแบรนด์เดียวทั้งที่ขาย 4 แบรนด์ (หน้าสินค้ายังคงบรรทัดของตัวเองที่ตรงกับหน้านั้นไว้)
- **ตัดช่องอีเมลในหน้า Contact** — `worldfilmcenter@email.com` เป็น placeholder ไม่ใช่อีเมลจริง ลูกค้าส่งแล้วหายเงียบ (เหลือ LINE / Facebook / โทรศัพท์ 3 ช่องทางที่ใช้ได้จริง)
- **แก้ชื่อรุ่นที่ไม่มีอยู่จริง "Finnix Ceramic 60" → "Finnix Ceramic"** ในการ์ดผลงานทั้ง 10 หน้า (works + หน้าสเปก 9 หน้า)
- ตรวจผ่าน localhost + Chrome ทุกหน้าที่แก้: หน้าแรก (สไลด์เหลือใบเดียว ป้าย 10 ปี ไม่มีปุ่ม/จุดค้าง), About (section หายไปเรียบร้อย ไม่เหลือ 8 ปี), brand-finnix (เหลือ 2 section + footer/ปุ่มลอยครบ), model-finnix-ceramic (การ์ดเฉดเหลือ 3 แถว แกลเลอรี+lightbox ยังทำงาน), contact (เหลือ 3 ช่องทาง), works (10 ปี, ไม่มี Ceramic 60)

**ยังไม่ได้แก้ (รอเจ้าของร้านยืนยันตัวเลขจริง):**
- ลดความร้อนสูงสุดไม่ตรงกัน: หน้าแรก/About 85% · หน้าแบรนด์ Finnix 88% · หน้ารุ่น Finnix Ceramic 82%
- "200+ โครงการที่ส่งมอบ" ในหน้าผลงาน — เป็นคำกล่าวอ้างทางธุรกิจที่ยังไม่ได้ยืนยัน จึงไม่ลบเอง

## ครั้งที่ 78 — 2026-07-22

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและปรับปรุงไฟล์ CLAUDE.md สำหรับ Claude Code instance ถัดไป

**สิ่งที่แก้ไข:**
- ทำงานค้างจากเทิร์นก่อนให้จบและ commit ก่อน (ครั้งที่ 77) แล้วจึงปรับปรุงเอกสาร
- อัปเดต `CLAUDE.md` ให้ตรงกับสภาพเว็บหลังตัดข้อความ:
  - หมวดสไลด์หน้าแรก: เปลี่ยนจาก "2 สไลเดอร์" เป็นสไลเดอร์เดียว (hero) + อธิบายว่าหมวดยินดีต้อนรับกลายเป็น layout นิ่งแล้ว, มาร์กอัป `.ipanel`/`.figure-slides` ยังอยู่ (ใช้จัดสไลด์เดียว) ถ้าจะเพิ่มพาแนลกลับต้องเอา nav+JS กลับด้วย
  - เพิ่มหมายเหตุว่าหน้าแบรนด์ 3 หน้าตั้งใจให้สั้น (ไม่มี section ติดต่อ) — ห้ามใส่กลับ
  - แทนที่หมายเหตุ "8 vs 10 ปี ขัดกัน" ด้วย "10 ปีทั่วทั้งเว็บแล้ว" + ระบุว่าตาราง spec รายรุ่น (5–8 ปี) เป็นคนละเรื่องและตั้งใจไม่แตะ
  - เพิ่มรายการสิ่งที่ **ตั้งใจตัดออก** ทั้งหมดจากครั้งที่ 77 เพื่อไม่ให้ instance ถัดไปเผลอใส่กลับ + ระบุ 2 ประเด็นที่ยังค้าง (ลดความร้อน 85/88/82 และ 200+ โครงการ) ว่าห้ามแก้เองต้องถาม
  - แก้จำนวนหน้าที่มี featurebar 14 → 11 หน้า และลบการอ้างถึงฟังก์ชัน `showIntro` ที่ถูกลบไปแล้ว
- อัปเดต `README.md` (คู่มือภาษาไทยของเจ้าของร้าน): เพิ่มหัวข้อ "ตัวเลขที่ยังขัดกันอยู่ รอเจ้าของร้านยืนยัน" + อัปเดตวันที่ท้ายไฟล์

## ครั้งที่ 79 — 2026-07-24

**Prompt:**
> /init — วิเคราะห์โค้ดเบสและปรับปรุงไฟล์ CLAUDE.md

**สิ่งที่แก้ไข:**
- ตรวจสอบว่า `CLAUDE.md` (เขียนใหม่ครั้งล่าสุดในคอมมิต d9cbb38) ยังตรงกับโค้ดจริง — ยืนยันจำนวนหน้า 16, โลโก้รุ่น 6 ไฟล์, `.gitignore`, และโครงสร้างทั้งหมดถูกต้อง ไม่มี drift
- เพิ่มสคริปต์เซิร์ฟเวอร์พรีวิว PowerShell `HttpListener` แบบก๊อปวางรันได้จริง ลงในหมวด "Running / previewing" — เดิมมีแต่คำอธิบายเป็นข้อความ ทำให้ทุกเซสชันต้องประกอบสคริปต์ขึ้นใหม่เอง

## ครั้งที่ 80 — 2026-07-24

**Prompt:**
> ในหน้า home แถวคอลัมน์ ความเย็นสบายและแสงธรรมชาติ อยู่รวมกันได้อย่างลงตัว ขอแบบเดิมเลยที่มีสไลด์ 5 สไลด์เอาแบบเดิมกลับคืนมา

**สิ่งที่แก้ไข — คืนสไลด์ 5 สไลด์ให้ section "ยินดีต้อนรับ" (index.html):**
- คืน **สไลด์รูป 5 ใบ** (`.fslide`) จากที่เหลือใบเดียว
- คืน **พาแนลข้อความ 5 หัวข้อ** (`.ipanel` 01–05): 01 WORLD FILM · 02 ลดความร้อน 85% · 03 UV 99% · 04 ใสคมชัด · 05 ความเป็นส่วนตัว
- คืน **ปุ่มลูกศรซ้าย/ขวา** (`.intro-nav`) + **จุดไข่ปลา 5 จุด** (`.intro-dots`) และ JS `showIntro` ที่เลื่อนรูป/พาแนล/จุด พร้อมกัน (วนรอบด้วย modulo)
- คืน CSS ของ `.intro-nav`/`.intro-dots` ทั้งบล็อกหลัก + media query ≤768 (ลูกศรลงมาเป็นแถวกลางใต้พาแนล) + ≤480 (ย่อปุ่มเหลือ 44px)
- **คงนโยบายเดิมของเว็บไว้:** ตรารับประกันยังเป็น **10 ปี** (ไม่ย้อนกลับไป 8) และลายเซ็นพาแนล 01 ยังใช้ข้อความสั้น "ติดตั้งโดยทีมช่างผู้เชี่ยวชาญ" (ไม่คืน "มากกว่า 40 สาขา")
- ยกโค้ดกลับมาจากคอมมิต 4537110 (ก่อนถูกตัดในครั้งที่ 77) แบบเลือกเฉพาะส่วนสไลด์ ไม่ revert ทั้งไฟล์
- ตรวจผ่าน localhost + Chrome: ปุ่ม next/prev/จุด เลื่อนครบ 5 สไลด์และวนรอบถูกต้อง, รูป+ตรา 10 ปี, ลูกศร 2 ข้าง, จุด 5 จุด (จุดแรกสีทอง) แสดงครบ
- อัปเดต `CLAUDE.md` (หมวด Homepage slider + รายการ text-bloat) ให้ระบุว่าสไลด์ถูกคืนแล้ว 2026-07-24

## ครั้งที่ 81 — 2026-07-24

**Prompt:**
> ในหน้า products แบรนด์ finnix ที่มี 4 รุ่นเพิ่มอีกรุ่นให้เป็น 5 รุ่น แล้วพอมี 5 รุ่นแล้ว เอา logo ที่อยู่ในไฟล์แต่ล่ะอันมาใส่ให้ตรงกับรุ่นนั้นๆ เอาแค่แบรนด์ finnix
> (ตามด้วย) ฉันใส่ logo UV Guard ไปในไฟล์แล้วลองเช็คดู

**สิ่งที่แก้ไข — เพิ่ม Finnix เป็น 5 รุ่น + จับคู่โลโก้ให้ตรงทุกรุ่น:**
- ตรวจไฟล์โลโก้ต้นฉบับใน `assets/source/` พบว่า Finnix มี 5 โลโก้จริง: `CM`=Ceramic, `EC`=Extra Clear, `T.T`=Titanium, `C`=**Crystalize**, และ `LOGO SM.png` (ชื่อชวนเข้าใจผิด) = **UV Guard** ตัวจริง (ผู้ใช้เพิ่งวางไฟล์นี้เข้ามาให้)
- **แก้โลโก้ UV Guard ที่ผิดมานาน** — เดิม `finnix-uvguard.png` เป็นโลโก้ Crystalize (ถูกจับใส่ผิดตั้งแต่แรกโดยการตัดตัวเลือก) ตอนนี้:
  - คัดโลโก้ Crystalize เดิม → `finnix-crystalize.png` (รุ่นใหม่)
  - สร้าง `finnix-uvguard.png` ใหม่จากโลโก้ UV Guard ตัวจริง — โลโก้นี้อัตราส่วนกว้าง-เตี้ยกว่ารุ่นอื่น จึงจัดกึ่งกลางบนแคนวาส 1200×848 (กว้าง ~1190px) ให้ขนาดเท่าการ์ดอื่นทั้ง 5
- **เพิ่มหน้ารุ่นที่ 5: `model-finnix-crystalize.html`** — ยกโครงจากหน้า UV Guard แล้วปรับเนื้อหาเป็น Crystalize (swatch `tone-prem`, VLT 50%, ลดความร้อน 84%, UV 99%, รับประกัน 8 ปี — ตัวเลข placeholder เหมือนรุ่นอื่น), แกลเลอรีผลงาน + lightbox ครบ
- **`brand-finnix.html`** — เพิ่มการ์ดที่ 5 (Crystalize) + แก้ข้อความ "4 รุ่น" → "5 รุ่น" ทั้ง eyebrow, lead, meta description
- ลบไฟล์ทดสอบ `assets/models/LOGO SM.png` ที่ผู้ใช้วางไว้ (ใช้ต้นฉบับใน `assets/source/` แทน)
- ตรวจผ่าน localhost + Chrome: การ์ด 5 ใบเรียงโลโก้ถูกทุกอัน (Ceramic·Titanium·UV Guard / Extra Clear·Crystalize), UV Guard โชว์โลโก้ที่ถูกแล้ว, หน้ารุ่น Crystalize เปิดได้ครบ
- อัปเดต `README.md` (tree + page map + วันที่) และ `CLAUDE.md` (hierarchy 5 รุ่น + ตารางจับคู่โลโก้ Finnix + ปิดประเด็น "Crystalize" ที่เคยค้าง)

## ครั้งที่ 82 — 2026-07-24

**Prompt:**
> ปรับในหน้า home ตรงแถวคอลัมน์ welcome world film แต่ล่ะหน้าจะแสดงข้อความและภาพพื้นหลังไม่เหมือนกันซึ่งมี 3 หน้า หน้าแรกเป็นภาพบ้านภายนอกสวยๆ ส่วนประโยคเอาแบบนี้เลยแต่จัดองค์ประกอบให้สวย หน้า 2 รูปภายในบ้านมีสัตว์เลี้ยง ประโยค "บ้านคือพื้นที่แห่งความสุข..." หน้า 3 รูปช่างกำลังติดตั้งฟิล์ม ประโยค "มีมาตรฐานการติดตั้งโดยทีมช่างมืออาชีพ..."

**สิ่งที่แก้ไข — ปรับสไลด์ section "ยินดีต้อนรับ" หน้าแรกจาก 5 → 3 สไลด์ เนื้อหา/ภาพต่างกันทุกใบ:**
- **สไลด์ 1** — ภาพบ้านภายนอกสวย ๆ (คงภาพเดิม) + ข้อความ WORLD FILM เดิมตามที่ผู้ใช้บอก "เอาแบบนี้เลย" (คง `.sign` + ตรา 10 ปี), kicker "01 / 03 · WORLD FILM"
- **สไลด์ 2** — ภาพภายในบ้านมีสุนัข (สัตว์เลี้ยง) + หัวข้อ "บ้านคือพื้นที่แห่งความสุข" / เนื้อหา "เลือกปกป้องรังสี UV ... เพราะทุกคนในบ้านสำคัญ ไม่ว่าจะเดิน 2 ขา หรือ 4 ขา ...", kicker "02 / 03 · เพื่อทุกคนในบ้าน"
- **สไลด์ 3** — ภาพช่างกำลังรีดฟิล์มบนกระจก + หัวข้อ "มีมาตรฐานการติดตั้งโดยทีมช่างมืออาชีพ" / เนื้อหา "ใส่ใจทุกรายละเอียด ... เพื่อคุณภาพที่ดีที่สุด", kicker "03 / 03 · มาตรฐานการติดตั้ง"
- เลือกภาพจาก Unsplash โดยเปิดดูจริงในเบราว์เซอร์ก่อน (exterior `1600585154340`, pet `1668036268050`, installer `1651481127251`) — ตรวจว่าโหลดได้จริงและตรงธีมทุกใบ
- ปรับจุดไข่ปลาเหลือ 3 จุด + aria-label เป็น "สไลด์" (JS `showIntro` เดิมใช้ `.length` อยู่แล้ว ไม่ต้องแก้)
- ตรวจผ่าน localhost + Chrome: กดลูกศร/จุด สลับครบ 3 สไลด์ ภาพ+ข้อความตรงทุกใบ, ครอสเฟดทำงานปกติ
- อัปเดต `CLAUDE.md` (หมวด Homepage slider + หมายเหตุ text-bloat) ให้ตรงว่าตอนนี้เป็น 3 สไลด์เนื้อหาใหม่

## ครั้งที่ 83 — 2026-07-24

**Prompt:**
> ปรับในหน้า home ตรงแถวคอลัมน์ welcome world film ... จัดองค์ประกอบให้มันสวย (ผู้ใช้ส่งสเปกเดิมซ้ำ)

**สิ่งที่แก้ไข — เก็บงานองค์ประกอบ section "ยินดีต้อนรับ" (3 สไลด์เดิมยังอยู่ครบ):**
- ยืนยันว่าสไลด์ 3 ใบ (บ้านนอก / ภายในบ้านมีสุนัข / ช่างติดตั้งฟิล์ม) ยัง live อยู่ครบ ตรวจซ้ำในเบราว์เซอร์ทั้ง 3 สไลด์
- **เพิ่มความ "สวย" ให้องค์ประกอบ:** ใส่ไล่เฉดเข้มบาง ๆ ก้นรูป (`.figure-slides::after`) — ทำให้ตรารับประกัน 10 ปี เด่นขึ้นและภาพดูมีมิติ/พรีเมียมขึ้น (เลย์เอาต์รูปซ้าย/ข้อความขวา + Ken-Burns zoom + กรอบทอง ยังคงเดิม)
- **เก็บการแก้ไขของผู้ใช้ที่แก้เองในการ์ด `.benefits`** (`ลดความร้อนสะสม` → `ลดความร้อน`, ตัด "85%" ออกจากคำโปรย) ไว้ครบ ไม่ทับ — commit รวมเพื่อกันงานหาย
- หมายเหตุ: ยังเหลือ "85%" ใน meta description บรรทัด 11 (ยังไม่แตะ รอผู้ใช้ตัดสินใจ)

## ครั้งที่ 84 — 2026-07-24

**Prompt:**
> เลือก B จัดมา (เปลี่ยนองค์ประกอบ section ยินดีต้อนรับเป็นแบบ "ข้อความซ้อนทับบนรูปเต็มความกว้าง / แบนเนอร์")

**สิ่งที่แก้ไข — เปลี่ยนเลย์เอาต์ section "ยินดีต้อนรับ" เป็นแบนเนอร์เต็มความกว้าง (Option B):**
- รื้อจากเลย์เอาต์ 2 คอลัมน์ (รูปการ์ดซ้าย + ข้อความขวา) เป็น **`.intro-banner` แบนเนอร์เต็มความกว้าง** ที่รูปเป็นพื้นหลังและข้อความซ้อนทับ
- รูปสไลด์ (`.fslide`) เป็นพื้นหลัง cover + ซูม Ken-Burns, มีเฉดมืด 2 แกน (`::before` เข้มทางซ้าย+ก้น) ให้ข้อความสีอ่อนอ่านชัด
- ข้อความ (`.intro-overlay`) ชิดซ้าย กึ่งกลางแนวตั้ง — หัวข้อสีขาว + kicker/eyebrow สีทอง; ตรารับประกัน 10 ปี ย้ายไปมุมขวาบน
- แถบควบคุมซ้อนบนภาพก้นแบนเนอร์: จุดไข่ปลาชิดซ้าย (จุด active เป็นแคปซูลทอง) + ลูกศรวงกลมกระจกฝ้าชิดขวา
- มือถือ (≤768): สลับเป็นข้อความชิดล่างเต็มความกว้าง + เฉดมืดจากก้นภาพ; ปรับ ≤980/≤480 ให้เข้ากับแบนเนอร์
- เนื้อหา 3 สไลด์เดิม (บ้านนอก / ในบ้านมีสุนัข / ช่างติดตั้ง) คงครบ, JS `showIntro` ใช้คลาสเดิมจึงไม่ต้องแก้; อัปรูปเป็น `w=1600` ให้คมบนแบนเนอร์ใหญ่
- ตรวจผ่าน localhost + Chrome ทั้ง 3 สไลด์ (ต้อง cache-bust `?v=` เพราะเซิร์ฟเวอร์ไม่ส่ง no-cache) — สลับสไลด์ครบ ข้อความอ่านชัดทุกใบ
- อัปเดต `CLAUDE.md` (หมวด Homepage slider) ให้ตรงกับเลย์เอาต์แบนเนอร์ใหม่

## ครั้งที่ 85 — 2026-07-24

**Prompt:**
> (ชี้แจง) ให้แก้ "หัวหน้าแรก / Welcome to WORLD FILM (hero)" ให้เป็น 3 สไลด์ ข้อความ+ภาพต่างกัน (บ้าน/สัตว์เลี้ยง/ช่าง) แล้วเอา section "ยินดีต้อนรับ" ที่เผลอแก้ กลับเป็นแบบเดิมที่มี 5 สไลด์

**สิ่งที่แก้ไข — สลับเป้าหมายให้ถูก: แก้ hero, คืน section กลาง:**
- **ทำ hero (บนสุด) เป็นสไลด์ 3 ใบ ข้อความเปลี่ยนตามภาพ** — เดิม hero เปลี่ยนแค่รูป ตอนนี้ห่อข้อความ (eyebrow+h1+thai-sub) เป็น `.hero-panels .hpanel` แล้วให้ `showSlide` สลับพร้อมรูป+จุด:
  - สไลด์ 1 รูปบ้านนอก + "Welcome to WORLD FILM" (คงเดิม "เอาแบบนี้เลย")
  - สไลด์ 2 รูปในบ้านมีสุนัข + "บ้านคือพื้นที่แห่งความสุข ..."
  - สไลด์ 3 รูปช่างติดตั้ง + "ทีมช่างมืออาชีพ / มีมาตรฐานการติดตั้ง ..."
  - การ์ด LINE/โทร/Facebook + บรรทัด note อยู่คงที่ใต้แผงข้อความ; หัวข้อไทยสไลด์ 2–3 ใช้ `.h-th` (เล็กกว่าหัว Welcome)
- **คืน section "ยินดีต้อนรับ" กลางหน้าเป็นสไลด์ 5 ใบ** (split layout เดิม) — ย้อน index.html กลับคอมมิต 4aab014 แล้วเก็บงานที่ตั้งใจไว้กลับมา: แก้การ์ด benefits (ตัด 85%), ย่อ thai-sub + note ของ hero (งานที่ผู้ใช้แก้เอง) — ไม่ให้ revert ทับ
- ตรวจผ่าน localhost + Chrome: hero สลับ 3 สไลด์ ข้อความ+ภาพตรงทุกใบ, section กลางกลับเป็น 5 สไลด์, การ์ด benefits/thai-sub/note คงค่าที่ผู้ใช้แก้ (ต้อง cache-bust `?v=`)
- อัปเดต `CLAUDE.md` (หมวด Homepage sliders เขียนใหม่ให้แยก hero vs ยินดีต้อนรับ ชัด) + จดว่าผู้ใช้สะดวกพิมพ์ตอบมากกว่าเมนูตัวเลือก

## ครั้งที่ 86 — 2026-07-25

**Prompt:**
> มาทำเว็บต่อ ตอนนี้หน้า home ค่อนข้างจะสมบูรณ์ที่สุด เอาหน้า home เป็นแบบอย่าง คือถ้ามีแถว/คอลัมน์ไหนซ้ำกันหรืออะไรซ้ำกันกับหน้าอื่น ๆ แก้ไขเลยเอาตามข้อความในหน้า home เป็นหลัก เช่น "ติดต่อเรา / พร้อมให้คำปรึกษาทุกพื้นที่" ที่หน้า home

**สิ่งที่แก้ไข — sync section "ติดต่อเรา / พร้อมให้คำปรึกษาทุกพื้นที่" (featurebar 4 ช่อง) ทุกหน้าให้ตรงกับหน้า home:**
- ยึดข้อความ+ไอคอน+ลำดับตามหน้า home เป็นต้นแบบ: **ปรึกษาฟรี** (โดยผู้เชี่ยวชาญ) · **ประเมิณพื้นที่หน้างานฟรี** (โดยไม่มีค่าใช้จ่าย · ไอคอนไม้บรรทัด) · **รับประกันสูงสุด 10 ปี** (ยาวนานหมดห่วง · ไอคอนโล่) · **ผ่อน 0%** (นานสูงสุด 4 เดือน · ไอคอน %)
- **9 หน้า** (`works.html` + 8 หน้า model) แทนที่ทั้ง block `.featurebar .grid` จากชุดเดิม (ผ่อน 0%/นานสูงสุด 10 เดือน · รับประกัน 10 ปี · ปลอดสารพิษ) → ชุด home; แก้ทั้งลำดับ ข้อความ และไอคอน (item2 % → ไม้บรรทัด, item4 ใบไม้ → %)
- **`brand-regionfilm.html`** (spec page, จัดบรรทัด SVG แบบ variant B บรรทัดเดียว) แทนที่ block เดียวกันให้ตรง home
- **`about.html`** เดิมเรียงข้อความตาม home แล้วแต่ไอคอนยังผิด (item2 เป็น % / item4 เป็นใบไม้) + พิมพ์ผิด `ประเมิน→` / `ค่าใช้ต่าย` / `ผ่อน 0% ` เว้นวรรคเกิน → แก้ไอคอนให้ถูก + ข้อความตาม home
- **`contact.html`** (ใช้ `.vprop` ของตัวเอง ไอคอนถูกอยู่แล้ว) แก้เฉพาะข้อความ 3 จุด: `วัดหน้างานฟรี→ประเมิณพื้นที่หน้างานฟรี`, `รับประกัน 10 ปี→รับประกันสูงสุด 10 ปี`, `นานสูงสุด 10 เดือน→4 เดือน`
- ตรวจด้วย grep: ไม่เหลือข้อความเก่า (`นานสูงสุด 10 เดือน`/`วัดหน้างานฟรี`/`ปลอดสารพิษ` ใน featurebar/`รับประกัน 10 ปี`) และ `ประเมิณพื้นที่หน้างานฟรี` ปรากฏครบทั้ง 14 หน้า
- หมายเหตุ: คำว่า `ประเมิณ` (ณ) ในหน้า home สะกดไม่ตรงหลัก (ที่ถูกคือ `ประเมิน`) — ทำตามข้อความ home ก่อนเพื่อให้ทุกหน้าเหมือนกัน รอเจ้าของยืนยันว่าจะแก้ให้ถูกทั้งเว็บหรือไม่

## ครั้งที่ 87 — 2026-07-27

**Prompt:**
> /init (ให้ตรวจโค้ดทั้งโปรเจกต์แล้วปรับปรุง `CLAUDE.md`)

**สิ่งที่แก้ไข — ตรวจ `CLAUDE.md` เทียบกับเว็บจริง แล้วอัปเดตส่วนที่ล้าสมัย:**
- **แถบ 4 ช่อง (featurebar):** เขียนใหม่ทั้งย่อหน้า — เดิมบอกว่าหน้าแรกใช้ข้อความคนละชุดกับอีก 11 หน้า แต่ครั้งที่ 86 sync ให้ตรงกันหมดแล้ว จึงบันทึกชุดข้อความ+ไอคอนมาตรฐาน 4 ช่อง และระบุว่าชุดเก่า (`วัดหน้างานฟรี`/`ปลอดสารพิษ`/`นานสูงสุด 10 เดือน`) เลิกใช้แล้ว ห้ามเอากลับ
- **เพิ่มกฎใหม่ "หน้า home คือต้นแบบข้อความ"** (คำสั่งยืนของผู้ใช้จากครั้งที่ 86) — ถ้าแถว/section ไหนซ้ำกันหลายหน้า ให้ยึดคำ/ลำดับ/ไอคอนของหน้าแรกเป็นหลักเสมอ
- **บันทึกคำสะกด `ประเมิณ`** ว่าเป็นคำผิดที่ตั้งใจปล่อยไว้ให้ตรงกันทั้งเว็บ รอเจ้าของสั่งแก้ทีเดียว 14 หน้า — ห้ามแก้เฉพาะบางหน้า
- **ปรับหมวดตัวเลขที่ยังขัดกัน:** ตัดเรื่อง "200+ โครงการ" ออก (ผู้ใช้แก้เป็น `1000+` และลบแถวสถิติ `.mini` ในหน้าผลงานเอง — commit รวมมาในครั้งนี้ พร้อมจดว่า CSS `.work-hero .mini` กลายเป็นโค้ดตายแล้ว) และแก้รายการ 85% ให้ตรงจริง (เหลือใน meta หน้าแรก, `about.html` ไม่มีแล้ว, เพิ่ม Ultra Guard Nano ในกลุ่ม 82%)
- **เพิ่มกับดักที่เจอซ้ำ ๆ 2 ข้อ:** เซิร์ฟเวอร์พรีวิว PowerShell ไม่ส่ง `Cache-Control` ต้อง cache-bust `?v=` ทุกครั้งที่เช็กงานที่เพิ่งแก้ · `CHANGELOG.md` ยาวเกิน 1,200 บรรทัดแล้ว ให้อ่านเฉพาะท้ายไฟล์ด้วย `Read` (ห้าม `Get-Content` ลงคอนโซล เพราะภาษาไทยเพี้ยน)
- **`README.md`:** อัปเดตรายการตัวเลขที่รอยืนยันให้ตรงกับ `CLAUDE.md` (1000+ เป็นตัวเลขที่เจ้าของยืนยันแล้ว + เพิ่มหมายเหตุคำสะกด `ประเมิณ`) และเลื่อนวันที่ท้ายไฟล์เป็น 2026-07-27

## ครั้งที่ 88 — 2026-07-27

**Prompt:**
> ในหมวดหมู่ Products ในหน้ารุ่นฟิล์มแต่ล่ะรุ่นฟิล์มที่โชว์สเปกฟิล์มและอธิบายฟิล์มแต่ล่ะรุ่น เอา textbox 5-35% แสงส่องผ่าน (VLT) 82% ลดความร้อนสูงสุด 99% กันรังสี UV เอาออกทั้ง 3 ช่องนั้น และในแถวคอลัมน์ ที่โชว์กันความร้อน ที่อธิบาย 35 15 05 ใส่สไลด์ Transition ในโทนสว่างโปร่ง มองวิวชัด หลักการคือ โชว์สเปกตอนก่อนพอเอาเคอร์เซอร์เมาส์ไปชี้แล้วตัวฟิล์มโปร่งจะสไลด์ขึ้นมาโชว์

**สิ่งที่แก้ไข:**
- **ถอดกล่องสถิติ 3 ช่องใต้ชื่อรุ่น (`.phl` / `.hi`) ออกครบ 10 หน้าสเปก** — 9 หน้า `model-*.html` + `brand-regionfilm.html` (VLT / ลดความร้อน / กันรังสี UV) ตัดทั้ง block ไม่เหลือเศษ hero จึงเหลือ eyebrow → ชื่อรุ่น → คำโปรย → ปุ่ม CTA (ตัวเลขชุดเดิมยังอยู่ที่ป้าย `.vlt` บนแผ่นสีและในตารางสเปก จึงไม่ได้หายไปจากหน้า)
- **ลบ CSS `.phl`/`.hi` ที่ตายแล้วออกจาก `product.css`** ทั้ง 3 จุด (บล็อกหลัก + override ใน `≤768` อีก 2 ที่)
- **ใส่สไลด์ Transition ให้การ์ดเฉด CM 35 / CM 15 / CM 05** (`model-finnix-ceramic.html` — หน้าเดียวที่มี `.shadegrid`): ปกติโชว์ตารางสเปกเหมือนเดิม พอเอาเมาส์ไปชี้ **แผ่นฟิล์มโปร่งจะสไลด์ขึ้นมาจากด้านล่าง** (0.6s) โชว์รูปวิวบ้านในโทนสว่างโปร่ง + ข้อความประจำเฉดนั้น
  - CM 35 "สว่าง โปร่ง มองวิวชัด" · CM 15 "วิวยังชัด ในโทนที่นุ่มตา" · CM 05 "มองออกได้ชัด มองเข้าแทบไม่เห็น"
  - โครงสร้าง: ห่อ `.sc-specs` ไว้ใน `.sc-body` แล้ววาง `.sc-specs` กับ `.sc-view` ซ้อนช่องเดียวกันแบบ grid (`grid-area:1/1`) — ความสูงการ์ดจึงยึดเลเยอร์ที่สูงกว่า ข้อความไม่โดนตัด
  - อ่านง่าย: พื้นขาวทึบใต้ข้อความ + ไล่เฉดจางไว้ "เหนือ" แผ่น (แบบแรกไล่เฉดทับตัวหนังสือ ทำให้บรรทัดทองจาง อ่านไม่ออก — แก้แล้ว) + เส้นทองบางที่ขอบบนแผ่นให้ดูเหมือนขอบฟิล์ม
  - จอสัมผัสไม่มี hover → เพิ่ม JS สั้น ๆ ให้ "แตะ" สลับ (`.sc-open`) เฉพาะเมื่อ `matchMedia("(hover: none)")` เป็นจริง คลิกด้วยเมาส์จึงไม่ค้าง; เพิ่ม `prefers-reduced-motion` ปิดอนิเมชัน
- ตรวจผ่าน localhost + Chrome: hero ทั้ง 10 หน้าไม่มีกล่องสถิติแล้ว, การ์ดเฉดสไลด์ขึ้น-ลงครบทั้ง 3 ใบ, จอ 430px ยังเป็นคอลัมน์เดียว ข้อความไม่ล้น/ไม่โดนตัด, ทดสอบ tap-toggle ผ่าน
- อัปเดต `CLAUDE.md`: บันทึกคอมโพเนนต์ `.sc-view` + เหตุผลเชิงโครงสร้าง (grid ซ้อนเลเยอร์ / พื้นทึบใต้ข้อความ), บันทึกว่ากล่อง `.phl` ถูกถอดถาวร และแก้จำนวนหน้าที่ล้าสมัย (หน้ารุ่น 8→9, หน้าสเปก 9→10 ตั้งแต่เพิ่ม Crystalize)

## ครั้งที่ 89 — 2026-07-27

**Prompt:**
> เกือบดีแต่ยังไม่ใช่ ความหมายคือ ที่สไลด์ขึ้น คือฟิล์มสีน้ำเงินโปร่งใส่ด้านบนที่จะทำใ้ห้มันสไลด์ขึ้นไม่ใช่ตรงนั้นแก้ไข

**สิ่งที่แก้ไข — สลับตัวที่เลื่อนให้ถูก: "แผ่นฟิล์มสีน้ำเงินด้านบน" เป็นตัวที่สไลด์ขึ้น:**
- **รื้อของเดิม** (ครั้งที่ 88 ทำผิดจุด — เอาแผงรูปสไลด์ขึ้นมาทับตารางสเปกด้านล่าง) เปลี่ยนเป็น: **แผ่นสีน้ำเงิน `.sc-top` เอง** คือ "ฟิล์ม" ที่เลื่อนขึ้น (`translateY(-100%)`) เผยรูปวิวใสที่ซ่อนอยู่ข้างหลัง — **ตารางสเปกด้านล่างอยู่นิ่ง ไม่ขยับ**
- โครงสร้างใหม่: ห่อ `.sc-view` (รูปวิว) + `.sc-top` (แผ่นฟิล์ม) ไว้ใน `.sc-head` ซ้อนช่องเดียวกันแบบ grid, `overflow:hidden` ตัดส่วนที่เลื่อนพ้นการ์ด; `.sc-specs` กลับมาเป็นลูกตรงของ `.shadecard` เหมือนเดิม
- **ทำให้ฟิล์ม "โปร่ง" จริงตามที่สั่ง** — เปลี่ยนพื้นหลัง 3 การ์ดจากสีทึบเป็น `rgba()` ไล่ตามความเข้มของเฉด (CM 35 โปร่งสุด ~.90 → CM 05 ทึบสุด ~.97) ตอนยังไม่ชี้เมาส์จึงเห็นวิวราง ๆ ทะลุฟิล์มแต่ละเฉดไม่เท่ากัน สมจริงตามระดับ VLT
- เพิ่ม `min-height:232px` ให้ `.sc-head` — ตอนฟิล์มเลื่อนขึ้นจะเห็นวิวเป็นแถบใหญ่พอ (เดิมเหลือแค่ริ้วบาง ๆ) และย้ายเส้นทองไปไว้ที่ **ขอบล่างของแผ่นฟิล์ม** (ขอบที่กำลังเลื่อน) โผล่ตอน hover
- ข้อความบรรยายวิว (`.sc-view-in`) ตั้ง `opacity:0` แล้วค่อยจางขึ้นหลังฟิล์มเลื่อน — กันไม่ให้เห็นทะลุแผ่นฟิล์มโปร่งตอนยังไม่ชี้เมาส์
- ตรวจผ่าน localhost + Chrome: ทั้ง 3 การ์ดฟิล์มเลื่อนขึ้น-ลงถูกจุด, ตอนพักเห็นวิวราง ๆ ผ่านฟิล์มตามระดับเฉด, ข้อความอ่านชัดตอนเปิด, จอ 430px คอลัมน์เดียวไม่ล้น, tap-toggle บนจอสัมผัสยังทำงาน
- อัปเดต `CLAUDE.md` หมวดการ์ดเฉดให้ตรงพฤติกรรมใหม่ + จดไว้ว่า "ห้ามทำให้ส่วนสเปกขยับ" กัน instance หน้าทำผิดจุดซ้ำ

## ครั้งที่ 90 — 2026-07-27

**Prompt:**
> เอา 3 เฉดที่ที่เราทำใส่ไปในทุกรุ่นส่วนข้อมูลค่อยว่ากันแต่ใส่ไปก่อน

**สิ่งที่แก้ไข — ยก section "3 เฉด" (การ์ดฟิล์มสไลด์) ไปใส่ทุกหน้ารุ่น:**
- **เพิ่ม section 3 เฉดครบอีก 9 หน้า** (`brand-regionfilm` + 8 หน้า model ที่เหลือ) ยกคอมโพเนนต์เดิมจาก `model-finnix-ceramic.html` มาทั้งชุด — แผ่นฟิล์มน้ำเงินโปร่งสไลด์ขึ้นตอนชี้เมาส์ เผยวิวใส + ข้อความ, ครบทั้ง 3 การ์ดทุกหน้า
- **วางไว้ระหว่าง hero กับตารางสเปก** (`.section` พื้นกระดาษ) เพื่อให้จังหวะสีพื้นยังสลับกระดาษ/เทาอ่อนเหมือนเดิม: hero → 3 เฉด (กระดาษ) → รายละเอียดทางเทคนิค (เทาอ่อน) → ผลงาน (กระดาษ) → ติดต่อ (เทาอ่อน)
- **เพิ่ม JS แตะสลับฟิล์ม** (`.sc-open` เฉพาะจอที่ไม่มี hover) ให้ทั้ง 9 หน้า — CSS ใช้ของเดิมใน `product.css` ไม่ต้องเพิ่ม
- **ข้อมูลเป็นตัวอย่างก่อนตามที่สั่ง** ("ส่วนข้อมูลค่อยว่ากัน"): ตั้งใจไม่แต่งตัวเลขใหม่ให้เยอะ
  - ชื่อเฉดใช้กลาง ๆ ว่า **โทนใส / โทนกลาง / โทนเข้ม** (ไม่ตั้งรหัสรุ่นปลอม เพราะมีแค่ Finnix Ceramic ที่มีรหัสจริง CM 35/15/05)
  - VLT ไล่ 3 ระดับคร่อมค่าเดิมของหน้านั้น เช่น Titanium 40/25/15 · Ultra Guard Nano 35/15/05 · 3M Ultra Clear 70/50/30
  - แถว **ลดความร้อนใช้ตัวเลขเดิมของหน้านั้นทั้ง 3 การ์ด** (เช่น Titanium สูงสุด 78% ทุกใบ) และ UV 99% เท่ากันทั้งเว็บ — จงใจไม่แต่งเส้นโค้งรายเฉด จะได้ไม่เพิ่มตัวเลขใหม่ที่ต้องมาตามแก้ทีหลัง
- ตรวจผ่าน localhost + Chrome ครบทั้ง 9 หน้า: การ์ด 3 ใบ, หัวข้อชื่อรุ่นถูกทุกหน้า, ฟิล์มสไลด์ขึ้น-ลงทำงาน, โครงสร้าง section ไม่พัง (5 section เท่ากันทุกหน้า)
- อัปเดต `CLAUDE.md`: section 3 เฉดอยู่ครบ 10 หน้าแล้ว + ระบุชัดว่าข้อมูลเฉดของ 9 หน้าเป็น placeholder รอเจ้าของ และตำแหน่ง section ในลำดับหน้า

## ครั้งที่ 91 — 2026-07-28

**Prompt:**
> /init — วิเคราะห์โค้ดเบสแล้วสร้าง/ปรับปรุง CLAUDE.md

**สิ่งที่แก้ไข — ตรวจสอบ CLAUDE.md เทียบกับเว็บจริง แล้วแก้จุดที่ไม่ตรง (ไม่แตะไฟล์เว็บเลย):**
- **ตรวจข้อมูลเดิมใน CLAUDE.md เกือบทั้งหมดถูกต้อง** — ยืนยันด้วย grep: `.phl`/`.hi` ถูกลบหมดจริง, section 3 เฉดอยู่ครบ 10 หน้า, `.fitem` 4 ช่องเท่ากันครบ 13 หน้า, เบอร์โทร/lightbox/`data-img` ตรงตามที่เขียนไว้
- **แก้เรื่อง `.reveal` (ข้อมูลเดิมผิด):** CLAUDE.md เขียนว่า IntersectionObserver อยู่ใน JS ของทุกหน้า แต่จริง ๆ **มีแค่ 4 หน้าหลัก** (`index`/`works`/`about`/`contact`) — `product.css` ไม่มีกฎ `.reveal` เลย และหน้า `products/*` ไม่มี observer สักหน้า ทั้งที่หน้า `model-*` ยังมี `class="reveal"` ค้างอยู่หน้าละ 3 จุด (รวม 27 จุด) = markup ตายที่ไม่ทำงาน เพิ่มคำเตือนว่าถ้าจะย้าย CSS `.reveal` เข้า `product.css` ต้องย้าย observer ตามไปด้วย ไม่งั้น 27 จุดนั้นจะหายไปทั้งเว็บ
- **แก้จำนวนหน้า 16 → 17** ในหัวข้อ accordion (ตอนทำปี 2026-07-21 ยังไม่มี `model-finnix-crystalize.html`) และเพิ่ม **บล็อกสรุปจำนวนหน้ามาตรฐาน**: 17 หน้า = 4 หน้าหลัก + 4 brand + 9 model · 10 หน้าสเปก · 14 หน้ามี section ติดต่อ · chrome ร่วมอยู่ครบ 17
- **เพิ่มหัวข้อใหม่ "Verifying a sitewide change"** — เว็บนี้ไม่มี test/linter ตัวตรวจว่าการแก้แบบทั้งเว็บลงครบทุกหน้าจริงคือ counting grep ใส่คำสั่งตัวอย่างพร้อมจำนวนที่ควรได้ไว้ให้
- **บันทึกโฟลเดอร์ `assets/PIC/`** (ไฟล์ `_1.jpg` ที่เจ้าของเพิ่งวางไว้ 2026-07-27) — ยังไม่มีหน้าไหนเรียกใช้ และ **ไม่ได้อยู่ใน .gitignore** จึงโผล่เป็น `?? assets/PIC/` เตือนไม่ให้ `git add -A` เข้าไปเองโดยไม่ถาม + หมายเหตุว่าคอมเมนต์ใน `product.css` ที่เขียนว่า "โฟลเดอร์ PIC ยังว่าง" ตกยุคแล้ว
- อัปเดตเลข CHANGELOG ใน CLAUDE.md เป็น 1,300 บรรทัด / 240 KB (N = 90)

## ครั้งที่ 92 — 2026-07-28

**Prompt:**
> /init (รันซ้ำอีกครั้ง ทันทีหลังครั้งที่ 91)

**สิ่งที่แก้ไข — ไม่มีอะไรเปลี่ยนในเว็บตั้งแต่ครั้งที่ 91 จึงใช้รอบนี้ตรวจ "ข้อที่ยังไม่เคยตรวจ" ใน CLAUDE.md แทนการทำซ้ำ:**
- **ตรวจเพิ่มอีกชุด ผลคือถูกต้องทั้งหมด** — path ของลิงก์ Products (`#brands` 1 · `index.html#brands` 3 · `../index.html#brands` 17 = brand 4 หน้ามี 2 จุด nav+breadcrumb, model 9 หน้ามี 1 จุด), `data-th` ครบ 5 label × 17 หน้า, `class="active"` ครบ 17 หน้า, hero 3 สไลด์ = 3 hpanel = 3 จุดกลม, intro carousel 5 `.fslide` = 5 `.ipanel`, `.submenu::before` = 18px ทั้งสองไฟล์, บล็อก nav micro-interactions อยู่ท้ายสุดจริงทั้งสองไฟล์ (index บรรทัด 1677 ก่อน `</style>` 1829 · product.css 1963–2114), `.bband` เป็น `display:block`, `.shadegrid` = `repeat(3,1fr)`, `.sc-head` min-height 232px
- **ยืนยัน featurebar ตรงกันเป๊ะ** — ทั้ง 4 คู่ label/sublabel ขึ้นมาหน้าละครั้ง รวมคู่ละ **13 ครั้งพอดี** (+ contact.html ที่ใช้ markup ของตัวเอง = 14 หน้า) รวมทั้งคำสะกด `ประเมิณ` ที่จงใจปล่อยไว้
- **ยืนยันของที่ตั้งใจลบไปแล้วยังไม่กลับมา** — `swatch--img`, `scrollcue`, `mailto:`, `intro-banner`/`intro-overlay`, `.bl` chips ไม่เหลือในไฟล์ไหนเลย
- **เจอเพิ่ม 1 จุด: CSS ของ `.sample-note` ยังอยู่ใน `product.css`** (~บรรทัด 989) ทั้งที่ markup ถูกถอดออกหมดแล้วตั้งแต่ 2026-07-18 — เท่ากับกฎยังพร้อมทำงาน ถ้าใครเผลอใส่ `<div class="sample-note">` แถบเหลืองจะกลับมาเงียบ ๆ บันทึกไว้ใน CLAUDE.md คู่กับ `.work-hero .mini` ที่เป็น dead code เหมือนกัน
- **ปรับบล็อก "Verifying a sitewide change"** ให้ใช้ grep ที่ดีกว่าเดิม — เช็คจาก **ข้อความ** แทน class (จับคำที่เพี้ยนได้ ไม่ใช่แค่ว่ามี element อยู่) และเพิ่มเหตุผลว่าทำไมวิธีนี้ทนต่อการที่ editor จัดบรรทัดใหม่

## ครั้งที่ 93 — 2026-07-28

**Prompt:**
> /init (รันครั้งที่สามติดกัน)

**สิ่งที่แก้ไข — รอบนี้เจอ "ของใหม่" ที่รอบก่อนไม่มีทางเห็น เพราะเจ้าของเพิ่งแก้ไฟล์เองหลังครั้งที่ 92 (ไม่แตะไฟล์เว็บเลย แก้เฉพาะ CLAUDE.md):**
- **เจ้าของเริ่มแก้คำว่า `ประเมิณ` → `ประเมิน` เองแล้ว** — ในไฟล์ที่ยังไม่ commit (`M works.html`) มีการแก้ label ในแถบ 4 ช่องของหน้าผลงานเป็น **`ประเมินพื้นที่หน้างานฟรี`** ทำให้ตอนนี้เว็บ **แยกเป็น 13 หน้าเขียนผิด / 1 หน้าเขียนถูก** ซึ่งชนกับกฎเดิมใน CLAUDE.md ที่เขียนว่า "รอเจ้าของสั่ง อย่าแก้หน้าเดียว" พอดี — แก้ข้อนั้นใหม่ให้ถือว่าเป็นสัญญาณจากเจ้าของ แต่ให้ยืนยันก่อนกวาด และถ้ากวาดต้องปิดให้ครบ 13 หน้าที่เหลือในรอบเดียว (`index` · `about` · `contact` · `brand-regionfilm` · `model-*` 9 หน้า) พร้อมเตือนว่า `README.md` ก็บันทึกคำถามเรื่องคำนี้ไว้ ต้องแก้ตามในรอบเดียวกัน
- **เจอจุดบอดของ grep ตรวจ featurebar ที่ใช้มา 2 รอบ** — pattern `><b>` **จับ `contact.html` ไม่ได้** เพราะ markup `.vprop`/`.m` ของหน้านั้นขึ้นบรรทัดใหม่ก่อน `<b>` (นี่คือสาเหตุจริงที่ตัวเลขได้ 13 ไม่ใช่ 14) ถ้าเป็นการกวาด **คำ** ต้องตามด้วย grep ข้อความล้วนอีกที ไม่งั้นหน้าที่ 14 จะถูกข้ามเงียบ ๆ ใส่คำสั่ง + จำนวนที่ควรได้ไว้ใน CLAUDE.md แล้ว
- **เตือนกับดักตอนนับคำที่แก้ถูกแล้ว** — `contact.html` มีคำว่า `ประเมินหน้างานฟรี` ในประโยคปกติ (~บรรทัด 455) อยู่ก่อนแล้วคนละจุดกับแถบ 4 ช่อง เพราะฉะนั้นเวลานับ "หน้าที่แก้แล้ว" ต้องนับจาก **label เต็ม** ไม่ใช่คำโดด ๆ ไม่งั้นตัวเลขเกินไป 1
- ตรวจซ้ำของเดิมอีกรอบ ยังตรงหมด: 17 หน้า · `getComputedStyle(burger)` 17 · `data-th` 5 label × 17 · เบอร์โทร 5/หน้า (6 contact, 4 brand สั้น) · `.reveal` มี observer แค่ 4 หน้าหลัก + markup ตายหน้า model ละ 3 จุด · `.shadegrid`/`.sc-open` ครบ 10 หน้า · `.bband` ครบ 4 หน้า brand · hero 3 สไลด์ = 3 hpanel = 3 ปุ่มจุด · intro 5 `.fslide` = 5 `.ipanel` · `assets/PIC/` ยังมีแค่ `_1.jpg` และยังไม่มีหน้าไหนเรียกใช้
- อัปเดตเลข CHANGELOG ใน CLAUDE.md เป็น N = 92 (จากเดิมเขียน 90)

## ครั้งที่ 94 — 2026-07-28

**Prompt:**
> ประโยค ประเมิณหน้างานฟรีแก้ไข เป็นประเมินหน้างานฟรี ทั้งหมด ในหมวดหมู่ Project Reference ตรงคำอธิบาย ผลงานการติดตั้ง OUR WORK ขอ 1000+ ปรับเป็น font ใหญ่ สีทองที่เราเคยใส่ก่อนหน้านี้ แล้วเอามาไว้ตรงกลางเอาสีเดี๋ยวกันกับ คำว่า ผลงานการติดตั้ง

**สิ่งที่แก้ไข:**
- **แก้คำสะกด `ประเมิณ` → `ประเมิน` ครบทั้ง 14 หน้าที่มีแถบ 4 ช่อง** (เจ้าของแก้หน้าผลงานไว้เองก่อนแล้ว 1 หน้า จึงกวาดที่เหลืออีก 13 หน้า: `index` · `about` · `contact` · `brand-regionfilm` · `model-*` ทั้ง 9 หน้า) ตอนนี้ `grep 'ประเมิณ'` ไม่เจอในไฟล์ไหนอีกเลย และคู่ label/sublabel ทั้ง 4 คู่กลับมาเท่ากันที่ **13 ครั้งเท่ากันหมด** (+ `contact.html` ที่ใช้ markup `.vprop` ของตัวเอง = 14 หน้า)
  - ใช้ `sed -i` ผ่าน Bash ซึ่งจัดการ UTF-8 ภาษาไทยได้สะอาด (ข้อควรระวังเรื่อง "แก้ข้อความไทยแล้วเพี้ยน" ใน CLAUDE.md เป็นเรื่องของ PowerShell ไม่ใช่ sed) ตรวจหน้าเว็บจริงในเบราว์เซอร์แล้วตัวอักษรไม่เพี้ยน ไอคอนไม้บรรทัดยังตรงกับ label เหมือนเดิม
- **หน้า Project Reference (works.html) — ทำเลข 1000+ ให้เด่นตามที่ขอ:** ดึง `1000+` ออกจากประโยคใน `<p class="th">` มาเป็นบล็อกของตัวเองใต้คำอธิบาย ตัวใหญ่ (2.4rem) **สีทอง `#e7c879`** และ**จัดกึ่งกลาง** โดยมีคำว่า `โครงการที่ส่งมอบ` เป็นคำกำกับตัวเล็กอยู่ใต้เลข
  - สีทองนี้คือ **เฉดเดียวกันเป๊ะกับคำว่า "ผลงานการติดตั้ง"** (`.work-hero .eyebrow` = `#e7c879`) ตามที่สั่ง
  - **ไม่ได้เขียน CSS ใหม่เลย** — ใช้กฎ `.work-hero .mini` เดิมที่ยังค้างอยู่ในไฟล์ตั้งแต่ตอนเจ้าของถอดแถบสถิติ 3 ช่อง (200+/40+/10 ปี) ออกไป ซึ่งเป็นสีทองและขนาดที่ "เคยใส่ก่อนหน้านี้" พอดี รวมถึงกฎย่อขนาดในจอ ≤768/≤480 ที่ยังอยู่ครบ
- อัปเดต `README.md` ย้ายเรื่องคำสะกดออกจากหัวข้อ "รอเจ้าของยืนยัน" ไปเป็นหัวข้อ "เรื่องที่ปิดแล้ว" + เปลี่ยนวันที่ท้ายไฟล์เป็น 2026-07-28
- อัปเดต `CLAUDE.md` ทั้ง 4 จุดที่เกี่ยวข้อง: label มาตรฐานเป็น `ประเมิน`, เปลี่ยนกฎ "อย่าแก้หน้าเดียว รอเจ้าของ" เป็น "ปิดแล้ว", บันทึกว่า `.work-hero .mini` กลับมาใช้งานจริงแล้ว (ไม่ใช่ dead code อีกต่อไป) และบันทึกว่า sed ผ่าน Bash ใช้แก้ข้อความไทยได้

## ครั้งที่ 95 — 2026-07-28

**Prompt:**
> ในหน้าหมวดหมู่ Products ในรุ่น finnix titanium ตรงแถวคอลัมน์ สเปกฟิล์มรายละเอียดทางเทคนิค Finnix Titanium เอาแถวคอลัมน์นั้นออกรวมถึง แบรนด์อื่นรุ่นอื่นด้วยถ้ามีแถวนี้คอลัมน์นี้ และรูป logo ในแบรนด์ Ultra guard ซึ่งในแต่ล่ะรุ่นที่ยังๆม่มี logo ใส่ logo ให้หน่อยซึ่งอยู่ในไฟล์ ที่ชื่อmodels และเช็คข้อมูลในเว็บไซต์และโค้ดว่าถูกต้องไหมมีอะไรที่ต้องกังวลไหมตรวจเช็คที่

**สิ่งที่แก้ไข:**
- **ถอด section ตารางสเปก "รายละเอียดทางเทคนิค" ออกครบทุกหน้า (9 หน้าที่มี)** — `model-finnix-titanium` · `model-finnix-crystalize` · `model-finnix-extra-clear` · `model-finnix-uvguard` · `model-3m-ceramate` · `model-3m-ultra-clear` · `model-ultraguard-ceramic` · `model-ultraguard-nano` · `brand-regionfilm` (ส่วน `model-finnix-ceramic` ไม่เคยมีตารางนี้ตั้งแต่แรก จึงครบ 10 หน้าสเปกเท่ากันแล้ว) ตารางที่ถอดออกมี 8 แถว: ซีรีส์ / โทนสี / VLT / TSER / UV / การสะท้อนแสง / เหมาะสำหรับ / การรับประกัน
- **แก้จังหวะสีพื้นหลังที่เสียไปจากการถอด** — ตารางสเปกเป็นพื้นสีอ่อน (`.section--tint`) พอถอดออกแล้วทำให้ section เฉดฟิล์มกับ section ผลงานเป็นพื้นกระดาษติดกัน 2 อัน จึงเปลี่ยน section เฉดฟิล์มของ 9 หน้าเป็น `.section--tint` ตอนนี้ทุกหน้าสเปกเรียงเหมือนกันหมด: เฉดฟิล์ม(อ่อน) → ผลงาน(กระดาษ) → ติดต่อ(อ่อน) ซึ่งตรงกับที่ `model-finnix-ceramic` เป็นอยู่แล้ว
- **CSS `.spectable` ใน `product.css` เก็บไว้ ไม่ได้ลบ** — ตัวเลขในตารางเป็นข้อมูลตัวอย่างมาตลอด ถ้าเจ้าของส่งสเปกจริงมาเมื่อไหร่ ตารางนี้คือสิ่งที่น่าจะกลับมา จึงปล่อยเป็น dead code ไว้ (บันทึกใน CLAUDE.md แล้ว)
- **แก้คำ eyebrow ของ section เฉดฟิล์มใน `model-finnix-ceramic` จาก "สเปกฟิล์มในรุ่น" เป็น "เฉดฟิล์มให้เลือก"** ให้ตรงกับอีก 9 หน้า (หน้านี้ได้ section นี้เป็นหน้าแรก เลยยังใช้คำเก่าค้างอยู่)
- **โลโก้ Ultra Guard — ยังไม่ได้ใส่ รอเจ้าของยืนยันก่อน** เพราะชื่อโลโก้จริงไม่ตรงกับชื่อรุ่นในเว็บ (ดูหัวข้อ "เรื่องที่ต้องกังวล" ด้านล่าง)

**ผลการตรวจเช็คเว็บไซต์และโค้ด:**
- ✅ **โครงสร้าง HTML ครบถ้วน** — แท็ก `<section>`/`<div>` เปิด-ปิดตรงกันทั้ง 17 หน้า และหน้าสเปกทั้ง 10 หน้ามีจำนวน div เท่ากันเป๊ะ (103) = ตัดออกได้สะอาดเหมือนกันหมด
- ✅ **ลิงก์ภายในไม่พัง** ทุกลิงก์ชี้ไปยังไฟล์ที่มีอยู่จริง · **รูปภาพในเครื่องครบ** ไม่มี src ที่หาไฟล์ไม่เจอ · ไม่มีลิงก์ `#` ค้างที่ชี้ไปยัง section ที่ถอดออก · ไม่มี id ซ้ำในหน้าเดียวกัน · ทุก `<img>` มี alt
- ✅ **ไม่มี error ใน console** ตอนเปิดหน้าจริง สคริปต์แกลเลอรี/lightbox/สไลด์ฟิล์มยังทำงานปกติ
- ✅ **เรื่องรับประกันหายขัดกันเองแล้ว** — ตัวเลข 5/6/7/8 ปี รายรุ่นอยู่ในตารางสเปกที่ถอดออกไป ตอนนี้ทั้งเว็บเหลือ "รับประกันสูงสุด 10 ปี" ค่าเดียว
- ⚠️ **ตัวเลขลดความร้อนยังขัดกันอยู่เหมือนเดิม** (ไม่เกี่ยวกับการถอดตาราง เพราะอยู่คนละที่) — หน้าแรกเขียน **85%** · หน้าแบรนด์ Finnix เขียน **88%** · หน้ารุ่น Finnix Ceramic เขียน **82%** ทั้งสามจุดอยู่ใน meta description/คำโปรย รอเจ้าของยืนยันตัวเลขจริง
- ⚠️ **ชื่อรุ่น Ultra Guard ในเว็บไม่ตรงกับสินค้าจริง** — โลโก้ที่เจ้าของวางมาคือ **Nanoceramic** กับ **Super Clear** แต่ในเว็บตั้งชื่อรุ่นไว้ว่า **Ceramic** กับ **Nano** ซึ่งดูเหมือนถูกแตกมาจากคำว่า "Nanoceramic" คำเดียว และ "Super Clear" (ฟิล์มใส) ไม่ตรงกับหน้าไหนเลยเพราะทั้งสองหน้าเป็นโทนเข้ม (VLT 30% / 15%)
- ℹ️ ข้อมูลเฉดฟิล์มใน 9 หน้ายังเป็นตัวอย่าง (โทนใส/โทนกลาง/โทนเข้ม) ตามที่เจ้าของรับทราบไว้แล้ว · โฟลเดอร์ `assets/PIC/` ยังมีแค่ `_1.jpg` และยังไม่มีหน้าไหนเรียกใช้

## ครั้งที่ 96 — 2026-07-28

**Prompt:**
> เอาแผน A แล้วจัดการเลย

**สิ่งที่แก้ไข — เปลี่ยนชื่อรุ่น Ultra Guard ให้ตรงสินค้าจริง แล้วใส่โลโก้ครบทั้ง 2 รุ่น:**
- **เปลี่ยนชื่อไฟล์หน้ารุ่นทั้งสอง** (ใช้ `git mv` เพื่อให้ประวัติไฟล์ไม่ขาด)
  - `model-ultraguard-nano.html` → **`model-ultraguard-nanoceramic.html`** = **Ultra Guard Nanoceramic** — สเปกเดิมทั้งหมด (VLT 15% · ลดความร้อน 82% · เฉด 35/15/05 · โทนเข้ม) เก็บไว้เหมือนเดิม เปลี่ยนแค่ชื่อ
  - `model-ultraguard-ceramic.html` → **`model-ultraguard-super-clear.html`** = **Ultra Guard Super Clear** — หน้านี้เปลี่ยนจากฟิล์มโทนเข้มเป็น**ฟิล์มใส** จึงแก้ทั้งชุด: swatch `tone-dark`→`tone-clear` · VLT 30%→**70%** · ลดความร้อน 78%→**60%** · เฉด 50/30/15→**70/50/30** · เขียนคำโปรย/คำอธิบายใหม่ให้เป็นฟิล์มใส
  - ตัวเลขชุดใหม่ของ Super Clear **อิงจากรุ่นใสที่มีอยู่แล้วในเว็บ** (Finnix Extra Clear และ 3M Ceramic Ultra Clear ใช้ VLT 70% / ลดความร้อน 60% เหมือนกัน) **ยังเป็นข้อมูลตัวอย่าง ไม่ใช่สเปกจริง**
- **ใส่โลโก้ในการ์ดเลือกรุ่นหน้า Ultra Guard ครบทั้ง 2 ใบ** — เปลี่ยนการ์ดจากแบบข้อความเป็นแบบโลโก้ (`.cmc--logo`) เหมือนหน้า Finnix/3M ตอนนี้ **ทุกรุ่นในเว็บมีโลโก้ครบหมดแล้ว** (Finnix 5 · 3M 2 · Ultra Guard 2)
- **จัดไฟล์โลโก้ตามระเบียบเดิมของโปรเจกต์** — ย่อเป็น **1200×848** เท่ากับโลโก้รุ่นอื่นทุกใบ (จากต้นฉบับ 3508×2480) และเปลี่ยนชื่อเป็น ASCII ไม่มีเว้นวรรค: `ultraguard-nanoceramic.png` (759KB→308KB) · `ultraguard-super-clear.png` (444KB→161KB) ส่วนไฟล์ต้นฉบับที่เจ้าของวางไว้ย้ายไปเก็บใน `assets/source/` ซึ่งเป็นที่เก็บไฟล์ต้นฉบับ (ไม่ขึ้น GitHub) — ไม่ได้ลบทิ้ง
- **แก้คำอธิบายใต้รูปผลงานที่อ้างชื่อรุ่นเก่า** — การ์ดผลงานเขียนว่า "ฟิล์มที่ใช้ · Ultra Guard Ceramic" ซึ่งเป็นชื่อที่ไม่มีอยู่แล้ว กวาดเปลี่ยนเป็น **Ultra Guard Nanoceramic** ครบทั้ง **11 หน้า** (works.html + หน้าสเปก 10 หน้า) ให้เหมือนกันหมดตามกติกาบล็อกที่ใช้ร่วมกัน
- ตรวจหลังแก้: ไม่มีลิงก์เสียหรือชื่อรุ่นเก่าค้างในไฟล์ไหนเลย · แท็ก HTML ครบ · หน้า `brand-ultraguard.html` มีจำนวน div เท่ากับ `brand-3m.html` เป๊ะ (19) ซึ่งเป็นหน้าแบรนด์ 2 รุ่นแบบการ์ดโลโก้เหมือนกัน · เปิดดูหน้าจริงแล้วโลโก้ทั้งสองขึ้นถูกใบ และหน้า Super Clear แสดง VLT 70% พร้อม swatch โทนใส
- อัปเดต `README.md` (ผังไฟล์ · ผังการเชื่อมหน้า · รายการโลโก้ · ย้ายเรื่องชื่อรุ่นไปหัวข้อ "ปิดแล้ว" + เพิ่มหมายเหตุว่าตัวเลข Super Clear เป็นค่าตัวอย่างที่ตั้งใหม่) และ `CLAUDE.md` (บันทึกเหตุผลที่เปลี่ยนชื่อ ไม่ใช่การเดาจับคู่โลโก้ + รายละเอียดที่เปลี่ยนทุกจุด)

## ครั้งที่ 97 — 2026-07-28

**Prompt:**
> ภาพพื้นหลังในหน้า home ที่เป็นภาพสไลด์ 3 ภาพ นำเอาภาพในไฟล์ที่ชื่อ PIC มาเปลี่ยนใส่แทนภาพเก่าตามคำอธิบายจัดการเลย

**สิ่งที่แก้ไข:**
- **เปลี่ยนภาพพื้นหลังสไลด์หน้าแรกเป็นภาพจริงของร้าน 2 ใบ** โดยจับคู่ตามคำอธิบายของแต่ละสไลด์
  - `_1.png` (ครอบครัว 3 รุ่นในบ้านกระจก มีหมากับแมว) → **สไลด์ที่ 2** "บ้านคือพื้นที่แห่งความสุข" ซึ่งมีคำว่า *"ไม่ว่าจะเดิน 2 ขา หรือ 4 ขา"* พอดี
  - `_2.png` (ทีมช่างใส่เสื้อ Finnix กำลังติดฟิล์มบนหลังคากระจก) → **สไลด์ที่ 3** "ทีมช่างมืออาชีพ"
- **สไลด์ที่ 1 ยังไม่ได้เปลี่ยน** — เป็นภาพบ้านมองจากภายนอก ซึ่งในโฟลเดอร์ PIC ไม่มีภาพแนวนี้ (มีมาแค่ 2 ใบ) จึงคงภาพสต็อกเดิมไว้ก่อน รอเจ้าของส่งภาพบ้าน/อาคารถ่ายจากภายนอกมาเพิ่ม
- **แปลงภาพให้พร้อมใช้บนเว็บก่อนใส่ ไม่ได้ลิงก์ไฟล์ต้นฉบับตรง ๆ** เพราะต้นฉบับเป็น PNG ขนาดใหญ่มาก (รวม 5.6 MB) ถ้าใส่ตรง ๆ หน้าแรกจะโหลดช้าผิดปกติ
  - **ตัดขอบขาวอัตโนมัติ** — `_2.png` มีแถบขาวคาดบนล่างอยู่ราว 156 พิกเซล ถ้าไม่ตัดจะเห็นเป็นแถบขาวพาดกลางจอ
  - ย่อกว้างสุด 1920px + แปลงเป็น JPEG คุณภาพ 86 → เหลือรวม **580 KB (เล็กลงเกือบ 10 เท่า)**
  - เก็บไว้ที่ `assets/hero/hero-family-pets.jpg` และ `assets/hero/hero-install-team.jpg` ส่วนไฟล์ต้นฉบับใน `assets/PIC/` ยังอยู่ครบ ไม่ได้ลบหรือย้าย
- ตรวจแล้ว: เปิดหน้าจริงทั้ง 3 สไลด์ ภาพขึ้นถูกใบ ตรงกับข้อความ ไม่มีแถบขาว ไม่มี error ใน console และปุ่มจุดเลื่อนสไลด์ยังทำงานปกติ
- อัปเดต `README.md` (เพิ่มโฟลเดอร์ `assets/hero/` ในผังไฟล์ + วิธีเปลี่ยนภาพสไลด์เองในอนาคต + บันทึกว่าสไลด์ 1 ยังรอภาพจริง) และ `CLAUDE.md` (บันทึกว่าสไลด์ไหนใช้ภาพจริงแล้ว + ขั้นตอนแปลงภาพจาก PIC ให้ทำซ้ำได้)

## ครั้งที่ 98 — 2026-07-30

**Prompt:**
> /init — Please analyze this codebase and create a CLAUDE.md file... If there's already a CLAUDE.md, suggest improvements to it.

**สิ่งที่แก้ไข — /init รอบสี่ (ตรวจ CLAUDE.md ทั้งไฟล์เทียบกับเว็บจริง แล้วอัปเดตเฉพาะจุดที่คลาดเคลื่อน):**
- **ตรวจข้อที่ตรวจวัดได้ทุกข้อในคู่มือ ผลออกมาตรงหมด ไม่มีอะไรเพี้ยน** — หน้าเว็บ 17 หน้า (4 หน้าหลัก + 4 แบรนด์ + 9 รุ่น) · แถบ 4 ช่องตรงกันเป๊ะ 13 ชุด + `contact.html` อีก 1 = 14 หน้า · สคริปต์เมนูมือถือครบ 17 หน้า · ป้ายเมนู 2 ภาษา 5 ป้าย × 17 หน้า · เบอร์โทรต่อหน้า 5 จุด (contact 6 · แบรนด์สั้น 4) · แกลเลอรีการ์ดผลงาน works.html 30 สไลด์ / หน้าสเปกละ 15 · ไม่มีคำสะกด `ประเมิณ` ค้างที่ไหนอีก
- **เจอเรื่องใหม่ที่คู่มือยังไม่ได้บันทึก: รูปภาพในเว็บเกือบทั้งหมดเป็นภาพสต็อกจากเน็ต** — ในเครื่องมีแต่โลโก้กับภาพสไลด์หน้าแรก 2 ใบ ที่เหลือดึงจาก Unsplash **เป็นภาพ 9 ใบใช้วนซ้ำราว 194 จุด** (works.html 31 · index.html 11 · about.html 2 · หน้าสเปกละ 15) จดไว้ในคู่มือ 3 ข้อ: ถ้าเปิดดูตอนเน็ตไม่ต่อจะเห็นพื้นกรมท่าเปล่า (ไม่ใช่เว็บพัง อย่าไป "แก้") · เว็บพึ่งพาบริการนอกอยู่ · และถ้าจะเปลี่ยนเป็นภาพจริง **ต้องกวาดทีละภาพ ไม่ใช่ทีละหน้า** เพราะภาพซ้ำกัน เปลี่ยนใบเดียวได้หลายจุดพร้อมกัน
- **เจอไฟล์โลโก้ที่ไม่มีหน้าไหนเรียกใช้แล้ว** — `assets/logo-dark.png` ไม่ถูกอ้างถึงในไฟล์ HTML เลย เพราะทั้งแถบเมนูแคปซูลและฟุตเตอร์เป็นพื้นกรมท่า จึงใช้ `logo-white.png` ทั้งคู่ (34 จุด = 17 หน้า × 2) ไฟล์นี้กลายเป็นไฟล์สำรองตั้งแต่เปลี่ยนมาใช้เฮดเดอร์แคปซูล **ไม่ได้ลบทิ้ง** แค่แก้คำอธิบายใน `README.md` ที่ยังเขียนว่า "ใช้บนเฮดเดอร์พื้นอ่อน" ให้ตรงความจริง และแก้หัวข้อ "เปลี่ยนโลโก้" ให้ระบุว่าไฟล์ที่ใช้จริงคือ `logo-white.png`
- **แก้ข้อความในคู่มือที่ขัดกันเองหลังถอดตารางสเปกไปเมื่อ 2026-07-28** — ข้อเรื่องถอดกล่องสถิติยังเขียนว่าตัวเลขชุดเดิม "ยังอยู่ในตารางสเปก" ซึ่งตารางนั้นถูกถอดออกไปแล้ว แก้เป็นอยู่ในป้าย VLT กับการ์ดเฉดฟิล์ม + จดเพิ่มว่า CSS `.phl`/`.hi` ถูกลบแล้วจริง เหลือแต่คอมเมนต์ที่ `product.css:434` (จึงเป็นเหตุผลที่ค้นหา `.phl` ยังเจอ 1 จุด)
- **รวมรายการ CSS ที่ตายแล้วแต่เก็บไว้ ให้อยู่จุดเดียว** — เดิมกระจายอยู่ 4 ข้อทั่วไฟล์ ตอนนี้สรุปไว้ในหัวข้อ CSS: `.sample-note` (แถบเหลืองแจ้งข้อมูลตัวอย่าง) กับ `.spectable` (ตารางสเปก) ยังมี CSS ครบทั้งชุด **แปะ markup กลับไปคลาส 2 ตัวนี้เท่ากับปลุกทั้ง component ขึ้นมาทันที** ส่วน `.swatch--img` กับ `.phl`/`.hi` ลบถาวรแล้ว และเตือนไว้ว่า `.work-hero .mini` เคยตายแล้วกลับมาใช้จริงอีก (2026-07-28) จึงอย่าเพิ่งลบ CSS ที่ไม่มีคนใช้ทิ้ง
- **เพิ่มคำสั่งตรวจงานอีก 2 บรรทัด** ในหัวข้อวิธีตรวจการแก้ทั้งเว็บ (นับสไลด์แกลเลอรี + นับโลโก้) พร้อมประทับไว้ว่า **ตรวจครบทุกข้อแล้วเมื่อ 2026-07-30 ตรงหมด** เพื่อให้รอบหน้าไม่ต้องไล่ตรวจซ้ำทั้งตาราง ถ้าเลขไม่ตรงให้ถือว่านั่นคือสิ่งที่เพี้ยน
- อัปเดตเลขอ้างอิงของ `CHANGELOG.md` ในคู่มือ (เดิมจดว่า N=92 ที่ 1,300 บรรทัด → ตอนนี้ N=97 ที่ 1,407 บรรทัด / 273 KB) และเพิ่มหมายเหตุเรื่องภาพสต็อกลง `README.md` ให้เจ้าของร้านเห็นด้วยว่ายังรอภาพผลงานจริงอยู่
- **ไม่ได้แก้หน้าเว็บเลยในรอบนี้** — เป็นการอัปเดตเอกสาร (`CLAUDE.md` · `README.md` · `CHANGELOG.md`) เท่านั้น
- **เจอตอนจะ push: เจ้าของร้านเปิดระบบเผยแพร่เว็บอัตโนมัติไว้เองแล้ว (GitHub Pages)** — มีคอมมิต `5b7cf34` เพิ่มไฟล์ `.github/workflows/static.yml` จากหน้าเว็บ GitHub เมื่อ 2026-07-28 (เทมเพลตมาตรฐานของ GitHub ไม่ได้เขียนขึ้นที่นี่) **ผลคือทุกครั้งที่ push ขึ้น main เว็บจะถูกอัปขึ้นออนไลน์ทันที** จดเรื่องนี้ลงคู่มือพร้อมข้อควรระวัง 3 ข้อ: การ push กลายเป็นการ "เผยแพร่จริง" ไม่ใช่แค่สำรองไฟล์แล้ว · ไฟล์ทุกไฟล์ในโปรเจกต์ถูกอัปขึ้นด้วย (`path: '.'`) รวม README/CHANGELOG · และเครื่องนี้ไม่มีคำสั่ง `gh` จึงเช็คสถานะการ deploy จากเครื่องไม่ได้ ต้องดูในเบราว์เซอร์ (ที่อยู่เว็บที่คาดว่าใช้คือ `https://kuyla2555.github.io/ProjectWorldFilm/` — **ยังไม่ได้ยืนยัน** และถ้าเปิดไม่ขึ้นเป็น 404 ให้เช็คก่อนว่า GitHub Pages บนโปรเจกต์แบบส่วนตัวต้องใช้แพ็กเกจแบบจ่ายเงิน)
- **จดวิธีรับมือเวลา push ไม่ผ่านเพราะเจ้าของแก้จากหน้าเว็บ GitHub** — รอบนี้ push ถูกปฏิเสธจริงเพราะคอมมิตข้างบน แก้ด้วยการ `git fetch` แล้ว `git rebase origin/main` (ไม่ใช้ force) ไม่มีไฟล์ทับกัน

## ครั้งที่ 99 — 2026-07-30

**Prompt:**
> ช่วยตัดระบบตรงนี้ออกคือระบบแชทสีเขียวและระบบโทรที่โชว์ทุกหน้าเอาออกเลย

**สิ่งที่แก้ไข — ถอดปุ่มลอย LINE/โทร (FAB) ออกทั้งเว็บ:**
- **สิ่งที่ถอดออก** = กลุ่มปุ่มวงกลม 2 ปุ่มที่ลอยติดมุมขวาล่างจอตลอดเวลาทุกหน้า — ปุ่ม **LINE สีเขียว `#06c755`** กับ **ปุ่มโทรสีทอง** (`.fab` / `position: fixed`) มีอยู่ครบทั้ง **17 หน้า** ถอดออกหมดแล้ว
- **ถอดทั้ง markup และ CSS ไม่ได้เหลือทิ้งไว้เป็นโค้ดตายค้างในไฟล์** — โค้ดส่วนนี้เหมือนกันเป๊ะทุกหน้า (คอมเมนต์ `FLOATING CTA` + บล็อก `<div class="fab">` 16 บรรทัด) จึงกวาดออกทีเดียวด้วยสคริปต์เดียว และลบ CSS อีก 6 กฎหลัก + 3 กฎจอมือถือ **ในสไตล์ชีตทั้งสองไฟล์** (`index.html` ที่ฝังในหน้า + `products/product.css`) เหลือไว้แต่คอมเมนต์บอกว่าถอดไปแล้วเมื่อไหร่ (ถ้าจะเอากลับ กู้จาก git คอมมิต `5135a4c` ได้)
- **ของที่หน้าตาคล้ายกันแต่ "ไม่ได้" แตะ** เพราะไม่ใช่ปุ่มที่โชว์ทุกหน้า: **การ์ด LINE / CALL / FACEBOOK ในภาพใหญ่หน้าแรก** ยังอยู่ครบ และ **การ์ดติดต่อในส่วน "ติดต่อเรา" ของแต่ละหน้า** ก็ยังอยู่ครบเหมือนเดิม
- **ตรวจหลังแก้:** ทุกหน้าลดลิงก์ `tel:` ไปหน้าละ 1 จุดเท่ากันหมดทั้ง 17 หน้า (ยืนยันว่าถอดครบไม่มีหน้าไหนตกหล่น) · ไม่เหลือคำว่า `.fab` หรือ `class="fab"` ในไฟล์ไหนเลย · วงเล็บปีกกาใน CSS ครบคู่ทั้งสองไฟล์ (345/345 และ 366/366) · แท็ก `<div>` เปิด-ปิดครบทุกหน้าไม่มีเพี้ยน · เปิดดูหน้าจริง 4 หน้า (หน้าแรก · หน้ารุ่น Finnix Ceramic · หน้าแบรนด์ Finnix · หน้าติดต่อ) ปุ่มลอยหายไปแล้วจริง เลื่อนหน้าจอแล้วก็ไม่โผล่มา ส่วนอื่นของหน้าไม่กระทบ ไม่มี error ใน console
- ⚠️ **ผลข้างเคียงที่ต้องแจ้ง: มี 3 หน้าที่ตอนนี้ไม่มีปุ่ม LINE ให้กดเลย** — หน้าแบรนด์ **Finnix · 3M · Ultra Guard** เพราะ 3 หน้านี้เป็นหน้าเลือกรุ่นสั้น ๆ ที่ถอด section ติดต่อออกไปแล้วเมื่อ 2026-07-22 ปุ่มลอยจึงเป็น LINE จุดเดียวที่เหลือ **เบอร์โทรยังกดได้ปกติ** (ปุ่มทองบนแถบเมนู) และ footer ยังโชว์คำว่า `LINE @WORLDCENTER` อยู่ แต่เป็นข้อความเปล่ากดไม่ได้ — **ยังไม่ได้แก้ รอเจ้าของร้านสั่ง** ว่าจะทำลิงก์ LINE ใน footer ให้กดได้ หรือเอา section ติดต่อกลับมาใส่ 3 หน้านี้
- อัปเดต `CLAUDE.md` (ลบ FAB ออกจากรายการโค้ดที่ใช้ร่วมกันทุกหน้า · บันทึกว่าถอดทั้ง markup+CSS พร้อมเหตุผลที่ไม่เก็บเป็น dead code · เตือนไม่ให้สับสนกับการ์ดติดต่อในเฮโร่/ส่วนติดต่อ · จดจำนวนลิงก์ `tel:`/LINE ต่อหน้าชุดใหม่ไว้ใช้ตรวจงานครั้งหน้า) และ `README.md` (แจ้งว่าปุ่มลอยถูกถอดแล้ว + เรื่อง 3 หน้าที่ LINE กดไม่ได้)

## ครั้งที่ 100 — 2026-07-30

**Prompt:**
> ในหมวดหมู่หน้า home สไลค์ภาพ 3 สไลค์ปรับให้อยู่นานขึ้นประมาณ10วิ แล้วค่อยเปลี่ยนภาพ และในประโยค เลือกปกป้องรังสี UV เพิ่มความสบาย มอบทุกช่วงเวลาที่มีความหมายเพราะทุกคนในบ้านสำคัญ ไม่ว่าจะเดิน 2 ขา หรือ 4 ขาให้ทุกวันในบ้านน่าอยู่ยิ่งขึ้น ปรับประโยคให้อยู่ในแค่ 3 แถวปรับให้สมดุล

**สิ่งที่แก้ไข:**
- **สไลด์ภาพหน้าแรกค้างนานขึ้นจาก 6 วิ เป็น 10 วิ** ก่อนเปลี่ยนภาพ (แก้ตัวเลขจังหวะในสคริปต์สไลด์ของหน้าแรก) จังหวะเฟดภาพและปุ่มจุดกดเลือกสไลด์ยังทำงานเหมือนเดิม กดจุดแล้วจับเวลา 10 วิใหม่ตามปกติ
- **จัดประโยคในสไลด์ที่ 2 ให้เหลือ 3 บรรทัดและยาวใกล้เคียงกัน** — เดิมตกเป็น 4 บรรทัด และบรรทัดสุดท้ายเหลือคำว่า "ขึ้น" ห้อยอยู่คำเดียว **ไม่ได้แก้คำในประโยคเลย** ใช้วิธีขยายกรอบข้อความ (`max-width` 34ch → 47ch) แล้วสั่งให้เบราว์เซอร์เกลี่ยความยาวแต่ละบรรทัดให้เท่า ๆ กัน (`text-wrap: balance`) ผลคือ 3 บรรทัดที่ยาวใกล้กันแทนที่จะเป็นบรรทัดยาว 2 บรรทัดแล้วห้อยท้ายสั้น ๆ
- **ทำไมไม่ใช้วิธีใส่ตัวขึ้นบรรทัดตายตัว** (`<br>`) — ถ้าจะให้ 3 บรรทัดยาวเท่ากันจริงต้องตัดกลางวลี อ่านแล้วสะดุด และบนมือถือจอแคบจะยิ่งเพี้ยนเพราะแต่ละท่อนจะไปตัดบรรทัดซ้อนอีก วิธีที่ใช้จึงเป็นการให้เบราว์เซอร์เกลี่ยเอง ปรับตามความกว้างจอได้เอง
- สไลด์ 1 และ 3 มีประโยคสั้น กรอบที่กว้างขึ้นจึงไม่กระทบ 2 สไลด์นั้น
- ⚠️ **ยังไม่ได้เปิดดูหน้าจริงยืนยันด้วยตา** — ส่วนขยายเบราว์เซอร์ของ Claude หลุดการเชื่อมต่อระหว่างทำงาน (ลอง 3 ครั้งไม่ติด) จำนวน 3 บรรทัดคำนวณจากภาพหน้าจอที่วัดไว้ก่อนหน้า (ความยาวข้อความรวมราว 1,126px ที่ขนาดฟอนต์จอใหญ่ 23.2px → ถ้าจะลงตัว 2 บรรทัดต้องกว้างบรรทัดละ 563px ซึ่งกรอบ 47ch ≈ 521px ทำไม่ได้ จึงลงตัวที่ 3 บรรทัดพอดี) **ถ้าเปิดดูแล้วไม่ใช่ 3 บรรทัดพอดี บอกได้เลย ปรับตัวเลข 47ch ให้ตรงได้ทันที**
- อัปเดต `CLAUDE.md` (จังหวะสไลด์ 10 วิ + บันทึกที่มาของเลข 47ch เผื่อต้องปรับใหม่ + เตือนว่าอย่าเปลี่ยนไปใช้ `<br>` ตายตัว)
- ℹ️ **มีการแก้ของเจ้าของร้านเองติดมาในคอมมิตนี้ด้วย** — ในไฟล์หน้าแรกบนเครื่อง ประโยคนี้ถูกแก้จาก `...หรือ 4 ขา ให้ทุกวัน...` เป็น `...หรือ 4 ขาให้ทุกวัน...` (ตัดช่องว่างระหว่าง "ขา" กับ "ให้" ออก) ซึ่งตรงกับประโยคที่เจ้าของพิมพ์มาในคำสั่งครั้งนี้พอดี จึงเก็บไว้ตามนั้น ไม่ได้แก้กลับ — และไม่กระทบการตัดบรรทัด เพราะเบราว์เซอร์ตัดคำไทยด้วยพจนานุกรม ไม่ได้อาศัยช่องว่าง

## ครั้งที่ 101 — 2026-07-30

**Prompt:**
> ในหมวดเมนูที่รวมทั้ง Home products Project Reference About us Contact แก้ลูกเล่นตรงที่ หน้าอยู่ในหน้าไหนไม่ต้องทำให้สีส้มมันขึ้นตลอดแก้ไขเอาแค่เวลาเอาเคอร์เซอร์เมาส์ไปชี้แล้วมีสีขึ้นแค่นี้

**สิ่งที่แก้ไข:**
- **เมนูของหน้าที่กำลังเปิดอยู่ไม่เป็นสีทองค้างไว้อีกแล้ว** — สีทองขึ้น **เฉพาะตอนเอาเมาส์ไปชี้** เท่านั้น (ทั้งตัวอักษรอังกฤษและคำไทยใต้เมนู) แก้ที่กฎ CSS ของเมนูใน**สไตล์ชีตทั้งสองไฟล์** (หน้าแรกที่ฝังในหน้า + `products/product.css`) โดยถอดตัวเลือก `.menu a.active` ออกจากกฎสี เหลือไว้แต่ `:hover`
- **ไม่ได้ไปลบ `class="active"` ในหน้าเว็บทิ้ง** เพราะ **คลาสชื่อเดียวกันนี้ถูกใช้กับจุดสไลด์รูปด้วย** — หน้าผลงานมี 7 จุด (จุดแกลเลอรี 6 + เมนู 1) หน้ารุ่นมี 4 หน้าแรกมี 2 ถ้ากวาดลบทั้งไฟล์จุดสไลด์ที่กำลังแสดงอยู่จะไม่เป็นสีทองอีกเลยทุกแกลเลอรี จึงแก้ที่ CSS ของเมนูจุดเดียว ปลอดภัยกว่า
- **ตรวจของจริงในเบราว์เซอร์แล้ว:** หน้าแรก เมนู `Home` (หน้าปัจจุบัน) ได้สีปกติ `rgb(233,236,242)` ไม่ใช่สีทอง · เอาเมาส์ไปชี้ `About Us` แล้วได้สีทอง `#d9a53a` + คำไทยใต้เมนู `#c9a75e` + พื้นจาง + ยกตัวขึ้นเล็กน้อย ครบเหมือนเดิม · หน้าผลงาน (ซึ่งใช้สไตล์ชีตอีกไฟล์) เมนู `Project Reference` ก็เป็นสีปกติแล้ว **และจุดสไลด์แกลเลอรีทั้ง 6 จุดยังเป็นสีทอง `#c0973f` ปกติ** ไม่ได้พลอยหายไปด้วย · วงเล็บปีกกาใน CSS ครบคู่ทั้งสองไฟล์
- **ปิดเรื่องที่ค้างจากครั้งที่ 100 ด้วย** — รอบนั้นเบราว์เซอร์ต่อไม่ติดจึงยังไม่ได้ยืนยันว่าประโยคในสไลด์ที่ 2 เหลือ 3 บรรทัดจริง รอบนี้ต่อติดแล้ว **วัดได้ 3 บรรทัดพอดี ยาว 460 / 452 / 474px** (ต่างกันไม่เกิน 5% ถือว่าสมดุล) ในกรอบกว้าง 654px และยังมีที่เหลืออีกราว 180px ก่อนจะตกไปบรรทัดที่ 4 คือแก้คำเพิ่มเล็กน้อยได้ไม่พัง — **ไม่ต้องปรับเลข 47ch แล้ว**
- อัปเดต `CLAUDE.md` (บันทึกว่าเมนูหน้าปัจจุบันไม่มีไฮไลต์แล้ว + เตือน 2 กับดัก: อย่าเพิ่มกฎ `.menu a.active` กลับ และอย่ากวาดลบ `class="active"` เพราะจุดสไลด์ใช้คลาสเดียวกัน + เปลี่ยนตัวเลขที่เคยเป็นการประมาณให้เป็นค่าที่วัดได้จริง)

## ครั้งที่ 102 — 2026-07-30

**Prompt:**
> ลบ 35% visible light ในหน้ารุ่นแต่ล่ะรุ่นที่อยูในกรอบสีน้ำเงินที่อยู่ฝั่งซ้าย และเรียงลําดับฟิล์มจากแพงไปถูกเช่น แบรนด์ finnix Film REGIONFILM BLACK CABON / Ultra Guard NANOCERAMIC / Ultra Guard SUPER CLEAR / HI-Kool CERAMIC BLACK NIGHT / SUPER HI-Kool BEYOND CERAMIC / 3M CERAMATE / 3M CERAMIC ULTRA CLEAR / FINNIX FILM UVGUARD / FINNIX FILM CERAMIC / FINNIX FILM EXTRA CLEAR / FINNIX FILM TITANIUM / FINNIX FILM CRYSTALIZE และแบรนด์อื่นๆแยกเป็นแต่ล่ะแบรนด์เหมือนเดิมแค่เรียงใหม่แต่เฉยๆ จัดการเลย

**สิ่งที่แก้ไข — 1) ลบป้าย VLT ในกรอบสีน้ำเงิน:**
- **ลบป้าย "NN% Visible Light" ในกรอบสีน้ำเงินฝั่งซ้ายของหน้ารุ่น ครบทั้ง 10 หน้าสเปก** (ตัวเลขต่างกันไปตามรุ่น — 15% / 25% / 35% / 40% / 50% / 70%) ลบทั้ง markup และ CSS ทั้ง 3 กฎ ไม่เหลือเป็นโค้ดตาย
- **กรอบสีน้ำเงินยังอยู่เหมือนเดิม** ไม่ได้หายไปด้วย เพราะการ์ดถูกกำหนดสัดส่วน 4:5 ไว้ ไม่ได้อาศัยป้ายในการกำหนดขนาด (เปิดดูจริงแล้ววัดได้ 490×612 เท่าเดิม)
- **ป้าย VLT ในการ์ด 3 เฉดด้านล่างไม่ได้แตะ** (35% / 15% / 5% ในหน้า Finnix Ceramic) เพราะเป็นคนละส่วน ไม่ใช่กรอบสีน้ำเงินฝั่งซ้ายที่สั่งมา — ตอนนี้ตัวเลข VLT เหลืออยู่แค่ในการ์ดเฉดเท่านั้น

**2) เรียงลำดับฟิล์มจากแพงไปถูก:**
- **ลำดับแบรนด์ใหม่: Regionfilm → Ultra Guard → 3M → Finnix** (เดิม Finnix → 3M → Regionfilm → Ultra Guard)
- **ลำดับรุ่นในแต่ละแบรนด์:** Ultra Guard = Nanoceramic → Super Clear (เดิมถูกอยู่แล้ว) · 3M = Ceramate → Ceramic Ultra Clear (สลับ) · Finnix = UV Guard → Ceramic → Extra Clear → Titanium → Crystalize (เดิม Ceramic → Titanium → UV Guard → Extra Clear → Crystalize)
- **แก้ให้ตรงกันครบ 5 จุดที่มีการเรียงลำดับ:** เมนูดรอปดาวน์ (17 หน้า) · คอลัมน์ "ฟิล์มของเรา" ใน footer (17 หน้า) · แถวโลโก้ 4 แบรนด์หน้าแรก · การ์ดเลือกรุ่นในหน้าแบรนด์ Finnix / 3M / Ultra Guard
- **ยังแยกเป็นแต่ละแบรนด์เหมือนเดิม** ไม่ได้รวมเป็นรายการเดียว ตามที่สั่ง
- **ตรวจแล้ว:** เปิดดูของจริง เมนู/แถวโลโก้/footer ขึ้นลำดับ Regionfilm → Ultra Guard → 3M → Finnix ตรงกันหมด · การ์ดรุ่นในหน้าแบรนด์ทั้ง 3 หน้าเรียงตามที่สั่งครบ · ไม่มีลิงก์เสีย (เช็คทุก href ว่าไฟล์มีจริง) · แท็ก `<div>` `<ul>` `<li>` ครบคู่ทุกหน้า

**⚠️ มี 3 รายการในลิสต์ที่ยังไม่มีในเว็บ จึงยังไม่ได้ใส่ (ไม่ได้สร้างเองเพราะไม่มีข้อมูลกับโลโก้):**
- **HI-Kool CERAMIC BLACK NIGHT** และ **SUPER HI-Kool BEYOND CERAMIC** — **แบรนด์ HI-Kool ยังไม่มีในเว็บเลย** ไม่มีหน้าแบรนด์ ไม่มีหน้ารุ่น ไม่มีโลโก้ใน `assets/brands/` ตามลำดับราคาที่ส่งมาควรแทรกอยู่ระหว่าง Ultra Guard กับ 3M **ถ้าจะเพิ่ม ส่งโลโก้กับสเปกมาได้เลย** (ต้องทำหน้าแบรนด์ + หน้ารุ่น 2 หน้า + แก้หัวข้อหน้าแรกจาก "4 แบรนด์ชั้นนำ" + ขยายความสูงเมนูดรอปดาวน์)
- **REGIONFILM BLACK CABON** — หน้า Regionfilm ในเว็บใช้ชื่อแค่ "ฟิล์ม Regionfilm" ไม่มีคำว่า Black Carbon **ถ้าชื่อจริงคือ Regionfilm Black Carbon บอกได้ จะแก้ชื่อให้ทั้งหน้า** (ยังไม่แก้เพราะรอบนี้สั่งแค่เรียงลำดับ)
- อัปเดต `CLAUDE.md` (ผังหน้าเรียงใหม่ + บันทึกว่าลำดับราคาต้องแก้ให้ตรงกัน 5 จุด + เตือนไม่ให้สับสน `.vlt` กับ `.sc-vlt` + บันทึกรายการที่ยังไม่มีในเว็บ) และ `README.md` (ผังการเชื่อมหน้า + หมายเหตุ 3 รายการที่รอข้อมูล)
