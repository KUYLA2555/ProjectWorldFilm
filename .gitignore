# ไฟล์ต้นฉบับงานออกแบบ — ไม่อัปขึ้น GitHub
assets/source/

# ไฟล์ระบบ / โปรแกรมแก้ไข
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
*.tmp

# กล่องรับรูปจากเจ้าของ — ไฟล์ต้นฉบับขนาดใหญ่ (รวม ~68 MB) และเป็นรูปลูกค้าจริง
# เก็บไว้ในเครื่องได้ตามปกติ แต่ห้ามขึ้น GitHub เพราะ push = ขึ้นเว็บจริงทันที
assets/PIC/
assets/PIC_HOME/
assets/WEB/
